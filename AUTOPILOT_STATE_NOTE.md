Historical A1 continuation note (2026-08-27; superseded): the first Visual V2 baseline run failed in the new harness. The harness was patched in commit `1a090cff` so the Playwright script runs from the repository root and can resolve the installed `playwright` package. This is retained only as operational evidence. `START_HERE.md` and `AUTOPILOT_STATE.json` contain the current resume point.
