Current A1 continuation note (2026-08-27): the first Visual V2 baseline run failed in the new harness. The harness was patched in commit `1a090cff` so the Playwright script runs from the repository root and can resolve the installed `playwright` package. Next action: inspect the rerun, collect visual artifacts/report, then fix the highest-priority real visual regression. This note is temporary operational evidence; `AUTOPILOT_STATE.json` remains the machine-readable queue source of truth.