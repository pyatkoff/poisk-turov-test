# Search3 — актуальный план доработки продукта и сайта

Рост CSS/JS разрешён ради реального улучшения продукта. Уточнение владельца от 2026-09-09: быстрее использовать удачные компоненты предыдущей оформленной версии как доноры. Адаптировать их к текущей разметке и заменять правила в одном актуальном владельце; старые подключения, decorators и дополнительные JS-слои не возвращать.

## Текущий checkpoint — календарь после догрузки и точные окна дат #1710/#1713 — 2026-09-09

#1710 начинает публичное окно цен с первой даты выбранного сочетания города вылета и ночей. Ранний тур другой длительности больше не отодвигает окно от подходящих предложений. #1713 сохраняет локальную выборку питания/названия/категории на search-complete/continued, включая пустой результат и выбор фильтра до завершения поиска. Исправлены текущие владельцы, без новых слоёв.

Source `48a717adc4105e6f000b26ae38e8456850b08ede` / `840cd2793932e67e0377f5bfc13b325ad4801569`; checked code release `ea3c146c1c5f6ab92a520eb71732db6fb1a7364e`. Новый суммарный вес: восемь assets 0 B, CSS 0 B, shared JS +83 B, initial/full external +83 B, inline 0 B. PHP mixed-duration/month smoke, Security34333682362/34334637089 и exact34333781969/34334821949 passed. Исходный красный34334354503 был ошибкой identity fixture; строгая проверка исходных объектов сохранена.

**Опубликован накопленный preview:** source `840cd2793932e67e0377f5bfc13b325ad4801569`, artifact10097233750/run34334637090, deploy34335163839, evidence10097405094. Существующие provenance, payload, noindex, disabled leads, counter0, rollback и13 production fingerprints прошли. Main `41ec8876c5c92a4a9d1b71d7387dcae6b5efee66` и production не менялись. Контрольные #1714/#1715 закрыты без merge.

Важная поправка статуса: helper commit-workflow не показал push-запуски. #1714 уже успешно опубликовал этот же артефакт в34335032534; #1715 непреднамеренно повторил его в34335163839. Оба успешны, прежняя запись «не запущен» исправлена. Перед повтором deploy проверять полный `/actions/runs?branch=...` с event=push; не повторять эту публикацию.

Live1363: главная блокирует handoff до готовности стран; после готовности сохраняет параметры. AI:48/100 отелей, минимум62475→70747 ₽ и11→9 дат; ноль скрывает календарь, очистка и сортировка восстанавливают правильную выборку. Реальная догрузка завершилась без новых вариантов и сохранила70747 ₽. Сентябрьский CTA:10–30 сентября. ARSI PARADISE:85089 ₽,4 рейса; выбор2 даёт TK3013/TK3070 и ту же сводку, CTA фокусирует телефон, возврат сохраняет48 AI-отелей. Заявок0. Осмотрены главная, пустое состояние/подвал и selected/форма; live mobile/Safari и полный site/SEO deferred. Публичных наблюдений для mixed-duration календаря в live месячной странице нет; этот случай подтверждён PHP CI.

**Следующий `S3_PRODUCT_CURRENT_ACCEPTANCE`:** завершить оставшиеся mobile/Safari/site/SEO проверки доступными средствами. Конкретное наблюдение: в описании ARSI виден literal `м&#178;`; проверить text-decoding в каноническом display owner и исправить только при подтверждении, без HTML injection и изменения payload. Документы не требуют deploy. Готовые этапы и ANEX не дублировать; production только после одобрения конкретной версии.

Audit: `docs/project/search3-calendar-acceptance-corrections.json`.

## Текущий checkpoint — точные даты перехода hot/month → Search3 #1699/#1700 — 2026-09-09

Два последовательных пакета завершили `S3_PRODUCT_PAGES_HANDOFF`. [#1699](https://github.com/pyatkoff/poisk-turov-test/pull/1699) выровнял диапазон CTA «Горящих туров» с московским бизнес-днём свежести офферов: системный часовой пояс PHP больше не может сдвинуть поиск на сутки. Source `cbeaab4fc07f5c0ded25a50abbec301cc8ea25b2`, release `87e56008989060a54f40d42a5aab5a1eac16bfdc`.

[#1700](https://github.com/pyatkoff/poisk-turov-test/pull/1700) перенёс месячные country/resort handoff в тот же канонический владелец `offer-freshness-v1.php`. Направление и месяц сохраняются, прошедшая часть месяца отсекается, полностью прошедший месяц не получает ложный диапазон. Удалён дублирующий расчёт `tomorrow` из seasonal renderer. Source `e90286ed063b66e0c22845c6f8d22b002ce47e70`, checked release `b3da2002305b14a97d058de1d4b0edbe053c622a`.

Country/resort offer-card CTA уже сохраняют country/region, город вылета, точную дату, ночи и правдивый состав snapshot `2 взрослых`; generic `/rb/` не имеет выбранных параметров для переноса. Восемь Search3 assets, начальный и полный внешний CSS/JS не менялись (**0 B**). Security `34328003803/34328644467`, exact `34328173433/34328788619`, boundary `34328173404` и standalone `34328173427` зелёные. Exact PHP-smoke покрывает обе стороны московской полуночи, начало/окончание месяца и Новый год; заявок 0.

Preview не публиковался: изменение невизуальное. Published source остаётся `8382d1bd8543fd3f0efb623673d1b70fa2be823d`; main observed `41ec8876c5c92a4a9d1b71d7387dcae6b5efee66`, main/production не менялись. Live mobile/Safari и полный site/SEO остаются deferred до накопленной приёмки.

**Следующий `S3_PRODUCT_USEFUL_FEATURES`:** проверить существующий календарь цен на публичных country/resort/month страницах и в Search3 на конкретное расхождение состояния или удобства. Не повторять готовые name/category/meal facets и не дублировать отдельную ANEX-ветку.

Audit: `docs/project/search3-pages-handoff-product.json`.

## Текущий checkpoint — безопасный переход с главной и один владелец CTA #1693/#1694 — 2026-09-09

Два пакета выполнены последовательно. [#1693](https://github.com/pyatkoff/poisk-turov-test/pull/1693) блокирует обе GET-точки перехода с главной до готовности списка стран: disabled `country` больше не выпадает из `FormData`, Enter не обходит блокировку, а запоздалый ответ для старого города не перезаписывает актуальные страны. После готовности нативная отправка и «Расширенный поиск» сохраняют страну. Source `276fca32150cb5f4d0dff28736293e6bae98670a`, release `9a020881f73237d726e1587eeae0fd61ade62e1e`.

[#1694](https://github.com/pyatkoff/poisk-turov-test/pull/1694) завершает shared CTA convergence: из `site-page-v1.css` удалены generic geometry/colour/hover/mobile-width правила `.sp-primary/.sp-secondary`; единственный поздний владелец — `shared-content-primitives-v1.css`. Контекстные card-primary и тёмный search-callout сохранены. Source `514d7fa92def05c65a1945266367dc8623c0d142`, checked release `865ee7b6b0f6e7b2c663393fcec08e24f1b6d94a`.

Восемь Search3 assets не менялись (**0 B**). Homepage CSS `10415→10581 B (+166)`, inline controller `3458→4243 B (+785)`; shared `site-page-v1.css 11283→10716 B (−567)`. Security `34325953808/34326518960`, exact `34326140281/34326631635`, navigation `34326140349` и standalone content `34326140267/34326631536` зелёные. Новый детерминированный тест покрывает initial loading, оба CTA и rapid-departure race; source-order guard не позволяет вернуть второго CTA-владельца. Заявок 0.

Preview не публиковался: первый пакет меняет короткое loading-состояние, второй сохраняет computed presentation. Published source остаётся `8382d1bd8543fd3f0efb623673d1b70fa2be823d`; main observed `41ec8876c5c92a4a9d1b71d7387dcae6b5efee66`, main/production не менялись. Live mobile/Safari и новый визуальный осмотр deferred до накопленного визуального пакета.

**Следующий S3_PRODUCT_PAGES_HANDOFF:** проверить оставшиеся country/resort/hot/rb offer CTA на потерю направления, дат, ночей и состава туристов при переходе в Search3; исправлять только подтверждённый дефект в текущем route/helper owner. Homepage loading/race и shared CTA owner не перезапускать; ANEX остаётся отдельно.

Audit: `docs/project/search3-shared-control-owner.json`.


## Текущий checkpoint — правдивый фильтр питания и фото #1686/#1687 — 2026-09-09

Два source-пакета завершены последовательно и опубликованы вместе. #1686 переносит ровно два файла owner main#1679: CDN URL вида `//…` нормализуются в HTTPS, опасные URL отклоняются. Source `d75d21d31520142b864d965997f310840de80280`, release `f814829e2c7e397a83191e24836cb599d9db3ef8`. CSS/JS delta0; это подготовка нормализации, не обещание восстановления старых записей БД. Collector/DB не запускались.

#1687 добавляет питание в существующий local-hotel-filter. Карточка, сортировка и раскрытие используют только подходящие исходные туры. Исходные state/event items, continuation count, tour references и payload сохранены; name/category/meal имеют одного владельца `card.hidden`. Неполные данные скрывают и сбрасывают фасет, dirty/reset проверены. Source `8382d1bd8543fd3f0efb623673d1b70fa2be823d`, tree `89830004db93c05bf1edfa19a86a970f61fc2a2b`, checked code release `598269e241391aae34112d835596f56d089fd6d7`. Основа оформления по-прежнему DS2 donor17b674fc; старые слои не возвращены.

Восемь assets **26579→28103 B (+1524)**: CSS18604→18677 (+73), JS7975→9426 (+1451). Shared emitted JS +80, inline delta0. Точный начальный и полный внешний CSS/JS **162859→164463 B (+1604)**; source/exact artifact совпадают. Учтены общие подключения и разделители ответа; HTML/fonts/images/third-party и inline totals не входят. Дополнительный selected-request0, перенос или lazy-loading экономией не называются.

Security34321149428/34321878627 и exact34321149460/34321878643 зелёные. Оба owner validators локально passed; ready-source Security проверил owner direction, его draft-only validator step штатно skipped. Docs checkpoint запускает существующий draft gate. Цена A RO90000/AI120000 против B AI100000, сортировка/раскрытие/сброс/неполные данные/escaping проверены raw+served375/1440; существующие16 result states на8 ширинах, native entry375/1440, source/PHP/path/presentation/isolation guards passed. Первый exact34321465874 был красным на устаревшем `version:2`; теперь assertion требует ровно current apply/clear/project/version3 owner и прежние guards сохранены. Test-only исправление без rebuild. Photo PHP smoke прошёл на точном donor34318224463, release exact выполнил PHP lint; локального PHP нет, повторный local smoke не заявлен.

**Published preview:** source `8382d1bd8543fd3f0efb623673d1b70fa2be823d`, artifact10092207048, digest `sha256:53355759364989ffa18505d07176cb831e45955dcc09f82243cdd4d35bfec9a8`, deploy34322213328. Control#1689 закрыт без merge. Evidence10092314613 подтверждает9 routes, noindex, disabled production leads/counter0, rollback и13 неизменных production fingerprints. Main `41ec8876c5c92a4a9d1b71d7387dcae6b5efee66` не менялся этой работой.

Live1363:79 отелей; AI оставляет25, вместе с SUN VERA —1. У SUN VERA исходные21 тура/BB87235 ₽ заменяются5 подходящими AI от94956 ₽; все5 раскрытых строк с AI, сортировка сохраняет питание. Сброс питания возвращает21 тур/87235 ₽, очистка названия клавишей —79 карточек. Overflow0, заявок0. Осмотрены live toolbar/card и CI375/1440. Live mobile/Safari/полный site+SEO/selected lead path deferred. Известный исторический flight-presentation test с удалённым helper не повторялся и не называется зелёным.

**Следующий S3_PRODUCT_SHARED_SITE:** продолжить после уже завершённого #1658 — ограниченно проверить header/nav/footer/container/secondary-button/link состояния поиска, главной и страны на mobile/desktop. Исправить подтверждённое расхождение в одном текущем DS2-владельце с удалением заменённого правила. Если расхождений нет — S3_PRODUCT_PAGES_HANDOFF, сохранение параметров country/hot→search. Завершённые форма/выдача/рейсы/питание не перезапускать; ANEX отдельно.

Audit: `docs/project/search3-local-meal-facet-product.json`. После двух source PR, исправления CI и общей публикации ресурс текущего прохода отведён на checkpoint; дальнейшая разработка остаётся активной.

## Предыдущий checkpoint — удобный выбор рейсов и питание #1680/#1681 — 2026-09-09

Два source-пакета завершены последовательно и опубликованы одним exact artifact. Основа оформления остаётся DS2-донор `17b674fc54fa6b49aaa73379bcbdc35bfccfda28`, адаптированный к текущим владельцам.

- **#1680:** большой список рейсов сворачивается до выбранного варианта; кнопка раскрывает все исходные radio в ограниченной по высоте панели. Порядок, индексы, цены и маршруты сохранены; короткие списки не меняются. Исправлен актуальный retry selector в том же CSS-владельце. Source `cdf760bb610b43fdb2805584bd7d96abe20623bb`, tree `32f0604d7c526c8b57f7399b485ad54ac62f9f26`, release `5f2e12a8358062477f42d8c2dcbccbd882704474`.
- **#1681:** один `V2Results.mealLabel` для выдачи и выбранного тура раскрывает сокращённые/пустые названия через `meal.fullName`. Приватный helper и поле питания в leadPayload сохранены. Source `0754f3dbb52d715c82a1f7212c85ada65fb659d6`, tree `46664ef73ef1ac805693c20d9a0572975af86f6c`, checked code release `1569e01a721d16374c6eb87b70c00a3c1123ebc7`.

Восемь assets **25210→26579 B (+1369)**: CSS +462, JS +907. Второй пакет добавляет183 B общего собранного JS и не меняет восемь assets. Точный внешний CSS/JS страницы: **161307→162676→162859 B (+1552)**, одинаково для начальной загрузки и полного selected-сценария. Учтены восемь assets и оба общих bundle вместе с разделителями ответа; inline delta0. HTML, fonts/images и third-party сюда не входят.

**Исправление старого учёта:** текущая страница уже загружает shared JS `all` сразу. Старые абсолютные eager/full цифры и selected-фаза42418 B в исторических checkpoint ниже больше не описывают загрузку. Дополнительный selected-request равен0; источник и exact опубликованный artifact дают одинаковые162859 B. Это исправление измерения, не удаление или lazy-loading экономия.

Security **34318264527 / 34318776520**, exact **34318264521 / 34318776481** прошли на финальных source. Текущий CI проверил89 вариантов на375/1440: раскрытие/сворачивание, последний вариант, ArrowUp, исходные индексы, совпадение цены/сводки и фокус телефона; существующие selected/result/source/PHP/path/isolation guards зелёные. Питание проверено на11 формах данных, escaping и неизменном полном payload. Первый exact #1681 `34318622993` был красным из-за полного исторического hash controller. Исправление разрешает ровно одну reviewed display expression, обращает только её и проверяет прежний полный hash: остальной controller остаётся заблокирован byte-for-byte. Дополнительной сборки после test-only исправления не было.

**Published preview:** source `0754f3dbb52d715c82a1f7212c85ada65fb659d6`, exact artifact `10091093868`, digest `sha256:47039ae87856dd47242b71da1f597ed1e2724ce8d89febf883f59bb4904852c4`, deploy `34319053098`. Control #1682 закрыт без merge. Evidence `10091160692` подтверждает9 маршрутов, noindex, disabled production leads, counter0, rollback и13 неизменных production fingerprints. [Посмотреть preview](https://anytoour.ru/_preview/search3-site-candidate/poisk-turov/).

Короткая live-проверка1363px:100 отелей, подпись BB совпадает в выдаче/selected; ARES CITY дал4 рейса, SUN VERA1 рейс, цена80032 ₽ и TK3025/TK3024 совпали со сводкой, телефон фокусируется; overflow0, заявок0. Осмотрены live-форма и CI375/1440. Живой большой набор не найден в текущей выдаче, поэтому live collapse/expand **deferred**,89 вариантов подтверждены именно CI. Live mobile/физический Safari/полный site+SEO/production acceptance deferred. Исторический `tests/search3-flight-presentation.cjs` остаётся красным на уже удалённом booking/format helper и не входит в текущие обязательные source gates; не выдаётся за passed.

Main независимо обновлён владельцем через #1679: `86fc165277a13ae9bef1369659e9b150399f0e35`→`41ec8876c5c92a4a9d1b71d7387dcae6b5efee66` (scheme-relative hotel photos). Эта работа main не изменяла, source preview эту независимую правку ещё не включает; production fingerprints публикации неизменны.

**Следующий `S3_PRODUCT_USEFUL_FEATURES`:** сначала освежить release/main и оценить независимый перенос уже объединённого #1679 в release. Затем реализовать правдивый локальный фильтр питания: исходные `state.items`, event items и continuation count сохранить; matching tours проецировать в отдельные renderer viewItems до выбора цены, сортировки и раскрытия. Пример проверки: A RO90000/AI120000, B AI100000 — при AI сначала B, у A120000, в раскрытии нет RO; сброс возвращает исходное. Неполные progressive-данные скрывают/сбрасывают фасет; dirty/reset не поднимает старую выдачу. Один владелец `card.hidden`, без новых API и без ANEX-дублирования.

Audit: `docs/project/search3-flight-meal-product.json`. После двух source PR и общей публикации сохранена конкретная точка продолжения; продуктовая очередь и регулярная разработка остаются активны.

## Предыдущий checkpoint — восстановление DS2 выдачи и выбранного тура #1675/#1676 — 2026-09-09

Два пакета выполнены последовательно без остановки после первого PR. Донор `17b674fc54fa6b49aaa73379bcbdc35bfccfda28`: карточки/toolbar из прежнего DS2 и композиция selected-tour. Единственный актуальный CSS-владелец — `src/search3/styles/results-layout.css`.

- #1675: фото и ясная иерархия карточек, сетка фактов, цена/CTA и локальные фильтры. Канонический renderer дополнительно читает `meal.fullName`; lead payload и controller не менялись. Source `c9d1ddd6add646616498ae501dd91be1db96837b`, checked release `dc0abe36a9b9844f7078f666a67150b35eb3692f`. Восемь assets `15986→19578 B` (+3592), общая начальная загрузка +3628 B с учётом36 B shared JS.
- #1676: целиком заменён selected CSS-блок — header/цена/фото, facts, рейсы, форма и сводка контакта. Source `3e33d4addb9317b78434e92fcbc56851416a0abb`, tree `e5aba6a88f8edd1fe30b8e59dfe450c7742cf3e7`, checked release `417bf7d2d3024a6c59c3367dc1f1a43a82431e04`. Восемь assets `19578→25210 B` (+5632); JS/price/lead/API не менялись.

Итого восемь публичных assets `15986→25210 B` (**+9224**); внешний eager CSS/JS `87339→96599 B`, полный сценарий `129757→139017 B` (**+9260**). Selected-фаза42418 B, inline без изменений. Это полезный eager-прирост, не экономия и не lazy-перенос. Исторический лимит одного CSS owner изменён под этот разрешённый пакет:16408 B при бюджете17000; guards владельцев/путей/безопасности/изоляции сохранены.

Security `34315997361`/`34316326525` и exact `34315997382`/`34316326507` прошли. Результаты:16 состояний на375/760/761/999/1000/1024/1025/1440 с полным raw/served parity; selected:8 состояний на375/760/1000/1440, цена, radio-switch, один переход к телефону и исходные contracts. Первые два запуска #1675 были красными из-за гонки offline fixture: сообщение о блокировке каталогов вставлялось между snapshots. Ожидание канонического recovery исправило измерение; проверки не ослаблены.

Опубликован только exact source `3e33d4addb9317b78434e92fcbc56851416a0abb` в [whole-site preview](https://anytoour.ru/_preview/search3-site-candidate/poisk-turov/). Artifact `10090230450`, digest `sha256:85a6a7a9219446c930863307dbef867ed9991bd3476c29519ef999624e112e30`; deploy `34316577672`, control #1677 закрыт без merge. Evidence `10090282179`;9 маршрутов/noindex/leads disabled/counter0/rollback/13 production fingerprints прошли. Main `86fc165277a13ae9bef1369659e9b150399f0e35`, main/production не менялись.

Живая desktop-проверка1363px:100 отелей; реальное фото220px и CTA50px; ANAHTAR APART,89 рейсов; выбор варианта2 меняет67314→84519 ₽ (+17205), сводка совпадает, телефон фокусируется, возврат даёт100 карточек. Overflow0, реальных заявок0. Скриншоты live-выдачи/формы и CI375/1440 осмотрены. Живой mobile/физический Safari и полная site/SEO/production приёмка deferred.

**Следующий конкретный шаг `S3_PRODUCT_SELECTED_LEAD`:** улучшить работу с большим списком рейсов — реальные89 альтернатив растянули выбранный тур примерно на12509px. Сохранить доступ ко всем radio/ценам/маршрутам, выбранные сегменты и один CTA; не возвращать старые drawers/stepper и не менять lead payload. В этот же связный пакет включить актуальный retry selector `.retry-tour` вместо неработающего декоративного `.tour-load-retry`. Затем вернуться к понятным названиям питания и правдивому локальному meal-фасету; ANEX-сопоставление отдельно.

Audit: `docs/project/search3-ds2-restoration-product.json`. Текущие план/current_task/active_queue_ids авторитетны; нижние checkpoint и сокращения — история.

## Предыдущий продуктовый checkpoint — фасет точной категории #1672 — 2026-09-09

Source `d69bb12907cf6bb890f9f86565b16a173825d96d`, tree
`c6ab0497bb375ed5effb10ef79908c055a981c6a`; checked release
`b6c1837a817562100355586205366d5bad6c208a`. Существующий единый владелец
локального поиска теперь предлагает точную категорию отеля только когда все
текущие загруженные карточки имеют положительную категорию и есть минимум две
разные категории. Название и категория объединяются до одной записи `card.hidden`;
выбор сохраняется при сортировке и сбрасывается при новом поиске. При неполной
прогрессивной выборке категория сразу сбрасывается и контроль скрывается.

Свежий аудит Турции, Египта и ОАЭ охватил 234 отеля / 843 тура: категория полна
для 220 отелей (94,0%), рейтинг для 212 (90,6%), расстояние до моря для 101
(43,2%), текущее отображаемое питание для 210 (89,7%). Поле `meal.fullName`
присутствовало у всех 843 туров, но канонический renderer его пока не читает,
поэтому отдельный фасет питания сейчас был бы преждевременным.

Восемь Search3 assets: **14677→15986 raw B (+1309)**; eager route
**86030→87339 B**, полный сценарий **128448→129757 B**, selected-фаза остаётся
42418 B. Это честный функциональный прирост в существующем eager-владельце.

Security `34312454899` и exact artifact `34312455035` / `34312459604` зелёные.
Переиспользован artifact `10088877773`, digest
`sha256:6f905758b370e581b618b16bd80cb9c3b9c01f094c82e5e38cab9ea16fd5fe6b`.
CI проверил 16 состояний на 375/760/761/999/1000/1024/1025/1440, точное
совпадение категории, сохранение при сортировке, сброс/скрытие на неполных данных,
raw/served parity, отсутствие overflow, внешних вызовов и заявок.

Deploy `34312777229` через закрытый без merge control #1673 опубликовал exact
source только в isolated preview. Evidence `10088967073`, digest
`sha256:272ec5c425952db66cef1cc7d0d05ef7b35d650488b3ff79c2113c3ab3da8c15`.
Живой desktop 1363 px: на ранних 25 полных отелях фасет появился с 2★/3★/4★;
после догрузки 100 отелей с неполной категорией корректно исчез. Noindex и нулевой
overflow подтверждены; заявок 0. Main observed
`86fc165277a13ae9bef1369659e9b150399f0e35`, main/production не менялись.

Следующий шаг: добавить `meal.fullName` в каноническую нормализацию текста
renderer с узкими fixture/source-проверками и без изменения price/API/lead/lifecycle,
затем повторно оценить полноту и полезность локального фасета питания.

Audit: `docs/project/search3-local-category-facet-product.json`.

Авторитетные точки: этот план, `OWNER_PRIORITY.json.active_roadmap`, `AUTOPILOT_STATE.json.current_task`, issue #996. Начать с формы/подвала/сводки; затем выдача, выбранный тур и остальной сайт. Широкий технический рефакторинг и массовое SEO по-прежнему отложены.

## Работа без остановки после одного PR

Прямое правило владельца: **не останавливаться после одного PR, если в текущем запуске есть время на следующий безопасный шаг**.

1. Читать этот план, свежие current_task/state, AGENTS, OWNER_PRIORITY и комментарии #996/#1334; сверять release/main, открытые рабочие PR и выполняющиеся CI/deploy.
2. Завершать начатый полезный пакет или брать ближайший независимый пункт. Не повторять завершённое, не дублировать чужую работу, не force-push.
3. Один финальный build/CI-цикл на связный source-пакет. Красный обязательный CI исправлять в том же PR; красный код не интегрировать.
4. После зелёных применимых проверок интегрировать в release; успешный source artifact переиспользовать. Документация не требует asset rebuild, браузера или deploy.
5. Готовый значимый визуальный пакет публиковать штатным exact artifact в isolated whole-site preview; коротко проверять изменённые состояния.
6. Записывать точные code/release/preview SHA, пользу, изменение веса, проверенное/deferred и конкретный следующий шаг в состоянии и #996; #1334 хранит сводку.
7. Пока есть время, сразу переходить к следующему безопасному шагу в этом же запуске. Завершённый PR, ожидание production-приёмки и один заблокированный пункт не являются причиной остановки.
8. При блокере записывать точную причину и брать независимую задачу. Не обходить отказы инструментов/права, обязательные проверки или защищённые контракты; не тратить запуск на повтор одинаковых проверок.
9. Завершать запуск при исчерпании доступного ресурса, выполненной очереди либо отсутствии безопасной доступной задачи. Сохранить точку продолжения.

Ежечасное расписание возобновляет работу между запусками. Это не бесконечный один процесс: внутри запуска продолжается следующий безопасный шаг, после окончания ресурса сохраняется состояние для следующего. Обновляется существующая автоматизация, вторая для той же задачи не создаётся.

## Идентификаторы очереди

1. `S3_PRODUCT_FORM_READABILITY` — Group native form; footer/lead-summary spacing.
2. `S3_PRODUCT_RESULTS_CLARITY` — Clear result cards and truthful search states.
3. `S3_PRODUCT_SELECTED_LEAD` — Flight options, selected total and contact handoff.
4. `S3_PRODUCT_SHARED_SITE` — Shared DS2 site components.
5. `S3_PRODUCT_PAGES_HANDOFF` — Whole-site content and parameter-preserving handoff.
6. `S3_PRODUCT_USEFUL_FEATURES` — Useful calendar/facets and separately ready ANEX integration.
7. `S3_PRODUCT_CURRENT_ACCEPTANCE` — Current whole-site/mobile/Safari acceptance.
8. `S3_PRODUCT_APPROVED_MIGRATION` — Owner-approved concrete production migration.

## Подробный согласованный план

**AnyTour / Search3 — план доработки поиска и сайта**

Дата: 8 сентября 2026. Целевое превью: https://anytoour.ru/_preview/search3-site-candidate/poisk-turov/

Цель: довести путь от выбора направления до заявки до удобного, цельного продукта с понятной архитектурой. Рост CSS/JS допустим, когда он улучшает визуал, удобство или функциональность. Размеры учитываются вместе с результатом для пользователя.

Уточнение владельца после первой версии плана: «Я не против роста размера файлов если это реально улучшит визуал и функциональность. Но не когда мы 15 слоев друг на друга накладываем». Это уточнение заменяет предложенные ранее жёсткий потолок 12 КБ, нулевой рост и обязательное уменьшение лимита после каждого сокращения. Главное требование — не накапливать дублирующие реализации и переопределения. Отдельное согласование каждого прироста байтов не требуется.

**Исходная точка**

План составлен по свежим исходникам release/search3-production-ready-v1, документам состояния и последним комментариям #996. Ветка при чтении: d87ffe4f15c868dc28723593c90c9752998435b8. Последняя публикация по журналу проекта: source 89a5a8c37b3c5e5473f8f0d99b9ce19c4b51cbf1, deploy 34286947471.

Прямой осмотр живого превью в этой сессии не состоялся. Описанные ниже результаты прежних проверок взяты из журнала публикации; будущие улучшения не выдаются за подтверждённые текущие дефекты.

После удаления прежних слоёв уже опубликовано компактное оформление формы, карточек, выбранного тура и заявки. Исправлено повторное появление формы поиска над выбранным туром. В последнем записанном живом проходе поиск вернул 96 отелей, выбранный тур открылся, возврат восстановил выдачу, редактирование сохранило восемь основных параметров. Повторять эти работы с нуля не требуется.

Подтверждённая очередь из последнего отчёта: группировка полей формы, отступы подвала и сводки заявки, удобное представление длинных списков рейсов. Физический Safari и итоговая визуальная приёмка остаются открытыми.

**Учёт размера и защита от дублирующих слоёв**

Все размеры ниже — байты без HTTP-сжатия; 1 КБ в этом плане = 1 000 байт.

| Что считаем | База опубликованного source | Правило для ближайших пакетов |
| --- | ---: | --- |
| Восемь собственных файлов интерфейса Search3 | 11 984 B: CSS 5 377, JS 6 607 | База сравнения; рост допустим ради визуального или функционального улучшения |
| Начальные HTTP-тела собственных внешних CSS/JS | 73 840 B | Оценивать прирост вместе с пользой и влиянием на загрузку |
| Отложенная загрузка выбранного тура и заявки | 42 310 B | Историческая фаза опубликованного source; в checked release #1640 включена в initial. Не восстанавливать автоматически |
| Полный набор этих HTTP-тел за путь поиска | 116 150 B | Оценивать прирост вместе с пользой и влиянием на загрузку |
| Inline CSS/JS, общий код остальных страниц, внешние скрипты | Полный маршрутный замер ещё не выполнен | Учесть вместе с первой доработкой; без отдельного этапа ради замеров |

В 73 840 / 116 150 B входят тела собственных внешних ресурсов и разделители общего JS endpoint. Эти суммы не включают HTML, inline-код, изображения, шрифты и сторонний виджет. Их нельзя называть всем сетевым весом страницы. Общий учёт дополняется этой оставшейся частью; перенос в inline или общий файл не считается экономией.

На исходной проверенной версии tests/search3_production_presentation_test.py порог восьми файлов равен **89 174 B**, а документация указывает 12 000 B. Сам по себе такой порог не предотвращает дублирование слоёв. По уточнённому направлению владельца не ужесточать его механически до 12 000 B: привести документы и существующую проверку в соответствие с допустимым полезным ростом, сохранить отчёт «до → после» и имеющиеся проверки состава сборки. Учёт первоначального и полного набора ресурсов встроить в ту же сборку, без нового конвейера и блокировки улучшений только из-за прироста байтов.

Открытый на момент исходного плана PR #1640 показывает проблему одной метрики: по его описанию, общий объём уменьшается 116 150 → 114 263 B, но первоначальная загрузка увеличивается 73 840 → 114 263 B, поскольку код выбранного тура становится ранним. Это реальное сокращение общего кода на 1 887 B и одновременно рост старта на 40 423 B. Оценивать такой пакет по упрощению архитектуры, удобству выбора тура и влиянию на загрузку. Один прирост байтов не означает автоматический отказ, как и уменьшение общего веса не доказывает ускорение. В этом плановом проходе PR не изменялся.

**Порядок работ**

| Пакет | Что сделать | Когда пакет готов |
| --- | --- | --- |
| 0. Учёт и карта компонентов вместе с первым пакетом | Учитывать первоначальный/полный вес и inline-код; обновить карту владельцев; отразить допустимость полезного роста; учесть параллельный #1640 | Понятно, что выросло, какую пользу дало и какие прежние реализации удалены; визуальные улучшения не задерживаются отдельным этапом замеров |
| 1. Форма поиска и базовая читаемость | Сгруппировать направление, даты, ночи и туристов; сделать ясной связь минимального/максимального значения; сохранить нативные даты, select и возраста детей. Одним пакетом исправить отступы подвала и сводки заявки | Форма понятна на телефоне и компьютере; исходные параметры и введённые значения сохраняются; улучшение реализовано без второго конкурирующего слоя оформления |
| 2. Выдача и сравнение предложений | Выстроить иерархию фото → название/курорт → существенные условия → общая цена → действие. Удалить повторяющиеся факты; выровнять длинные названия и цены; ясно показать ход поиска, пустой результат, ошибку и повтор | За несколько секунд понятно, что предлагается и сколько стоит; не появляются противоречащие друг другу цены/состояния; возврат сохраняет выдачу |
| 3. Выбранный тур, рейсы и заявка | Сгруппировать длинные списки рейсов средствами существующего интерфейса; показать текущий выбор и разницу общей стоимости; сохранить доступ ко всем вариантам. Собрать читаемую сводку и понятный переход к контактам | Один основной путь к заявке; одна авторитетная сумма; доступны смена рейса, возврат и повтор при ошибке; открытие формы сохраняет контекст тура |
| 4. Общий вид сайта | Согласовать шапку, навигацию, подвал, контейнеры, типографику, поля, кнопки и состояния ссылок в существующих общих владельцах DS2 | Главная, поиск, направления и служебные страницы выглядят частью одного сайта; новый компонент не копируется на каждую страницу |
| 5. Страницы и переходы | Доработать главную, направления/курорты/месяцы, горящие туры, раннее бронирование, контакты и «Как купить»; проверить соответствие витрин реальным предложениям и сохранение параметров перехода | Пользователь понимает следующий шаг; выбранные город, даты, ночи и состав не теряются; нет неподтверждённых обещаний и ссылок на отсутствующий контент |
| 6. Полезные расширения поиска | После основного пути оценить компактное возвращение календаря цен, действительно полезных локальных фильтров и поиска отеля; предложения ANEX включать с учётом готовности отдельной интеграции | У каждой функции есть понятный эффект, источник данных и владелец; прирост объясним, дублирующих оболочек нет |
| 7. Приёмка и подготовка переноса | Один общий проход по сайту и пути до заявки на телефоне/компьютере; завершить Safari; сверить текущий main, старый поиск, откат и различия preview/production | Есть конкретная версия для визуального одобрения; её размеры зафиксированы; перед переносом выполнены действующие обязательные условия |

Рабочий порядок: 0 и 1 выполняются вместе, затем 2 → 3. После фиксации общих компонентов пакет 4 и подготовку содержимого пакета 5 можно вести параллельно в непересекающихся файлах. Полезные расширения из пакета 6 выбираются по эффекту для поиска и продаж; они не задерживают завершение базового пути.

**Содержание пакетов, которое важно сохранить**

Для формы: использовать текущие канонические поля и обработчики. Сохранение параметров при редактировании, переходах со страниц страны и возврате из тура важнее декоративных всплывающих контролов. Не создавать второй механизм дат или туристов.

Для выдачи: локальный фильтр по уже загруженным предложениям не должен запускать новый поиск у поставщика. Предлагать фильтр только при достаточно полных данных для него. Не вводить пагинацию вместо существующей постепенной загрузки. Статусы должны соответствовать реальному этапу поиска.

Для цены и рейсов: отображать данные существующего владельца цены. Уточняемый рейс не превращать в подтверждённый; неизвестный багаж не превращать в нулевой. Группировка рейсов не должна исключать предложения. Форматирование суммы не должно становиться вторым расчётом стоимости.

Для сайта: сохранить оригинальный логотип и действующее направление DS2. Статичные страницы не должны загружать контроллер поиска, рейсов или заявки ради одинаковой кнопки. Общую кнопку и отступ реализовать общим стилем, содержимое страны — данными/шаблоном.

Для содержимого: прежние исправления просроченных вылетов, склонений и неработающих ссылок уже есть в истории — не объявлять их снова незавершёнными без свежего подтверждения. Проверять сохранность на текущей версии. Отсутствующие утверждённые материалы об оплате и конфиденциальности остаются отдельной зависимостью; условия не сочинять.

Для календаря и фильтров: некоторые необязательные функции были сознательно удалены в крупном сокращении. Их возвращение — отдельная продуктовая задача с замером, а не восстановление всего старого слоя. Для календаря сначала определить, показывает ли он минимумы уже полученной выдачи или требует нового запроса; не выдавать неполную выборку за все доступные цены.

Для ANEX: учитывать текущую работу по сопоставлению отелей, не дублировать её в браузере. Показ поставщика, сопоставленной гостиницы и доступного предложения опирается на согласованные данные. Изменения API и сопоставления не смешиваются с пакетом оформления.

**Правила, которые предотвращают накопление дублирующих слоёв**

1. Один актуальный владелец каждого компонента. Обычно правка выполняется в существующем renderer/стиле. Если нужна лучшая архитектура, компонент можно заменить целиком, удалив прежний в том же пакете; очередной patch/final/override поверх старого решения не добавляется.
2. Основные исходники Search3 живут в src/search3; готовые v2/search3-* создаёт существующая сборка. Сохраняются стабильные публичные пути, порядок и контроль воспроизводимости.
3. Новая реализация заменяет прежнюю в том же пакете. Удаляются старые подключения и ненужные обработчики; отключение только через display:none не считается очисткой.
4. Контролировать итоговые ресурсы страницы и весь сценарий. Дробление файлов, перенос в общий bundle/inline и отложенная загрузка отражаются отдельно от реального удаления байтов.
5. Рост допускается ради более качественного визуала, удобства, доступности и полезных функций. В итоговом отчёте кратко объяснять пользу и изменение размера. Не вводить вечный меньший потолок после каждого сокращения или отдельное согласование каждого прироста.
6. Критерий оформления — заметное улучшение результата и понятный код. Дополнительные стили для адаптива, типографики, состояний и компонентов допустимы; жертвовать ими ради 12 КБ не требуется. Дублирование и цепочки подавляющих друг друга правил устраняются при доработке компонента.
7. Использовать текущие средства, когда они подходят. Новую библиотеку добавлять при конкретной пользе с учётом веса, поддержки и заменяемого кода; не подключать две реализации одной функции.
8. Видимость секций не должна требовать отдельного MutationObserver, таймера или resize-обработчика на каждый блок. Сначала CSS, нативные элементы и существующие события жизненного цикла.
9. CSS общий там, где поведение действительно общее; исключения ограничены компонентом. Не наращивать глобальную специфичность и !important для подавления предыдущего слоя.
10. Не дробить четыре оставшихся JS-владельца на десятки файлов ради формальной «модульности». Короткая актуальная карта ответственности полезнее большого каталога устаревших модулей.

Для новых функций сначала определить полезный результат и способ интеграции без дублирования. Замерить изменение веса в общей сборке. Точное число байтов заранее не нужно: важны соразмерный прирост, польза и отсутствие лишних реализаций. Осмысленное разделение на общие стили, компоненты и локальные варианты допустимо и не означает требование свести весь сайт в один файл.

**Проверки и темп работы**

Сохраняется уже согласованный облегчённый режим для тестового превью. Один завершённый пакет — одна сборка и один цикл применимых обязательных CI; замер байтов встроить туда же. Не запускать новый workflow только ради размера и не добавлять отдельный согласовательный процесс для полезного роста.

Вручную смотреть изменённый участок на телефоне и компьютере; промежуточную ширину добавлять, если меняется соответствующая сетка или breakpoint. Ориентир ручного прохода — до пяти минут, как в текущей политике; непроверенное честно отмечать. Изменения расчёта, выбора рейсов, API и транспорта заявки в эти UI-пакеты не входят.

Полный путь по сайту нужен на накопленном рубеже и перед переносом. Нет отдельного повторного обхода сайта и десятков скриншотов на каждую правку отступа. Исправление конкретной найденной ошибки проверяется адресно. Успешный артефакт переиспользуется; документация не вызывает пересборку или выкладку.

В коротком отчёте пакета показывать результат для пользователя, прирост/сокращение текущего пакета и четыре показателя: CSS, JS, первоначальная загрузка, полный сценарий. Различать «подготовлено», «проверено» и «опубликовано».

**Текущий практический пакет**

Опубликован source `840cd2793932e67e0377f5bfc13b325ad4801569`; checked release `ea3c146c1c5f6ab92a520eb71732db6fb1a7364e`. Актуальная точка — верхний checkpoint #1710/#1713 и `AUTOPILOT_STATE.json.current_task`. Остаток: ограниченная mobile/Safari/site/SEO приёмка и наблюдение literal entity в описании; повторная публикация завершённых пакетов не нужна.

**Источники текущего состояния**

- [Рабочий трекер #996](https://github.com/pyatkoff/poisk-turov-test/issues/996).
- [Точка продолжения на зафиксированной ветке](https://github.com/pyatkoff/poisk-turov-test/blob/d87ffe4f15c868dc28723593c90c9752998435b8/docs/project/search3-refactor-autopilot.md).
- [Аудит компактного оформления](https://github.com/pyatkoff/poisk-turov-test/blob/d87ffe4f15c868dc28723593c90c9752998435b8/docs/project/search3-compact-native-presentation.json).
- [Текущая проверка размера](https://github.com/pyatkoff/poisk-turov-test/blob/d87ffe4f15c868dc28723593c90c9752998435b8/tests/search3_production_presentation_test.py).
- [Манифест Search3](https://github.com/pyatkoff/poisk-turov-test/blob/d87ffe4f15c868dc28723593c90c9752998435b8/src/search3/manifest.json).
- [Общий план сайта и история выполненного](https://github.com/pyatkoff/poisk-turov-test/blob/d87ffe4f15c868dc28723593c90c9752998435b8/docs/project/search3-site-finish-and-legacy.md).
- [Действующий режим проверок и границы](https://github.com/pyatkoff/poisk-turov-test/blob/d87ffe4f15c868dc28723593c90c9752998435b8/OWNER_PRIORITY.json).
- [PR #1640 (влит): обмен общего веса на первоначальную загрузку](https://github.com/pyatkoff/poisk-turov-test/pull/1640).
