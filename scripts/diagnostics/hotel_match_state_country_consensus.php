<?php
declare(strict_types=1);
putenv('MATCH_TEST_LIBRARY=1');
require_once __DIR__.'/hotel_match_live_residual_current_review.php';

const MSC_OP='hotel-match-state-country-consensus-1971-20260915-v3';

function msc_json(array $v):string{return json_encode($v,JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES|JSON_PRETTY_PRINT|JSON_THROW_ON_ERROR)."\n";}
function msc_query(PDO $db,string $sql,array $args=[]):array{$q=$db->prepare($sql);$q->execute($args);return $q->fetchAll(PDO::FETCH_ASSOC);}
function msc_write(string $file,array $v):string{$raw=msc_json($v);$f=@fopen($file,'x+b');if(!$f)throw new RuntimeException('exclusive_file');try{if(fwrite($f,$raw)!==strlen($raw)||!fflush($f))throw new RuntimeException('write');if(function_exists('fsync')&&!fsync($f))throw new RuntimeException('sync');rewind($f);if(stream_get_contents($f)!==$raw)throw new RuntimeException('readback');}finally{fclose($f);}return hash('sha256',$raw);}
function msc_country_aliases(array $coreIds):array{
    $vocab=[
        'египет'=>['egypt','египет'],
        'таиланд'=>['thailand','таиланд'],
        'турция'=>['turkey','turkiye','türkiye','турция'],
        'мальдивы'=>['maldives','maldivas','maldive','мальдивы','мальдив'],
        'оаэ'=>['uae','united arab emirates','оаэ','объединенные арабские эмираты'],
        'куба'=>['cuba','куба'],
        'шри ланка'=>['sri lanka','шри ланка'],
        'вьетнам'=>['vietnam','вьетнам'],
    ];
    $out=[];foreach($coreIds as $cid=>$name){$k=trim(mcr_country_key((string)$name));foreach($vocab[$k]??[$k] as $a){$a=trim(mcr_country_key((string)$a));if($a!=='')$out[(int)$cid][$a]=true;}}
    return array_map(fn($x)=>array_keys($x),$out);
}
function msc_explicit_countries(array $names,array $aliases):array{
    $found=[];foreach($names as $name){$flat=' '.trim(mcr_country_key((string)$name)).' ';foreach($aliases as $cid=>$terms)foreach($terms as $term)if(str_contains($flat,' '.$term.' ')){$found[(int)$cid]=true;break;}}
    $ids=array_map('intval',array_keys($found));sort($ids,SORT_NUMERIC);return$ids;
}
function msc_marker_consensus(array $pending,array $coreIds):array{
    $aliases=msc_country_aliases($coreIds);$votes=[];$examples=[];$conflicting=[];
    foreach($pending as $r){$e=mcr_evidence((string)($r['evidence_json']??''));$sk=mcr_state_key($e);if($sk===null)continue;$ids=msc_explicit_countries(mcr_names($e),$aliases);if(count($ids)>1){$conflicting[$sk]=($conflicting[$sk]??0)+1;continue;}if(count($ids)!==1)continue;$cid=$ids[0];$votes[$sk][$cid]=($votes[$sk][$cid]??0)+1;if(count($examples[$sk][$cid]??[])<8)$examples[$sk][$cid][]=['external_hotel_id'=>(string)$r['external_hotel_id'],'names'=>mcr_names($e)];}
    $out=[];foreach($votes as $sk=>$counts){arsort($counts,SORT_NUMERIC);$cid=(int)array_key_first($counts);$winner=(int)$counts[$cid];$total=array_sum($counts);$share=$total?$winner/$total:0.0;$multi=(int)($conflicting[$sk]??0);if($winner<5||$share<0.98||$multi>0||!isset($coreIds[$cid]))continue;$out[(string)$sk]=['country_id'=>$cid,'country_name'=>$coreIds[$cid],'winner'=>$winner,'total_marked'=>$total,'share'=>$share,'multi_country_rows'=>$multi,'method'=>'current_provider_explicit_country_marker_consensus','examples'=>$examples[$sk][$cid]??[]];}
    return$out;
}
function msc_countryless_norm(string $raw,int $cid,array $aliases):string{
    $n=trim(mcr_country_key(mcr_norm($raw)));if($n===''||!isset($aliases[$cid]))return$n;$p=' '.$n.' ';$terms=$aliases[$cid];usort($terms,fn($a,$b)=>strlen($b)<=>strlen($a));foreach($terms as $term)$p=str_replace(' '.$term.' ',' ',$p);return trim(preg_replace('/\s+/',' ',$p)??$p);
}
function msc_countryless_tokens(string $raw,int $cid,array $aliases):array{$n=msc_countryless_norm($raw,$cid,$aliases);return$n===''?[]:(preg_split('/\s+/',$n)?:[]);}
function msc_countryless_score(string $a,string $b,int $cid,array $aliases):float{$aa=array_fill_keys(msc_countryless_tokens($a,$cid,$aliases),true);$bb=array_fill_keys(msc_countryless_tokens($b,$cid,$aliases),true);if(!$aa||!$bb)return 0.0;$i=count(array_intersect_key($aa,$bb));$u=count($aa+$bb);return$u?$i/$u:0.0;}
function msc_countryless_indexes(array $hotels,array $forms,array $aliases):array{$exact=[];$tokens=[];foreach($forms as $id=>$list){if(!isset($hotels[$id]))continue;$cid=(int)$hotels[$id]['country_id'];foreach(array_unique($list) as $raw){$n=msc_countryless_norm((string)$raw,$cid,$aliases);if($n==='')continue;$exact[$cid][$n][$id]=true;foreach(msc_countryless_tokens((string)$raw,$cid,$aliases) as $t)$tokens[$cid][$t][$id]=true;}}return[$exact,$tokens];}
function msc_countryless_select(array $names,int $cid,array $points,array $hotels,array $forms,array $aliases,array $exact,array $tokenIndex):array{
    $source=[];foreach($names as $raw){$n=msc_countryless_norm((string)$raw,$cid,$aliases);if($n!=='')$source[$n]=(string)$raw;}if(!$source)return['route'=>'needs_extra_evidence','reason'=>'country_strip_empty_name'];
    $ids=[];foreach($source as $n=>$raw)foreach(array_keys($exact[$cid][$n]??[]) as $id)$ids[(int)$id]=true;
    if($ids){$safe=[];$blocked=[];foreach(array_keys($ids) as $id){if(!isset($hotels[$id]))continue;$qok=false;foreach($names as $src)foreach($forms[$id]??[] as $lf)if(msc_countryless_norm((string)$src,$cid,$aliases)===msc_countryless_norm((string)$lf,$cid,$aliases)&&mcr_qualifier_ok((string)$src,(string)$lf)){$qok=true;break 2;}if(!$qok){$blocked[$id]='qualifier_conflict';continue;}$ds=[];foreach($points as $p){$d=mcr_distance($p,$hotels[$id]);if($d!==null)$ds[]=$d;}if($ds&&max($ds)>5.0){$blocked[$id]='coordinate_conflict_gt5km';continue;}$safe[$id]=['distance_km'=>$ds?min($ds):null];}
        if(count($safe)===1){$id=(int)array_key_first($safe);return['route'=>'auto_accept_candidate','reason'=>'unique_exact_after_country_geography_strip','target'=>$id,'score'=>1.0]+$safe[$id];}
        if(count($safe)>1){$near=[];foreach($safe as $id=>$v)if($v['distance_km']!==null&&$v['distance_km']<=1.0)$near[$id]=$v;if(count($near)===1){$id=(int)array_key_first($near);return['route'=>'auto_accept_candidate','reason'=>'coordinate_resolved_exact_after_country_strip','target'=>$id,'score'=>1.0]+$near[$id];}return['route'=>'needs_extra_evidence','reason'=>'ambiguous_exact_after_country_strip','candidate_count'=>count($safe),'candidate_ids'=>array_slice(array_map('intval',array_keys($safe)),0,20)];}
        return['route'=>'hard_conflict','reason'=>in_array('coordinate_conflict_gt5km',$blocked,true)?'country_strip_exact_coordinate_conflict_gt5km':'country_strip_exact_qualifier_conflict'];
    }
    $candidateIds=[];$maxTokens=0;foreach($source as $n=>$raw){$tt=msc_countryless_tokens($raw,$cid,$aliases);$maxTokens=max($maxTokens,count($tt));foreach($tt as $t)foreach(array_keys($tokenIndex[$cid][$t]??[]) as $id)$candidateIds[(int)$id]=true;}if($maxTokens<2||!$candidateIds)return['route'=>'needs_extra_evidence','reason'=>'country_strip_no_strong_candidate'];
    $rank=[];foreach(array_keys($candidateIds) as $id){$best=0.0;$qual=false;foreach($names as $src)foreach($forms[$id]??[] as $lf){$best=max($best,msc_countryless_score((string)$src,(string)$lf,$cid,$aliases));if(mcr_qualifier_ok((string)$src,(string)$lf))$qual=true;}if($qual)$rank[$id]=$best;}arsort($rank,SORT_NUMERIC);$rids=array_keys($rank);if(!$rids)return['route'=>'needs_extra_evidence','reason'=>'country_strip_qualifier_filtered_fuzzy'];$id=(int)$rids[0];$best=(float)$rank[$id];$second=isset($rids[1])?(float)$rank[$rids[1]]:0.0;$margin=$best-$second;$ds=[];foreach($points as $p){$d=mcr_distance($p,$hotels[$id]);if($d!==null)$ds[]=$d;}if($ds&&max($ds)>5.0)return['route'=>'hard_conflict','reason'=>'country_strip_fuzzy_coordinate_conflict_gt5km','target'=>$id,'score'=>$best,'margin'=>$margin,'distance_km'=>min($ds)];$near=$ds?min($ds):null;if(($best>=0.88&&$margin>=0.18)||($best>=0.82&&$margin>=0.12&&$near!==null&&$near<=1.0))return['route'=>'auto_accept_candidate','reason'=>'strong_fuzzy_after_country_geography_strip','target'=>$id,'score'=>$best,'margin'=>$margin,'distance_km'=>$near];return['route'=>'needs_extra_evidence','reason'=>'country_strip_fuzzy_insufficient','top_target'=>$id,'score'=>$best,'margin'=>$margin,'distance_km'=>$near];
}

if(getenv('MATCH_STATE_CONSENSUS_TEST_LIBRARY')==='1')return;
if(PHP_SAPI!=='cli'){http_response_code(404);exit;}
$op=(string)getenv('MATCH_OPERATION_ID');$sha=(string)getenv('MATCH_SOURCE_SHA');if($op!==MSC_OP||!preg_match('/^[0-9a-f]{40}$/D',$sha))throw new RuntimeException('operation_or_source_guard');$home=(string)getenv('HOME');if($home==='')throw new RuntimeException('home');$dir=$home.'/.anytoour-match/operations/'.MSC_OP;$res=mcr_evidence((string)@file_get_contents($dir.'/reservation.json'));if(($res['operation_id']??'')!==MSC_OP||($res['source_sha']??'')!==$sha||($res['state']??'')!=='reserved_before_db_access')throw new RuntimeException('reservation');$root=realpath(getcwd());if(!$root||basename($root)!=='anytoour.ru')throw new RuntimeException('root');require_once $root.(is_file($root.'/data/db-v1.php')?'/data/db-v1.php':'/v2/data/db-v1.php');$db=v2_data_db();$db->setAttribute(PDO::ATTR_ERRMODE,PDO::ERRMODE_EXCEPTION);$db->exec('SET TRANSACTION ISOLATION LEVEL REPEATABLE READ');$db->exec('START TRANSACTION READ ONLY');
try{
    $coreIds=[];foreach(msc_query($db,'SELECT id,name FROM catalog_countries WHERE is_active=1 ORDER BY id') as $c)if(mcr_is_core8_name((string)$c['name']))$coreIds[(int)$c['id']]=(string)$c['name'];if(count($coreIds)<6)throw new RuntimeException('core8');$marks=implode(',',array_fill(0,count($coreIds),'?'));
    $hotels=[];$forms=[];foreach(msc_query($db,"SELECT id,country_id,country_name,region_id,region_name,subregion_id,subregion_name,name,category,latitude,longitude FROM catalog_hotels WHERE is_active=1 AND country_id IN ($marks) ORDER BY country_id,id",array_keys($coreIds)) as $h){$id=(int)$h['id'];$hotels[$id]=$h;$forms[$id]=[(string)$h['name']];}foreach(msc_query($db,"SELECT a.hotel_id,a.alias FROM hotel_aliases a JOIN catalog_hotels h ON h.id=a.hotel_id WHERE h.is_active=1 AND h.country_id IN ($marks) ORDER BY a.hotel_id,a.id",array_keys($coreIds)) as $a){$id=(int)$a['hotel_id'];if(isset($forms[$id]))$forms[$id][]=(string)$a['alias'];}
    $exact=[];$tokenIndex=[];foreach($forms as $id=>$list){$cid=(int)$hotels[$id]['country_id'];foreach(array_unique($list) as $raw){$n=mcr_norm((string)$raw);if($n==='')continue;$exact[$cid][$n][]=$id;foreach(mcr_tokens((string)$raw) as $t)$tokenIndex[$cid][$t][$id]=true;}}$geoExact=mcr_geo_exact_index($hotels,$forms);$aliases=msc_country_aliases($coreIds);[$countrylessExact,$countrylessTokens]=msc_countryless_indexes($hotels,$forms,$aliases);
    $pending=msc_query($db,"SELECT supplier_namespace,external_hotel_id,evidence_sha256,evidence_json FROM andromeda_hotel_identities WHERE supplier_namespace='andromeda_catalog' AND decision_status='pending' AND local_hotel_id IS NULL ORDER BY external_hotel_id");$inferred=msc_marker_consensus($pending,$coreIds);$routes=[];$candidates=[];$reason=[];
    foreach($pending as $r){$e=mcr_evidence((string)$r['evidence_json']);$sk=mcr_state_key($e);if($sk===null||!isset($inferred[$sk]))continue;$cid=(int)$inferred[$sk]['country_id'];$names=mcr_names($e);$points=mcr_points($e);$explicit=msc_explicit_countries($names,$aliases);if($explicit&&$explicit!==[$cid]){$sel=['route'=>'hard_conflict','reason'=>'row_explicit_country_conflict','explicit_country_ids'=>$explicit];}else{$base=mcr_select_candidate($names,$cid,$points,$hotels,$forms,$exact,$tokenIndex,$geoExact);if(($base['route']??'')==='needs_extra_evidence'){$geo=msc_countryless_select($names,$cid,$points,$hotels,$forms,$aliases,$countrylessExact,$countrylessTokens);$sel=($geo['route']??'')==='needs_extra_evidence'?$base:$geo;}else{$sel=$base;}}$item=array_merge(['supplier_namespace'=>'andromeda_catalog','external_hotel_id'=>(string)$r['external_hotel_id'],'evidence_sha256'=>(string)$r['evidence_sha256'],'state_key'=>$sk,'inferred_country_id'=>$cid,'inference'=>$inferred[$sk]],$sel);$routes[$item['route']][]=$item;$reason[$item['reason']]=($reason[$item['reason']]??0)+1;if($item['route']==='auto_accept_candidate')$candidates[]=$item;}
    foreach($routes as &$ls)usort($ls,fn($a,$b)=>strcmp((string)$a['external_hotel_id'],(string)$b['external_hotel_id']));unset($ls);ksort($routes);ksort($reason);$result=['schema'=>'hotel-match-state-country-consensus/3','operation_id'=>MSC_OP,'source_sha'=>$sha,'state'=>'completed_read_only','server_current'=>true,'transaction'=>'REPEATABLE READ READ ONLY','core8_countries'=>$coreIds,'pending_andromeda'=>count($pending),'inferred_states'=>$inferred,'candidate_count'=>count($candidates),'candidates'=>$candidates,'route_counts'=>array_map('count',$routes),'reason_counts'=>$reason,'routes'=>$routes,'supplier_calls'=>0,'tourvisor_calls'=>0,'external_calls'=>0,'booking_calls'=>0,'db_writes'=>0,'mapping_writes'=>0,'no_replay'=>true,'created_at'=>gmdate('c')];$hash=msc_write($dir.'/result.json',$result);$raw=(string)file_get_contents($dir.'/result.json');$ok=hash('sha256',$raw)===$hash&&($x=mcr_evidence($raw))&&($x['operation_id']??'')===MSC_OP&&($x['db_writes']??-1)===0;$receipt=['operation_id'=>MSC_OP,'source_sha'=>$sha,'state'=>'completed_read_only','result_sha256'=>$hash,'readback_verified'=>$ok,'pending_andromeda'=>count($pending),'inferred_state_count'=>count($inferred),'candidate_count'=>count($candidates),'route_counts'=>array_map('count',$routes),'db_writes'=>0,'supplier_calls'=>0,'tourvisor_calls'=>0,'external_calls'=>0,'no_replay'=>true,'completed_at'=>gmdate('c')];msc_write($dir.'/receipt.json',$receipt);if(!$ok)throw new RuntimeException('result_readback');echo msc_json(['operation_id'=>MSC_OP,'state'=>'completed_read_only','pending_andromeda'=>count($pending),'inferred_states'=>$inferred,'candidate_count'=>count($candidates),'route_counts'=>array_map('count',$routes),'reason_counts'=>$reason,'result_sha256'=>$hash]);$db->commit();
}catch(Throwable $e){if($db->inTransaction())$db->rollBack();if(!is_file($dir.'/failure.json'))msc_write($dir.'/failure.json',['operation_id'=>MSC_OP,'source_sha'=>$sha,'state'=>'read_only_failed','error_class'=>get_class($e),'error'=>substr($e->getMessage(),0,300),'db_writes'=>0,'supplier_calls'=>0,'external_calls'=>0,'no_replay'=>true,'failed_at'=>gmdate('c')]);throw $e;}
