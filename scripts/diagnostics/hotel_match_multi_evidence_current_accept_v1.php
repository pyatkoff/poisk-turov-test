<?php
declare(strict_types=1);
const OP='hotel-match-multi-evidence-current-accept-1971-20260912-v1';
const POLICY='owner_multi_evidence_consensus_20260912_v1';
function j($v):string{return json_encode($v,JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES|JSON_THROW_ON_ERROR);}
function h($v):string{return hash('sha256',j($v));}
function save_once(string $p,array $d):void{$f=fopen($p,'x');if(!$f)throw new RuntimeException('receipt_exists');chmod($p,0600);$r=j($d)."\n";fwrite($f,$r);fflush($f);if(function_exists('fsync'))fsync($f);fclose($f);if(file_get_contents($p)!==$r)throw new RuntimeException('receipt_readback');}
$plan=json_decode(<<<'JSON'
[[8803,17461,50],[28990,17680,50],[29860,17346,50],[30720,17656,50],[32599,46679,50],[32769,17656,50],[32826,17461,50],[34078,37383,50],[29426,17612,49],[41114,59144,49],[1639,150,27],[14902,55574,27],[32734,17456,18],[794,229,1],[32575,17692,1],[35869,17366,1]]
JSON,true,512,JSON_THROW_ON_ERROR);
if(in_array('--self-test',$_SERVER['argv']??[],true)){if(count($plan)!==16)exit(2);$s=[];foreach($plan as$r){if(isset($s[$r[0]]))exit(3);$s[$r[0]]=1;}echo "MATCH multi evidence self-test PASS rows=16\n";exit;}
error_reporting(0);ob_start();$db=null;$dir=null;$attempt=false;$written=[];$skipped=[];
try{if(PHP_SAPI!=='cli')throw new RuntimeException('cli_only');$root=realpath(getcwd());if(!$root||basename($root)!=='anytoour.ru')throw new RuntimeException('root');$home=realpath((string)getenv('HOME'));if(!$home)throw new RuntimeException('home');$base=$home.'/.anytoour-match/operations';if(!is_dir($base)&&!mkdir($base,0700,true))throw new RuntimeException('receipt_root');$dir=$base.'/'.OP;if(file_exists($dir)||!mkdir($dir,0700)){$dir=null;throw new RuntimeException('prior_operation_no_replay');}save_once($dir.'/reservation.json',['operation_id'=>OP,'planned'=>16,'source'=>'reports/hotel-match-live106-multi-evidence-20260912.json','source_sha'=>'57bad850f699fd4dfcbee0f7a7093fc678f09579','no_replay'=>true]);require_once $root.(is_file($root.'/data/db-v1.php')?'/data/db-v1.php':'/v2/data/db-v1.php');$db=v2_data_db();$db->setAttribute(PDO::ATTR_ERRMODE,PDO::ERRMODE_EXCEPTION);$db->exec('SET SESSION innodb_lock_wait_timeout=10');$db->exec('SET TRANSACTION ISOLATION LEVEL REPEATABLE READ');$db->beginTransaction();$ins=$db->prepare('INSERT INTO anex_hotel_search_mappings(anex_hotel_id,catalog_hotel_id,match_class,scope,approval_policy,source_row_digest,mapping_digest,enabled) VALUES(?,?,?,?,?,?,?,1)');foreach($plan as$r){[$aid,$lid,$count]=$r;$q=$db->prepare('SELECT id,is_active FROM catalog_hotels WHERE id=? FOR UPDATE');$q->execute([$lid]);$hrow=$q->fetch(PDO::FETCH_ASSOC);if(!$hrow||(int)$hrow['is_active']!==1){$skipped[]=['anex_id'=>$aid,'local_id'=>$lid,'reason'=>'target_inactive'];continue;}$q=$db->prepare('SELECT anex_hotel_id FROM anex_hotel_decisions WHERE anex_hotel_id=? LIMIT 1 FOR UPDATE');$q->execute([$aid]);$manual=(bool)$q->fetchColumn();$q=$db->prepare('SELECT anex_hotel_id FROM anex_review_pair_exclusions WHERE anex_hotel_id=? LIMIT 1 FOR UPDATE');$q->execute([$aid]);$ex=(bool)$q->fetchColumn();if($manual||$ex){$skipped[]=['anex_id'=>$aid,'local_id'=>$lid,'reason'=>'manual_or_exclusion'];continue;}$q=$db->prepare('SELECT catalog_hotel_id,enabled FROM anex_hotel_search_mappings WHERE anex_hotel_id=? ORDER BY catalog_hotel_id FOR UPDATE');$q->execute([$aid]);$maps=$q->fetchAll(PDO::FETCH_ASSOC);if($maps){$exact=count($maps)===1&&(int)$maps[0]['enabled']===1&&(int)$maps[0]['catalog_hotel_id']===$lid;$skipped[]=['anex_id'=>$aid,'local_id'=>$lid,'reason'=>$exact?'already_exact':'current_mapping_conflict'];continue;}$proof=['operation_id'=>OP,'anex_hotel_id'=>$aid,'catalog_hotel_id'=>$lid,'search_count'=>$count,'rule'=>'two_or_more_independent_target_voting_lanes_same_local'];$sd=h($proof);$md=h(['anex_hotel_id'=>$aid,'catalog_hotel_id'=>$lid,'scope'=>'preview','approval_policy'=>POLICY,'source_row_digest'=>$sd]);$ins->execute([$aid,$lid,'multi_evidence_consensus','preview',POLICY,$sd,$md]);if($ins->rowCount()!==1)throw new RuntimeException('insert_count');$written[]=['anex_id'=>$aid,'local_id'=>$lid,'mapping_digest'=>$md];}save_once($dir.'/precommit-intent.json',['operation_id'=>OP,'written'=>$written,'skipped'=>$skipped,'no_replay'=>true]);$attempt=true;$db->commit();$db->exec('START TRANSACTION READ ONLY');foreach($written as$w){$q=$db->prepare('SELECT catalog_hotel_id,enabled,mapping_digest FROM anex_hotel_search_mappings WHERE anex_hotel_id=?');$q->execute([$w['anex_id']]);$x=$q->fetchAll(PDO::FETCH_ASSOC);if(count($x)!==1||(int)$x[0]['catalog_hotel_id']!==(int)$w['local_id']||(int)$x[0]['enabled']!==1||$x[0]['mapping_digest']!==$w['mapping_digest'])throw new RuntimeException('readback_anex');}$db->exec('ROLLBACK');$reasons=[];foreach($skipped as$s)$reasons[$s['reason']]=($reasons[$s['reason']]??0)+1;ksort($reasons);$out=['status'=>'accepted','operation_id'=>OP,'planned'=>16,'written_count'=>count($written),'provider_counts'=>['anex'=>count($written)],'skipped_count'=>count($skipped),'skip_reasons'=>$reasons,'readback_verified'=>true,'supplier_calls'=>0,'tourvisor_calls'=>0,'no_replay'=>true];save_once($dir.'/result.json',$out);}catch(Throwable$e){try{if($db instanceof PDO&&$db->inTransaction())$db->rollBack();}catch(Throwable$z){}$out=['status'=>$attempt?'commit_outcome_requires_readback':'rolled_back_or_not_started','operation_id'=>OP,'safe_message'=>preg_match('/^[a-z0-9_]+$/D',$e->getMessage())?$e->getMessage():'guard_failure','supplier_calls'=>0,'tourvisor_calls'=>0,'no_replay'=>true];if($dir&&is_dir($dir)&&!file_exists($dir.'/failure.json'))try{save_once($dir.'/failure.json',$out);}catch(Throwable$z){}}while(ob_get_level())ob_end_clean();echo 'MATCH_MULTI_EVIDENCE_ACCEPT:'.j($out).PHP_EOL;exit(($out['status']??'')==='accepted'?0:2);
