<?php
declare(strict_types=1);
/** MATCH #1971: read-only strong typo/compound census for proven Sri Lanka state116 tail. */
const SF_OP='hotel-match-andromeda-state116-strong-fuzzy-1971-20260916-v1';
const SF_OLD='andromeda-1759-country6-20260910-v1-sri-lanka';
const SF_COUNTRY=12;
const SF_STATE='116';
const SF_MIN_SCORE=0.94;
const SF_MIN_MARGIN=0.12;
function sf_j($v):string{return json_encode($v,JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES|JSON_THROW_ON_ERROR);}
function sf_put(string $p,$v):string{$raw=sf_j($v)."\n";$f=@fopen($p,'x');if(!$f)throw new RuntimeException('durable_open_failed');try{if(fwrite($f,$raw)!==strlen($raw))throw new RuntimeException('durable_write_failed');fflush($f);}finally{fclose($f);}if(file_get_contents($p)!==$raw)throw new RuntimeException('durable_readback_failed');return hash('sha256',$raw);}
function sf_read(string $p):array{if(is_link($p)||!is_file($p)||filesize($p)>20000000)throw new RuntimeException('sealed_file_missing');$x=json_decode((string)file_get_contents($p),true,128,JSON_THROW_ON_ERROR);if(!is_array($x))throw new RuntimeException('sealed_json_invalid');return $x;}
function sf_dbp(string $r):string{foreach([$r.'/data/db-v1.php',$r.'/v2/data/db-v1.php'] as $p)if(is_file($p))return $p;throw new RuntimeException('db_bootstrap_missing');}
function sf_q(PDO $d,string $sql,array $p=[]):array{$s=$d->prepare($sql);$s->execute($p);return $s->fetchAll(PDO::FETCH_ASSOC)?:[];}
function sf_ev($raw):array{if(!is_string($raw)||trim($raw)==='')return[];try{$x=json_decode($raw,true,256,JSON_THROW_ON_ERROR);return is_array($x)?$x:[];}catch(Throwable){return[];}}
function sf_box(array $e):array{return is_array($e['source']??null)?$e['source']:$e;}
function sf_sv($v):?string{if(!is_scalar($v))return null;$s=trim((string)$v);return $s===''?null:$s;}
function sf_first(array $a,array $keys):?string{foreach($keys as $k)if(array_key_exists($k,$a)&&($v=sf_sv($a[$k]))!==null)return $v;return null;}
function sf_num($v):?float{if(!is_scalar($v)||!is_numeric((string)$v))return null;$x=(float)$v;return is_finite($x)?$x:null;}
function sf_norm(string $s):string{$s=mb_strtolower($s,'UTF-8');$s=strtr($s,['ё'=>'е','é'=>'e','è'=>'e','ê'=>'e','á'=>'a','à'=>'a','ä'=>'a','â'=>'a','ö'=>'o','ô'=>'o','ü'=>'u','ú'=>'u','ç'=>'c','ş'=>'s','ğ'=>'g','ı'=>'i']);$s=preg_replace('/\b(?:hotel|hotels|resort|resorts|spa|the|and)\b/iu',' ',$s)??$s;$s=preg_replace('/[^\pL\pN]+/u',' ',$s)??$s;return trim(preg_replace('/\s+/u',' ',$s)??$s);}
function sf_forms(?string $s):array{if($s===null||trim($s)==='')return[];$out=[];$old=[];if(preg_match_all('/\((?:\s*(?:ex\.?|former(?:ly)?|ех\.?)\s*[:.-]?\s*)([^)]{2,180})\)/iu',$s,$m))foreach($m[1] as $x)$old[]=(string)$x;$cur=preg_replace('/\((?:\s*(?:ex\.?|former(?:ly)?|ех\.?)\s*[:.-]?\s*)[^)]{2,180}\)/iu',' ',$s)??$s;foreach(array_merge([$cur],$old) as $x){$n=sf_norm((string)$x);if($n!=='')$out[$n]=1;}return array_map('strval',array_keys($out));}
function sf_tokens(string $s):array{$o=[];foreach(preg_split('/\s+/u',$s,-1,PREG_SPLIT_NO_EMPTY)?:[] as $t)$o[(string)$t]=1;return array_map('strval',array_keys($o));}
function sf_sig(string $s):array{$qual=array_fill_keys(['annex','beach','garden','north','south','adults','adult','only','family','palace','park','villa','villas','suite','suites','club','wing'],1);$o=[];foreach(sf_tokens($s) as $t)if(isset($qual[$t])||preg_match('/^[0-9]+$/D',(string)$t))$o[(string)$t]=1;$a=array_map('strval',array_keys($o));sort($a,SORT_STRING);return $a;}
function sf_tokkey(string $s):string{$a=sf_tokens($s);sort($a,SORT_STRING);return implode(' ',$a);}
function sf_compact(string $s):string{return str_replace(' ','',$s);}
function sf_similar(string $a,string $b):float{similar_text($a,$b,$pct);return $pct/100;}
function sf_score(string $a,string $b):?float{if(sf_sig($a)!==sf_sig($b))return null;$ta=sf_tokens($a);$tb=sf_tokens($b);if(!$ta||!$tb)return null;if(sf_tokkey($a)===sf_tokkey($b))return 1.0;if(sf_compact($a)===sf_compact($b))return 0.985;$ia=array_fill_keys($ta,1);$ib=array_fill_keys($tb,1);$inter=count(array_intersect_key($ia,$ib));$f1=(2*$inter)/(count($ta)+count($tb));$char=max(sf_similar($a,$b),sf_similar(sf_compact($a),sf_compact($b)));return 0.70*$char+0.30*$f1;}
function sf_km($a,$b,$c,$d):?float{foreach([$a,$b,$c,$d] as $x)if($x===null)return null;$r=6371.0088;$p1=deg2rad($a);$p2=deg2rad($c);$dp=deg2rad($c-$a);$dl=deg2rad($d-$b);$h=sin($dp/2)**2+cos($p1)*cos($p2)*sin($dl/2)**2;return 2*$r*asin(min(1,sqrt($h)));}
function sf_has_hold_marker($v):bool{if(!is_array($v))return false;foreach($v as $k=>$x){$key=mb_strtolower((string)$k,'UTF-8');if(in_array($key,['manual','manual_decision','excluded','exclusion','rejected','protected'],true)){if($x===true||$x===1||$x==='1'||(is_string($x)&&trim($x)!==''&&mb_strtolower(trim($x),'UTF-8')!=='false'))return true;}if(is_array($x)&&sf_has_hold_marker($x))return true;}return false;}
if(in_array('--self-test',$argv??[],true)){if(sf_score('sea life','sealife')!==0.985)throw new RuntimeException('compound_test');if(sf_score('royal north club','royal south club')!==null)throw new RuntimeException('qualifier_test');if(sf_score('villa 7','villa 8')!==null)throw new RuntimeException('numeric_test');if(sf_norm('Royal Hotel Resort & Spa')!=='royal')throw new RuntimeException('generic_test');echo "PASS\n";exit(0);}
$op=(string)getenv('OPERATION_ID');$sha=(string)getenv('MATCH_SOURCE_SHA');if($op!==SF_OP||!preg_match('/^[0-9a-f]{40}$/D',$sha))throw new RuntimeException('operation_guard');$root=(string)realpath(getcwd());if($root===''||basename($root)!=='anytoour.ru')throw new RuntimeException('root_guard');$base=rtrim((string)getenv('HOME'),'/').'/.anytoour-match/operations';if(!is_dir($base))throw new RuntimeException('operations_root_missing');$dir=$base.'/'.$op;if(!mkdir($dir,0700))throw new RuntimeException('operation_exists');sf_put($dir.'/reservation.json',['operation_id'=>$op,'source_sha'=>$sha,'state'=>'reserved_before_sealed_evidence_and_db','read_only'=>true,'database_writes'=>0,'mapping_writes'=>0,'supplier_calls'=>0,'tourvisor_calls'=>0,'no_replay'=>true]);
$db=null;try{$cfg=require $root.'/_preview/search3-anex-candidate/.andromeda-private.php';$catalogPath=$cfg['catalog_path']??null;if(!is_string($catalogPath)||$catalogPath==='')throw new RuntimeException('private_catalog_config_missing');$private=(string)realpath(dirname($catalogPath));if($private==='')throw new RuntimeException('private_dir_missing');$old=$private.'/'.SF_OLD;$capture=sf_read($old.'/capture.json');$plan=sf_read($old.'/plan.json');$oldResult=sf_read($old.'/result.json');if(($oldResult['status']??null)!=='imported'||($oldResult['readback_verified']??false)!==true)throw new RuntimeException('sealed_import_unverified');$sealedCountry=(int)($capture['country_id']??0);$sealedState=(string)($capture['supplier_country_id']??'');$sealedCatalog=(string)($plan['catalog_sha256']??'');if($sealedCountry!==SF_COUNTRY||$sealedState!==SF_STATE||$sealedCatalog===''||(int)($plan['country_id']??0)!==SF_COUNTRY||(string)($plan['supplier_country_id']??'')!==SF_STATE)throw new RuntimeException('sealed_bridge_mismatch');
require_once sf_dbp($root);$db=v2_data_db();$db->setAttribute(PDO::ATTR_ERRMODE,PDO::ERRMODE_EXCEPTION);$db->exec('SET TRANSACTION ISOLATION LEVEL REPEATABLE READ');$db->exec('START TRANSACTION WITH CONSISTENT SNAPSHOT, READ ONLY');$pend=sf_q($db,"SELECT external_hotel_id,evidence_json,evidence_sha256,catalog_sha256 FROM andromeda_hotel_identities WHERE supplier_namespace='andromeda_catalog' AND decision_status='pending' AND local_hotel_id IS NULL ORDER BY external_hotel_id");$hotels=sf_q($db,'SELECT id,name,country_id,country_name,region_name,subregion_name,category,latitude,longitude FROM catalog_hotels WHERE is_active=1 AND country_id=? ORDER BY id',[SF_COUNTRY]);$aliases=sf_q($db,'SELECT a.hotel_id,a.alias FROM hotel_aliases a JOIN catalog_hotels h ON h.id=a.hotel_id WHERE h.is_active=1 AND h.country_id=? ORDER BY a.hotel_id,a.alias',[SF_COUNTRY]);$occ=sf_q($db,"SELECT local_hotel_id,external_hotel_id FROM andromeda_hotel_identities WHERE supplier_namespace='andromeda_catalog' AND decision_status='accepted' AND local_hotel_id IS NOT NULL");$db->exec('ROLLBACK');
$byId=[];$formsBy=[];$exact=[];foreach($hotels as $h){$id=(int)$h['id'];$byId[$id]=$h;foreach(sf_forms((string)$h['name']) as $f){$formsBy[$id][$f]=1;$exact[$f][$id]=1;}}foreach($aliases as $a){$id=(int)$a['hotel_id'];if(!isset($byId[$id]))continue;foreach(sf_forms((string)$a['alias']) as $f){$formsBy[$id][$f]=1;$exact[$f][$id]=1;}}foreach($formsBy as $id=>$m)$formsBy[$id]=array_map('strval',array_keys($m));$occupied=[];foreach($occ as $r)$occupied[(int)$r['local_hotel_id']][]=(string)$r['external_hotel_id'];
$state=[];$catalogs=[];foreach($pend as $r){$e=sf_ev($r['evidence_json']??null);$s=sf_box($e);if((string)(sf_first($s,['stateKey','state_id','countryKey'])??'')!==SF_STATE)continue;$state[]=['row'=>$r,'evidence'=>$e,'source'=>$s];$catalogs[(string)$r['catalog_sha256']]=1;}$bridge=(count($catalogs)===1&&isset($catalogs[$sealedCatalog]));if(!$bridge)throw new RuntimeException('current_catalog_bridge_unproven');
$rows=[];$provisional=[];$scores=0;$counts=[];foreach($state as $z){$r=$z['row'];$e=$z['evidence'];$s=$z['source'];$name=sf_first($s,['name','hotelName','title']);$lname=sf_first($s,['lName','latinName','nameLatin']);$sf=[];foreach([$name,$lname] as $n)foreach(sf_forms($n) as $f)$sf[$f]=1;$sf=array_map('strval',array_keys($sf));$row=['external_hotel_id'=>(string)$r['external_hotel_id'],'source_name'=>$name,'source_lname'=>$lname,'source_forms'=>$sf,'candidate_local_id'=>null,'candidate_local_name'=>null,'winner_score'=>null,'runner_up_score'=>null,'winner_margin'=>null,'distance_km'=>null,'same_provider_occupancy'=>[],'route'=>null,'evidence_sha256'=>(string)$r['evidence_sha256'],'catalog_sha256'=>(string)$r['catalog_sha256']];if(sf_has_hold_marker($e)){$row['route']='hold_current_protected_marker';$rows[]=$row;continue;}if(!$sf){$row['route']='hold_source_name_missing';$rows[]=$row;continue;}$ex=[];foreach($sf as $f)foreach(array_keys($exact[$f]??[]) as $id)$ex[(int)$id]=1;if($ex){$row['route']=count($ex)===1?'skip_exact_tier_unique':'skip_exact_tier_ambiguous';$row['exact_candidate_ids']=array_keys($ex);$rows[]=$row;continue;}$rank=[];foreach($formsBy as $id=>$localForms){$best=null;foreach($sf as $a)foreach($localForms as $b){$sc=sf_score($a,$b);if($sc!==null&&($best===null||$sc>$best))$best