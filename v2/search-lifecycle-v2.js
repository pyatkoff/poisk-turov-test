(function(){'use strict';
const rt=window.V2Runtime,renderer=window.V2Results,catalogs=window.V2Catalogs;if(!rt||!renderer||!catalogs)return;
const form=document.getElementById('tourSearch'),status=document.getElementById('status'),results=document.getElementById('results'),selected=document.getElementById('selectedTour'),tools=document.getElementById('resultsTools');if(!form||!status||!results)return;
let searchId=0,pollTimer=0;
const api=(action,params,options)=>rt.api(action,params,options);
function emit(name,detail){window.dispatchEvent(new CustomEvent('v2:search-'+name,{detail:Object.assign({searchId},detail||{})}));}
function money(v){const n=Number(v||0);return n?new Intl.NumberFormat('ru-RU').format(n):'';}
function show(text){status.hidden=false;status.textContent=text;}
function params(){const f=new FormData(form),ages=f.getAll('child_age[]').map(v=>Number(v)).filter(Number.isFinite);return{departureId:f.get('from'),countryId:f.get('country'),dateFrom:f.get('dateFrom'),dateTo:f.get('dateTo'),nightsFrom:f.get('daysFrom'),nightsTo:f.get('daysTill'),adults:f.get('count_people')||2,childs:ages,meal:f.get('food')||'',hotelCategory:f.get('stars')||'',hotelRating:f.get('rating')||'',hotelTypes:f.get('hotel_type')?[f.get('hotel_type')]:[],hotelIds:f.get('hotel')?[f.get('hotel')]:[],hotelServices:f.getAll('hotel_service[]'),arrivalId:f.get('arrival')||'',regionIds:f.get('region')?[f.get('region')]:[],subregionIds:f.get('subregion')?[f.get('subregion')]:[],operatorIds:f.get('operator')?[f.get('operator')]:[],priceFrom:f.get('price_from')||'',priceTo:f.get('price_till')||'',currency:'RUB',onlyCharter:f.get('onlyCharter')?'true':'false',onlyDirect:f.get('onlyDirect')?'true':'false'};}
async function loadResults(){if(!searchId)return[];const list=await api('search_results',{searchId,limit:25});renderer.render(list);return Array.isArray(list)?list:[];}
function complete(s){return Number(s&&s.progress||0)>=100||String(s&&s.status||'').toLowerCase()==='complete';}
async function poll(){if(!searchId)return;try{const s=await api('search_status',{searchId}),p=Math.max(0,Math.min(100,Number(s.progress||0))),detail={progress:p,minPrice:Number(s.minPrice||0),status:s};show('Ищем лучшие предложения · '+p+'%'+(s.minPrice?' · от '+money(s.minPrice)+' ₽':'') );emit('progress',detail);if(p>0)await loadResults();if(complete(s)){const btn=form.querySelector('.primary');if(btn)btn.disabled=false;show('Поиск завершён · предложения актуальны на сейчас');const items=await loadResults();emit('complete',{progress:100,items});return;}pollTimer=setTimeout(poll,2500);}catch(e){show(e&&e.code==='TIMEOUT'?'Tourvisor отвечает дольше обычного. Попробуйте повторить поиск.':'Ошибка статуса: '+e.message);const btn=form.querySelector('.primary');if(btn)btn.disabled=false;emit('error',{phase:'status',error:e});}}
async function submit(){if(pollTimer)clearTimeout(pollTimer);results.innerHTML='<div class="skeleton-grid">'+Array.from({length:4},()=>'<div class="skeleton-card"></div>').join('')+'</div>';if(tools)tools.hidden=true;if(selected){selected.hidden=true;selected.innerHTML='';}searchId=0;rt.setSearchId(0);const btn=form.querySelector('.primary');if(btn)btn.disabled=true;show('Запускаем поиск Tourvisor…');try{const d=await api('search_start',params());searchId=Number(d.searchId||0);rt.setSearchId(searchId);if(!searchId)throw new Error('Tourvisor не вернул searchId');show('Поиск запущен · собираем предложения');emit('started',{response:d});pollTimer=setTimeout(poll,1000);}catch(e){results.innerHTML='';show(e&&e.code==='TIMEOUT'?'Tourvisor не ответил вовремя. Попробуйте ещё раз.':'Не удалось запустить поиск: '+e.message);if(btn)btn.disabled=false;emit('error',{phase:'start',error:e});}}
form.addEventListener('change',e=>{Promise.resolve(catalogs.handleChange(e)).catch(err=>console.warn('catalog change',err));});
form.addEventListener('submit',e=>{e.preventDefault();e.stopImmediatePropagation();submit();},true);
catalogs.renderChildAges();catalogs.init();
window.V2SearchLifecycle={submit,params,get searchId(){return searchId;},version:2};
})();