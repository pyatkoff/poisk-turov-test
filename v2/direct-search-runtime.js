(function(){'use strict';
const rt=window.V2Runtime,renderer=window.V2Results;if(!rt||!renderer)return;
const qs=new URLSearchParams(location.search);if(qs.get('legacySearch')==='1')return;
const form=document.getElementById('tourSearch'),status=document.getElementById('status'),results=document.getElementById('results'),selected=document.getElementById('selectedTour'),tools=document.getElementById('resultsTools'),childCount=document.getElementById('childCount'),childAges=document.getElementById('childAges'),serviceBox=document.getElementById('hotelServices'),serviceCount=document.getElementById('serviceCount');if(!form||!status||!results)return;
let searchId=0,pollTimer=0;
const api=(action,params)=>rt.api(action,params);
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
function money(v){const n=Number(v||0);return n?new Intl.NumberFormat('ru-RU').format(n):'';}
function show(text){status.hidden=false;status.textContent=text;}
function optionName(x){return x&&(x.russianName||x.fullRussianName||x.name||x.nameGenitive||x.fullName||x.id)||'';}
function fillSelect(el,items,placeholder,preferred){if(!el)return;const current=String(preferred!==undefined?preferred:el.value||''),arr=Array.isArray(items)?items:[];el.innerHTML='<option value="">'+esc(placeholder)+'</option>'+arr.map(x=>'<option value="'+esc(x.id)+'">'+esc(optionName(x))+'</option>').join('');if(current&&arr.some(x=>String(x.id)===current))el.value=current;}
async function loadCountries(){const from=form.elements.from,country=form.elements.country;if(!from||!country)return;const previous=country.value,d=await api('countries',{departureId:from.value,onlyDirect:!!(form.elements.onlyDirect&&form.elements.onlyDirect.checked),onlyCharter:!!(form.elements.onlyCharter&&form.elements.onlyCharter.checked)});fillSelect(country,d,'Выберите страну',previous);if(!country.value&&Array.isArray(d)&&d.length)country.value=String(d[0].id);}
async function loadArrivals(){const el=form.elements.arrival;if(!el)return;const departure=form.elements.from&&form.elements.from.value,country=form.elements.country&&form.elements.country.value;if(!departure||!country){fillSelect(el,[],'Любой аэропорт');return;}try{let list=await api('arrivals',{departureId:departure,onlyDirect:!!(form.elements.onlyDirect&&form.elements.onlyDirect.checked),onlyCharter:!!(form.elements.onlyCharter&&form.elements.onlyCharter.checked)});if(Array.isArray(list))list=list.filter(x=>String(x&&x.countryId||'')===String(country));fillSelect(el,list,'Любой аэропорт');}catch(e){fillSelect(el,[],'Любой аэропорт');console.warn('arrivals',e);}}
async function loadRegions(){const el=form.elements.region,country=form.elements.country&&form.elements.country.value;if(!el)return;if(!country){fillSelect(el,[],'Все курорты');return;}try{fillSelect(el,await api('regions',{countryId:country,arrivalId:form.elements.arrival&&form.elements.arrival.value||''}),'Все курорты');}catch(e){fillSelect(el,[],'Все курорты');console.warn('regions',e);}}
async function loadSubregions(){const el=form.elements.subregion,country=form.elements.country&&form.elements.country.value,region=form.elements.region&&form.elements.region.value;if(!el)return;if(!country||!region){fillSelect(el,[],region?'Все районы':'Сначала выберите курорт');return;}try{fillSelect(el,await api('subregions',{countryId:country,regionId:region}),'Все районы');}catch(e){fillSelect(el,[],'Все районы');console.warn('subregions',e);}}
function catalogRating(v){return({'2':'3','3':'3.5','4':'4','5':'4.5'})[String(v||'')]||'';}
async function loadHotelTypes(){const el=form.elements.hotel_type,country=form.elements.country&&form.elements.country.value;if(!el)return;if(!country){fillSelect(el,[],'Любой тип');return;}try{fillSelect(el,await api('hotel_types',{countryId:country}),'Любой тип');}catch(e){fillSelect(el,[],'Любой тип');console.warn('hotel_types',e);}}
async function loadHotels(){const el=form.elements.hotel,country=form.elements.country&&form.elements.country.value,region=form.elements.region&&form.elements.region.value,subregion=form.elements.subregion&&form.elements.subregion.value;if(!el)return;if(!country||!region){fillSelect(el,[],'Сначала выберите курорт');return;}try{let list=await api('hotels',{countryId:country,regionId:region,category:form.elements.stars&&form.elements.stars.value||'',rating:catalogRating(form.elements.rating&&form.elements.rating.value),types:form.elements.hotel_type&&form.elements.hotel_type.value?[form.elements.hotel_type.value]:[],limit:100});if(subregion&&Array.isArray(list))list=list.filter(x=>String(x&&x.subRegion&&x.subRegion.id||'')===String(subregion));fillSelect(el,list,'Любой отель');}catch(e){fillSelect(el,[],'Любой отель');console.warn('hotels',e);}}
function updateServiceCount(){if(!serviceCount)return;const n=form.querySelectorAll('input[name="hotel_service[]"]:checked').length;serviceCount.textContent=n?n+' выбрано':'не выбраны';}
function renderHotelServices(groups){if(!serviceBox)return;const chosen=new Set(Array.from(form.querySelectorAll('input[name="hotel_service[]"]:checked')).map(x=>String(x.value))),list=Array.isArray(groups)?groups:[];if(!list.length){serviceBox.innerHTML='<div class="service-loading">Нет доступных фильтров услуг для выбранного направления.</div>';updateServiceCount();return;}serviceBox.innerHTML=list.map(group=>'<div class="service-group"><strong>'+esc(group.name||'Услуги')+'</strong><div class="service-items">'+(Array.isArray(group.items)?group.items:[]).map(item=>'<label class="service-check"><input type="checkbox" name="hotel_service[]" value="'+esc(item.id)+'"'+(chosen.has(String(item.id))?' checked':'')+'><span>'+esc(item.name||item.id)+'</span></label>').join('')+'</div></div>').join('');updateServiceCount();}
async function loadHotelServices(){if(!serviceBox)return;const country=form.elements.country&&form.elements.country.value,region=form.elements.region&&form.elements.region.value;if(!country){renderHotelServices([]);return;}serviceBox.innerHTML='<div class="service-loading">Загружаем доступные услуги…</div>';try{renderHotelServices(await api('hotel_services',{countryId:country,regionIds:region?[region]:[]}));}catch(e){serviceBox.innerHTML='<div class="service-loading">Не удалось загрузить услуги отелей.</div>';console.warn('hotel_services',e);}}
async function loadOperators(){const el=form.elements.operator;if(!el)return;try{fillSelect(el,await api('operators',{departureId:form.elements.from.value,countryId:form.elements.country.value}),'Все операторы');}catch(e){fillSelect(el,[],'Все операторы');console.warn('operators',e);}}
async function loadMeals(){const el=form.elements.food;if(!el)return;try{fillSelect(el,await api('meals',{}),'Любое');}catch(e){console.warn('meals',e);}}
async function refreshDestinationCatalogs(){await loadArrivals();await Promise.all([loadRegions(),loadOperators(),loadHotelTypes()]);await loadSubregions();await Promise.all([loadHotels(),loadHotelServices()]);}
async function initCatalogs(){try{const from=form.elements.from,prev=from.value,deps=await api('departures',{departureCountryId:1});fillSelect(from,deps,'Город вылета',prev);if(!from.value&&Array.isArray(deps)&&deps.length)from.value=String(deps[0].id);await loadCountries();await Promise.all([refreshDestinationCatalogs(),loadMeals()]);form.dataset.catalogSource='tourvisor-direct';}catch(e){form.dataset.catalogSource='partial';console.warn('V2 catalogs:',e.message);}}
function renderChildAges(){if(!childCount||!childAges)return;const count=Math.max(0,Math.min(3,Number(childCount.value)||0));childAges.hidden=count===0;childAges.innerHTML='';for(let i=0;i<count;i++){const label=document.createElement('label');label.className='field child-age';label.innerHTML='<span>Возраст ребёнка '+(i+1)+'</span><select name="child_age[]">'+Array.from({length:18},(_,age)=>'<option value="'+age+'"'+(age===7?' selected':'')+'>'+age+(age===1?' год':age>=2&&age<=4?' года':' лет')+'</option>').join('')+'</select>';childAges.appendChild(label);}}
function params(){const f=new FormData(form),ages=f.getAll('child_age[]').map(v=>Number(v)).filter(Number.isFinite);return{departureId:f.get('from'),countryId:f.get('country'),dateFrom:f.get('dateFrom'),dateTo:f.get('dateTo'),nightsFrom:f.get('daysFrom'),nightsTo:f.get('daysTill'),adults:f.get('count_people')||2,childs:ages,meal:f.get('food')||'',hotelCategory:f.get('stars')||'',hotelRating:f.get('rating')||'',hotelTypes:f.get('hotel_type')?[f.get('hotel_type')]:[],hotelIds:f.get('hotel')?[f.get('hotel')]:[],hotelServices:f.getAll('hotel_service[]'),arrivalId:f.get('arrival')||'',regionIds:f.get('region')?[f.get('region')]:[],subregionIds:f.get('subregion')?[f.get('subregion')]:[],operatorIds:f.get('operator')?[f.get('operator')]:[],priceFrom:f.get('price_from')||'',priceTo:f.get('price_till')||'',currency:'RUB',onlyCharter:f.get('onlyCharter')?'true':'false',onlyDirect:f.get('onlyDirect')?'true':'false'};}
async function loadResults(){if(!searchId)return;renderer.render(await api('search_results',{searchId,limit:25}));}
async function poll(){if(!searchId)return;try{const s=await api('search_status',{searchId}),p=Math.max(0,Math.min(100,Number(s.progress||0)));show('Ищем лучшие предложения · '+p+'%'+(s.minPrice?' · от '+money(s.minPrice)+' ₽':'') );if(p>0)await loadResults();if(p>=100||String(s.status||'').toLowerCase()==='complete'){const btn=form.querySelector('.primary');if(btn)btn.disabled=false;show('Поиск завершён · предложения актуальны на сейчас');await loadResults();return;}pollTimer=setTimeout(poll,2500);}catch(e){show('Ошибка статуса: '+e.message);const btn=form.querySelector('.primary');if(btn)btn.disabled=false;}}
form.addEventListener('change',async e=>{if(e.target===childCount){renderChildAges();return;}if(e.target.name==='hotel_service[]'){updateServiceCount();return;}if(e.target.name==='from'||e.target.name==='onlyDirect'||e.target.name==='onlyCharter'){try{await loadCountries();await refreshDestinationCatalogs();}catch(x){console.warn(x);}return;}if(e.target.name==='country'){await refreshDestinationCatalogs();return;}if(e.target.name==='arrival'){await loadRegions();await loadSubregions();await Promise.all([loadHotels(),loadHotelServices()]);return;}if(e.target.name==='region'){await loadSubregions();await Promise.all([loadHotels(),loadHotelServices()]);return;}if(e.target.name==='subregion'){await loadHotels();return;}if((e.target.name==='stars'||e.target.name==='rating'||e.target.name==='hotel_type')&&form.elements.region&&form.elements.region.value)await loadHotels();});
form.addEventListener('submit',async e=>{e.preventDefault();e.stopImmediatePropagation();if(pollTimer)clearTimeout(pollTimer);results.innerHTML='<div class="skeleton-grid">'+Array.from({length:4},()=>'<div class="skeleton-card"></div>').join('')+'</div>';if(tools)tools.hidden=true;if(selected){selected.hidden=true;selected.innerHTML='';}searchId=0;rt.setSearchId(0);const btn=form.querySelector('.primary');if(btn)btn.disabled=true;show('Запускаем поиск Tourvisor…');try{const d=await api('search_start',params());searchId=Number(d.searchId||0);rt.setSearchId(searchId);if(!searchId)throw new Error('Tourvisor не вернул searchId');show('Поиск запущен · собираем предложения');pollTimer=setTimeout(poll,1000);}catch(e2){results.innerHTML='';show('Не удалось запустить поиск: '+e2.message);if(btn)btn.disabled=false;}},true);
renderChildAges();initCatalogs();
window.V2SearchController={refresh:refreshDestinationCatalogs,params};
})();