(function(){'use strict';
const rt=window.V2Runtime;if(!rt)return;
const cache=new Map();let currentTour=null;
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
function clean(v){return String(v||'').replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim();}
function imageUrl(v){const s=String(v||'').trim();return s.indexOf('//')===0?'https:'+s:s;}
function normalizeRooms(d){if(Array.isArray(d))return d;if(d&&Array.isArray(d.rooms))return d.rooms;return[];}
function gallery(images,name){const a=(Array.isArray(images)?images:[]).map(imageUrl).filter(Boolean).slice(0,10);if(!a.length)return'';return '<div class="room-gallery"><div class="room-gallery-main"><img src="'+esc(a[0])+'" alt="'+esc(name?'Номер '+name:'Фото номера')+'" loading="lazy"></div>'+(a.length>1?'<div class="room-gallery-thumbs">'+a.map((x,i)=>'<button type="button" data-src="'+esc(x)+'" class="room-gallery-thumb'+(i?'':' is-active')+'" aria-label="Показать фото '+(i+1)+'"><img src="'+esc(x)+'" alt="" loading="lazy"></button>').join('')+'</div>':'')+'</div>';}
function html(r){const facts=[r.area?['Площадь',r.area+' м²']:null,r.roomCount?['Комнат',r.roomCount]:null,r.bedroomCount?['Спален',r.bedroomCount]:null,r.hasBalcony?['Балкон','Есть']:null,r.hasKitchen?['Кухня','Есть']:null].filter(Boolean);return '<div class="room-detail-card">'+gallery(r.images,r.name)+'<div class="room-detail-body"><div class="section-heading"><strong>'+esc(r.name||'Описание номера')+'</strong><span>'+esc(r.location||r.viewDescription||'')+'</span></div>'+(facts.length?'<div class="room-facts">'+facts.map(x=>'<div><span>'+esc(x[0])+'</span><b>'+esc(x[1])+'</b></div>').join('')+'</div>':'')+(clean(r.description)?'<p>'+esc(clean(r.description))+'</p>':'')+(clean(r.sleepingPlaces)?'<p><b>Спальные места:</b> '+esc(clean(r.sleepingPlaces))+'</p>':'')+(clean(r.services)?'<p><b>В номере:</b> '+esc(clean(r.services))+'</p>':'')+(clean(r.comment)?'<p class="room-comment">'+esc(clean(r.comment))+'</p>':'')+'</div></div>';}
function roomIds(t){const ids=[];['roomId','roomTypeId'].forEach(k=>{const v=t&&t[k];if(Number(v)>0)ids.push(Number(v));});if(t&&t.room&&Number(t.room.id)>0)ids.push(Number(t.room.id));return [...new Set(ids)];}
function bindGallery(host){host.querySelectorAll('.room-gallery-thumb').forEach(btn=>btn.addEventListener('click',()=>{const card=btn.closest('.room-detail-card'),main=card&&card.querySelector('.room-gallery-main img');if(main)main.src=btn.dataset.src||main.src;card&&card.querySelectorAll('.room-gallery-thumb').forEach(x=>x.classList.toggle('is-active',x===btn));}));}
async function decorate(t){const selected=document.getElementById('selectedTour');if(!selected||!t||selected.hidden)return;currentTour=t;const anchor=selected.querySelector('.tour-flights')||selected.querySelector('.lead-form');if(!anchor)return;const ids=roomIds(t);if(!ids.length)return;let host=selected.querySelector('.room-details-host');if(!host){host=document.createElement('div');host.className='room-details-host';anchor.insertAdjacentElement('beforebegin',host);}const tourId=String(t.id||'');host.dataset.tourId=tourId;host.innerHTML='<div class="room-detail-loading">Загружаем описание номера…</div>';try{const missing=ids.filter(id=>!cache.has(id));if(missing.length){const response=await rt.api('rooms',{ids:missing});normalizeRooms(response).forEach(r=>{if(r&&Number(r.id)>0)cache.set(Number(r.id),r);});}const rooms=ids.map(id=>cache.get(id)).filter(Boolean);if(!host.isConnected||host.dataset.tourId!==tourId)return;host.innerHTML=rooms.length?rooms.map(html).join(''):'<div class="room-detail-loading">Для выбранного номера подробное описание не заполнено.</div>';bindGallery(host);}catch(e){if(!host.isConnected||host.dataset.tourId!==tourId)return;host.innerHTML='<div class="room-detail-loading">Описание номера временно недоступно. <button type="button" class="secondary room-detail-retry">Повторить</button></div>';}}
window.addEventListener('v2:tour-selected',e=>{const t=e.detail&&e.detail.tour;if(t)decorate(t);});
document.addEventListener('click',e=>{const retry=e.target&&e.target.closest&&e.target.closest('.room-detail-retry');if(!retry||!currentTour)return;e.preventDefault();decorate(currentTour);});
window.V2RoomDetails={decorate,normalizeRooms,roomIds,version:3};
})();
