(function(){'use strict';
const hotelData=new Map(),detailCache=new Map();
const nativeFetch=window.fetch.bind(window);
function key(v){return String(v||'').trim().toLowerCase();}
function clean(v){return String(v||'').replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim();}
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
function api(action,params){const q=new URLSearchParams({action});Object.entries(params||{}).forEach(([k,v])=>{if(v!==''&&v!==null&&v!==undefined)q.append(k,String(v));});return nativeFetch((window.V2_CONFIG&&window.V2_CONFIG.api||'api.php')+'?'+q.toString(),{credentials:'same-origin'}).then(async r=>{const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||('API HTTP '+r.status));return d;});}
window.fetch=async function(input,init){const response=await nativeFetch(input,init);try{const url=typeof input==='string'?input:(input&&input.url)||'';if(url.indexOf('action=search_results')!==-1){response.clone().json().then(list=>{if(!Array.isArray(list))return;list.forEach(h=>hotelData.set(key(h&&h.name),h||{}));decorateAll();}).catch(()=>{});}}catch(e){}return response;};
function normalizeDetail(d){return Array.isArray(d)?(d[0]||{}):(d&&typeof d==='object'?d:{});}
function mapUrl(h){const common=h&&h.common||{},lat=Number(h.latitude||common.latitude||0),lon=Number(h.longitude||common.longitude||0);if(lat&&lon)return 'https://yandex.ru/maps/?pt='+encodeURIComponent(lon+','+lat)+'&z=15&l=map';return 'https://yandex.ru/maps/?text='+encodeURIComponent([h.name,h.region&&h.region.name,h.country&&h.country.name].filter(Boolean).join(', '));}
function textBlock(title,value){const text=clean(value);return text?'<section class="hotel-detail-section"><h4>'+esc(title)+'</h4><p>'+esc(text)+'</p></section>':'';}
function factsHtml(d){const c=d.common||{},facts=[['Адрес',c.address],['Расположение',c.place],['Построен',c.build],['Реновация',c.repair],['Площадь',c.square]].filter(x=>clean(x[1]));if(!facts.length)return'';return '<div class="hotel-detail-facts">'+facts.map(x=>'<div><span>'+esc(x[0])+'</span><b>'+esc(clean(x[1]))+'</b></div>').join('')+'</div>';}
function tagsHtml(services){const groups=services&&Array.isArray(services.tags)?services.tags:[];if(!groups.length)return'';return '<section class="hotel-detail-section"><h4>Услуги</h4><div class="hotel-detail-tags">'+groups.map(g=>{const items=Array.isArray(g.items)?g.items:[];if(!items.length)return'';return '<div class="hotel-detail-tag-group"><strong>'+esc(g.name||'Услуги')+'</strong><div>'+items.map(i=>'<span>'+esc(i.name||'')+'</span>').join('')+'</div></div>';}).join('')+'</div></section>';}
function serviceTextHtml(s){if(!s)return'';return textBlock('В отеле',s.available)+textBlock('Бесплатно',s.free)+textBlock('Платные услуги',s.servicesPay)+textBlock('В номере',s.inRoom)+textBlock('Для детей',s.child)+textBlock('Анимация',s.animation);}
function galleryHtml(images,name){const list=(Array.isArray(images)?images:[]).filter(Boolean).slice(0,12);if(!list.length)return'';return '<div class="hotel-gallery"><div class="hotel-gallery-main"><img src="'+esc(list[0])+'" alt="'+esc(name||'Отель')+'" loading="lazy"></div>'+(list.length>1?'<div class="hotel-gallery-thumbs">'+list.map((src,i)=>'<button type="button" class="hotel-gallery-thumb'+(i===0?' is-active':'')+'" data-src="'+esc(src)+'"><img src="'+esc(src)+'" alt="" loading="lazy"></button>').join('')+'</div>':'')+'</div>';}
function detailHtml(d,fallback){const common=d.common||{},infra=d.infrastructure||{},meals=d.meals||{},desc=clean(common.description||fallback.hotelDescription||'');return '<div class="hotel-detail-inner">'+galleryHtml(d.images,d.name||fallback.name)+factsHtml(d)+(desc?'<section class="hotel-detail-section hotel-detail-description"><h4>Об отеле</h4><p>'+esc(desc)+'</p></section>':'')+textBlock('Территория',infra.territory)+textBlock('Пляж',infra.beach)+textBlock('Питание',[meals.list,meals.description].filter(Boolean).join(' · '))+textBlock('Номера',d.roomTypes)+serviceTextHtml(d.services)+tagsHtml(d.services)+'</div>';}
function bindGallery(detail){detail.querySelectorAll('.hotel-gallery-thumb').forEach(btn=>btn.addEventListener('click',()=>{const main=detail.querySelector('.hotel-gallery-main img');if(main)main.src=btn.dataset.src||main.src;detail.querySelectorAll('.hotel-gallery-thumb').forEach(x=>x.classList.toggle('is-active',x===btn));}));}
async function toggleDetails(card,h,btn,detail){if(!detail.hidden){detail.hidden=true;btn.textContent='Об отеле';return;}detail.hidden=false;btn.textContent='Скрыть';if(detail.dataset.loaded==='1')return;detail.innerHTML='<div class="hotel-detail-loading">Загружаем описание и фотографии…</div>';const id=Number(h.id||0);try{let d=id?detailCache.get(id):null;if(!d&&id){d=normalizeDetail(await api('hotel_details',{hotelId:id}));detailCache.set(id,d);}if(!d||!Object.keys(d).length)d=h;detail.innerHTML=detailHtml(d,h)||'<div class="hotel-detail-loading">Для этого отеля расширенное описание не заполнено.</div>';detail.dataset.loaded='1';bindGallery(detail);const map=card.querySelector('.hotel-map-link');if(map)map.href=mapUrl(d&&Object.keys(d).length?d:h);}catch(e){detail.innerHTML=detailHtml(h,h)||'<div class="hotel-detail-loading">Не удалось загрузить полное описание. '+esc(e.message)+'</div>';detail.dataset.loaded='1';bindGallery(detail);}}
function decorate(card){if(!card||card.dataset.hotelActions==='1')return;const title=card.querySelector('.hotel-title');if(!title)return;const h=hotelData.get(key(title.textContent));if(!h)return;card.dataset.hotelActions='1';const actions=document.createElement('div');actions.className='hotel-actions';actions.innerHTML='<button type="button" class="hotel-info-toggle">Об отеле</button><a class="hotel-map-link" href="'+esc(mapUrl(h))+'" target="_blank" rel="noopener noreferrer">На карте</a>';const main=card.querySelector('.hotel-main');if(main)main.insertAdjacentElement('afterend',actions);else card.prepend(actions);const detail=document.createElement('div');detail.className='hotel-inline-detail';detail.hidden=true;actions.insertAdjacentElement('afterend',detail);actions.querySelector('.hotel-info-toggle').addEventListener('click',()=>toggleDetails(card,h,actions.querySelector('.hotel-info-toggle'),detail));}
function decorateAll(){document.querySelectorAll('#results .hotel-card').forEach(decorate);}
const results=document.getElementById('results');if(results)new MutationObserver(decorateAll).observe(results,{childList:true,subtree:true});document.addEventListener('DOMContentLoaded',decorateAll);
})();