(function(){'use strict';
const qs=new URLSearchParams(location.search),enabled=qs.get('legacySearch')!=='1';if(!enabled)return;
const form=document.getElementById('tourSearch'),status=document.getElementById('status'),results=document.getElementById('results'),selected=document.getElementById('selectedTour');if(!form||!status||!results)return;
let searchId=0,pollTimer=0,currentTour=null,flightVariants=[],selectedFlightIndex=0;
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
function money(v){const n=Number(v||0);return n?new Intl.NumberFormat('ru-RU').format(n):'';}
function clean(v){return String(v||'').replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim();}
async function api(action,params){const q=new URLSearchParams({action});Object.entries(params||{}).forEach(([k,v])=>{if(v!==''&&v!==null&&v!==undefined)q.append(k,String(v));});const r=await fetch(window.V2_CONFIG.api+'?'+q.toString(),{credentials:'same-origin'});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||('API HTTP '+r.status));return d;}
function show(text){status.hidden=false;status.textContent=text;}
function params(){const f=new FormData(form);return{departureId:f.get('from'),countryId:f.get('country'),dateFrom:f.get('dateFrom'),dateTo:f.get('dateTo'),nightsFrom:f.get('daysFrom'),nightsTo:f.get('daysTill'),adults:f.get('count_people')||2,meal:f.get('food')||'',hotelCategory:f.get('stars')||'',priceFrom:f.get('price_from')||'',priceTo:f.get('price_till')||'',currency:'RUB',onlyCharter:f.get('onlyCharter')?'true':'false',onlyDirect:f.get('onlyDirect')?'true':'false'};}
function tourRow(t){const meal=t.meal&&(t.meal.russianName||t.meal.fullRussianName||t.meal.name)||'',op=t.operator&&(t.operator.russianName||t.operator.name)||'';return '<div class="tour-row"><div class="tour-meta"><strong>'+esc(t.date||'Вариант тура')+'</strong><span>'+esc([t.nights?t.nights+' ноч.':'',meal,t.roomType||'',t.placement||'',op,t.isCharter?'чартер':''].filter(Boolean).join(' · '))+'</span></div><div class="tour-action"><b>'+money(t.price)+' ₽</b><button type="button" class="direct-tour" data-tid="'+esc(t.id||'')+'">Выбрать тур</button></div></div>';}
function render(items){results.innerHTML='';if(!Array.isArray(items)||!items.length){results.innerHTML='<div class="empty">Туры пока не получены.</div>';return;}items.forEach(h=>{const card=document.createElement('article');card.className='hotel-card';const img=h.picturelink||'';card.innerHTML='<div class="hotel-main"><div class="hotel-photo">'+(img?'<img src="'+esc(img)+'" alt="">':'')+'</div><div class="hotel-body"><div class="hotel-title">'+esc(h.name||'Отель')+'</div><div class="hotel-place">'+esc([h.country&&h.country.name,h.region&&h.region.name,h.subRegion&&h.subRegion.name].filter(Boolean).join(', '))+'</div><div class="hotel-rating">'+(h.category?esc(h.category)+'★ ':'')+(h.rating?'Рейтинг '+esc(h.rating):'')+(h.seaDistance?' · море '+esc(h.seaDistance)+' м':'')+'</div><div class="hotel-bottom"><div class="hotel-price">от '+money(h.price)+' <small>₽</small></div></div></div></div><div class="hotel-tours">'+((h.tours||[]).map(tourRow).join('')||'<div class="tour-loading">Нет вариантов</div>')+'</div>';results.appendChild(card);});}
async function loadResults(){if(!searchId)return;const d=await api('search_results',{searchId,limit:25});render(d);}
async function poll(){if(!searchId)return;try{const s=await api('search_status',{searchId});const p=Math.max(0,Math.min(100,Number(s.progress||0)));show('Поиск: '+p+'%'+(s.minPrice?' · от '+money(s.minPrice)+' ₽':'')+' · '+esc(s.status||''));if(p>0)await loadResults();if(p>=100||String(s.status||'').toLowerCase()==='complete'){form.querySelector('.primary').disabled=false;show('Поиск завершён · 100%');await loadResults();return;}pollTimer=setTimeout(poll,2500);}catch(e){show('Ошибка статуса: '+e.message);form.querySelector('.primary').disabled=false;}}
function portText(p){if(!p)return '—';const name=p.shortName||p.name||'',id=p.id||'';return esc(name+(id?' ('+id+')':''));}
function segmentHtml(f,label){if(!f||typeof f!=='object')return '';const company=f.company&&f.company.name||'',num=f.number||'',plane=f.plane||'',dep=f.departure||{},arr=f.arrival||{},baggage=f.baggage!==undefined&&f.baggage!==null?f.baggage+' кг':'—',carry=f.carryOn||'—';return '<div class="flight-segment"><div class="flight-segment-title"><strong>'+esc(label)+'</strong><span>'+esc([company,num,plane].filter(Boolean).join(' · '))+'</span></div><div class="flight-route"><div><b>'+portText(dep.port)+'</b><span>'+esc([dep.date,dep.time].filter(Boolean).join(' · ')||'—')+'</span></div><div class="flight-arrow">→</div><div><b>'+portText(arr.port)+'</b><span>'+esc([arr.date,arr.time].filter(Boolean).join(' · ')||'—')+'</span></div></div><div class="flight-baggage">Багаж: <b>'+esc(baggage)+'</b> · Ручная кладь: <b>'+esc(carry)+'</b></div></div>';}
function variantHtml(v,i){const fwd=Array.isArray(v.forward)?v.forward:[],back=Array.isArray(v.backward)?v.backward:[],priceObj=v.price||{},fuelObj=v.fuelCharge||{},priceValue=priceObj.value!==undefined?priceObj.value:priceObj,fuelValue=fuelObj.value!==undefined?fuelObj.value:fuelObj,checked=i===selectedFlightIndex?' checked':'';return '<div class="flight-variant"><label class="flight-choice"><input type="radio" name="v2flight" value="'+i+'"'+checked+'> <strong>Выбрать вариант '+(i+1)+(v.isDefault?' · основной':'')+'</strong>'+(priceValue?'<span> · '+money(priceValue)+' ₽</span>':'')+'</label>'+fwd.map((f,j)=>segmentHtml(f,j===0?'Туда':'Пересадка туда')).join('')+back.map((f,j)=>segmentHtml(f,j===0?'Обратно':'Пересадка обратно')).join('')+(fuelValue?'<div class="flight-fuel">Топливный сбор: <b>'+money(fuelValue)+' ₽</b></div>':'')+'</div>';}
function renderFlights(data){const box=selected.querySelector('.tour-flights');if(!box)return;let list=[];if(Array.isArray(data))list=data;else if(data&&Array.isArray(data.flights))list=data.flights;else if(data&&Array.isArray(data.items))list=data.items;flightVariants=list;if(!list.length){box.innerHTML='<strong>Авиарейсы</strong><p>Tourvisor не вернул детали рейсов для этого тура.</p>';return;}const def=list.findIndex(v=>v&&v.isDefault);selectedFlightIndex=def>=0?def:0;box.innerHTML='<strong>Авиарейсы</strong><p>Выберите вариант перелёта — он попадёт в заявку менеджеру.</p><div class="flight-variants">'+list.map(variantHtml).join('')+'</div>';}
async function loadFlights(tid,btn){if(!tid||!btn)return;btn.disabled=true;btn.textContent='Загружаем рейсы…';const box=selected.querySelector('.tour-flights');if(box)box.innerHTML='<strong>Авиарейсы</strong><p>Запрашиваем актуальные данные у туроператора…</p>';try{const d=await api('flights',{tourId:tid,currency:'RUB'});renderFlights(d);}catch(e){if(box)box.innerHTML='<strong>Авиарейсы</strong><p>Не удалось получить рейсы: '+esc(e.message)+'</p><button type="button" class="load-flights" data-tid="'+esc(tid)+'">Повторить запрос</button>';}}
function flightLegText(f,label){if(!f)return '';const dep=f.departure||{},arr=f.arrival||{},company=f.company&&f.company.name||'',depPort=dep.port||{},arrPort=arr.port||{};return label+': '+[company,f.number,f.plane].filter(Boolean).join(' · ')+'; '+[depPort.shortName||depPort.name,depPort.id].filter(Boolean).join(' ')+' '+[dep.date,dep.time].filter(Boolean).join(' ')+' → '+[arrPort.shortName||arrPort.name,arrPort.id].filter(Boolean).join(' ')+' '+[arr.date,arr.time].filter(Boolean).join(' ')+'; багаж '+(f.baggage!=null?f.baggage+' кг':'—')+', ручная кладь '+(f.carryOn||'—');}
function selectedFlightData(){const v=flightVariants[selectedFlightIndex];if(!v)return{flight:'',flightPrice:'',flightFuel:''};const parts=[];(Array.isArray(v.forward)?v.forward:[]).forEach((f,i)=>parts.push(flightLegText(f,i===0?'Туда':'Пересадка туда')));(Array.isArray(v.backward)?v.backward:[]).forEach((f,i)=>parts.push(flightLegText(f,i===0?'Обратно':'Пересадка обратно')));const p=v.price||{},fuel=v.fuelCharge||{};return{flight:parts.filter(Boolean).join(' | '),flightPrice:p&&p.value!==undefined?p.value:p||'',flightFuel:fuel&&fuel.value!==undefined?fuel.value:fuel||''};}
function attribution(){const p=new URLSearchParams(location.search),cookie=Object.fromEntries(document.cookie.split(';').map(v=>v.trim().split('=').map(decodeURIComponent)).filter(a=>a.length===2));return{yclid:p.get('yclid')||'',yaclient:cookie._ym_uid||'',utm_source:p.get('utm_source')||'',utm_medium:p.get('utm_medium')||'',utm_campaign:p.get('utm_campaign')||'',utm_content:p.get('utm_content')||'',utm_term:p.get('utm_term')||''};}
function leadPayload(fd){const t=currentTour||{},h=t.hotel||{},meal=t.meal&&(t.meal.russianName||t.meal.fullRussianName||t.meal.name)||'',op=t.operator&&(t.operator.russianName||t.operator.name)||'';return Object.assign({name:fd.get('name')||'',phone:fd.get('phone')||'',comment:fd.get('comment')||'',tourId:t.id||'',searchId:searchId||'',hotel:h.name||t.name||'',country:h.country&&h.country.name||'',region:h.region&&h.region.name||'',departure:t.departure&&t.departure.name||'',date:t.date||'',nights:t.nights||'',adults:t.adults||'',childs:t.childs||'',meal:meal,roomType:t.roomType||'',placement:t.placement||'',operator:op,price:t.price||'',currency:'RUB',page:location.href},selectedFlightData(),attribution());}
async function submitLead(leadForm){const btn=leadForm.querySelector('button[type="submit"]'),msg=leadForm.querySelector('.lead-message');btn.disabled=true;msg.textContent='Отправляем заявку…';let sent=false;try{const r=await fetch('/poisk-turov-test/v2/lead-adapter.php',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify(leadPayload(new FormData(leadForm)))}),d=await r.json().catch(()=>({}));if(!r.ok||!d.ok||!d.writes)throw new Error(d.error||'Заявка не была записана');sent=true;msg.textContent='Заявка отправлена менеджеру. Номер заявки: '+(d.leadId||'—');btn.textContent='Заявка отправлена';leadForm.dataset.sent='1';}catch(e){msg.textContent='Не удалось отправить заявку: '+e.message;}finally{if(!sent)btn.disabled=false;}}
async function showTour(tid){if(!tid)return;currentTour=null;flightVariants=[];selectedFlightIndex=0;selected.hidden=false;selected.innerHTML='<div class="selected-loading">Загружаем тур…</div>';selected.scrollIntoView({behavior:'smooth',block:'start'});try{const t=await api('tour',{tourId:tid,currency:'RUB'});currentTour=t;const h=t.hotel||{},meal=t.meal&&(t.meal.russianName||t.meal.fullRussianName||t.meal.name)||'',op=t.operator&&(t.operator.russianName||t.operator.name)||'',place=[h.country&&h.country.name,h.region&&h.region.name,h.subRegion&&h.subRegion.name].filter(Boolean).join(', '),desc=clean(t.hotelDescription),pic=t.picture||'',people=[t.adults?t.adults+' взр.':'',t.childs?t.childs+' дет.':''].filter(Boolean).join(' + '),flightType=t.isCharter?'Чартер':'Регулярный/не указан';selected.innerHTML='<button type="button" class="back-results">← К результатам</button><div class="selected-head"><div><span class="eyebrow">ВЫБРАННЫЙ ТУР</span><h2>'+esc(h.name||t.name||'Тур')+'</h2><p>'+esc(place)+'</p></div><div class="selected-price">'+money(t.price)+' ₽</div></div>'+(pic?'<div class="selected-picture"><img src="'+esc(pic)+'" alt=""></div>':'')+'<div class="facts"><div><span>Вылет из</span><b>'+esc(t.departure&&t.departure.name||'—')+'</b></div><div><span>Дата</span><b>'+esc(t.date||'—')+'</b></div><div><span>Ночей</span><b>'+esc(t.nights||'—')+'</b></div><div><span>Туристы</span><b>'+esc(people||'—')+'</b></div><div><span>Питание</span><b>'+esc(meal||'—')+'</b></div><div><span>Номер</span><b>'+esc(t.roomType||'—')+'</b></div><div><span>Размещение</span><b>'+esc(t.placement||'—')+'</b></div><div><span>Оператор</span><b>'+esc(op||'—')+'</b></div><div><span>Перелёт</span><b>'+esc(flightType)+'</b></div><div><span>Топливный сбор</span><b>'+(t.fuelCharge?money(t.fuelCharge)+' ₽':'—')+'</b></div></div>'+(desc?'<div class="hotel-desc">'+esc(desc)+'</div>':'')+'<div class="tour-flights"><strong>Авиарейсы</strong><p>Рейсы загружаются по запросу. Если вариантов несколько, можно выбрать нужный для заявки.</p><button type="button" class="load-flights" data-tid="'+esc(tid)+'">Показать рейсы</button></div><form class="lead-form"><h3>Оставить заявку на этот тур</h3><label>Имя<input name="name" maxlength="120" autocomplete="name"></label><label>Телефон<input name="phone" type="tel" required maxlength="40" autocomplete="tel" placeholder="+7 999 123-45-67"></label><label>Комментарий<textarea name="comment" maxlength="1000" rows="3" placeholder="Необязательно"></textarea></label><button type="submit" class="primary">Отправить заявку</button><p class="lead-message">Заявка будет записана менеджеру с выбранным туром и вариантом перелёта.</p></form>';}catch(e){selected.innerHTML='<button type="button" class="back-results">← К результатам</button><div class="selected-loading">Ошибка загрузки тура: '+esc(e.message)+'</div>';}}
form.addEventListener('submit',async e=>{e.preventDefault();e.stopImmediatePropagation();if(pollTimer)clearTimeout(pollTimer);results.innerHTML='';selected.hidden=true;selected.innerHTML='';currentTour=null;flightVariants=[];searchId=0;const btn=form.querySelector('.primary');btn.disabled=true;show('Запускаем поиск…');try{const d=await api('search_start',params());searchId=Number(d.searchId||0);if(!searchId)throw new Error('Tourvisor не вернул searchId');show('Поиск запущен');pollTimer=setTimeout(poll,1200);}catch(e2){show('Не удалось запустить поиск: '+e2.message);btn.disabled=false;}},true);
results.addEventListener('click',e=>{const b=e.target.closest('.direct-tour');if(b){e.preventDefault();e.stopImmediatePropagation();showTour(b.dataset.tid);}},true);
selected.addEventListener('change',e=>{if(e.target&&e.target.name==='v2flight')selectedFlightIndex=Math.max(0,Number(e.target.value)||0);},true);
selected.addEventListener('submit',e=>{const lf=e.target.closest('.lead-form');if(lf){e.preventDefault();if(lf.dataset.sent==='1')return;submitLead(lf);}},true);
selected.addEventListener('click',e=>{const flight=e.target.closest('.load-flights');if(flight){e.preventDefault();loadFlights(flight.dataset.tid,flight);return;}if(e.target.closest('.back-results'))results.scrollIntoView({behavior:'smooth',block:'start'});},true);
})();