# CI workflow audit batch index

`CI_WORKFLOW_AUDIT.md` is the primary audit record and `TEST_MATRIX.md` owns tier policy.

Verified companion batches that are intentionally kept narrow while the exhaustive inventory is in progress:

- `CI_WORKFLOW_AUDIT_ROOMS_FLIGHTS.md` — rooms / flights / price lifecycle, including PR browser contracts, live bootstrap overlap and the confirmed flight-price live trigger gap.

Once all workflow families are inventoried, these batch notes should be folded into the primary audit in one documentation-only consolidation so there is again one canonical audit document. Until then, no workflow should be removed from companion findings alone.
