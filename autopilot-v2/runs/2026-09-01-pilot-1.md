# Autopilot 2.0 — Pilot 1 dry run

## Selected task
`PILOT_ROOT_STABILIZATION_REAUDIT` — re-audit standalone results and selected-tour transitions.

## Why this task
The existing `AUTOPILOT_STATE.json` marks standalone production stabilization as the highest-priority active work and explicitly names results-state transitions plus selected-tour room/flight/price/lead recovery as the next action.

## Orchestrator decision
- Area: `ux-qa`
- Risk: `SAFE` for the audit itself; any code fix discovered later must be reclassified independently.
- Owner: `qa` for evidence collection; `developer` is assigned only if a concrete defect is reproduced.
- Product code mutation in this dry run: **none**.

## Minimal gates
1. No CI gate is required for observation-only audit output.
2. If a product-code defect is fixed: run `Fast CI`.
3. If the fix changes user-facing UI: perform targeted visual QA only for the affected route/state and relevant mobile + desktop widths.
4. If a release occurs: use existing deploy workflow plus `Production smoke` only.
5. Do not launch retired broad visual/live/SEO matrices.

## QA handoff
Audit the current production flow in this order:
1. Search results populated state and result-refresh transitions.
2. Hotel/result detail open → selected-tour state.
3. Room selection and price synchronization.
4. Flight information visibility and selected-flight state.
5. Lead-form entry/recovery without changing the external lead contract.

For each stage record only one of:
- `PASS` — expected state transition works;
- `DEFECT` — reproducible user-visible or data defect, with exact reproduction;
- `BLOCKED_EXTERNAL` — cannot verify because of an external dependency.

Do not create a code task from subjective polish alone during this pilot. A code task requires a concrete failure, wrong data, broken transition, or material UX regression.

## QA attempt 1
A targeted live probe was attempted against `https://anytoour.ru/poisk-turov/` and the minimal API sequence `search_start → search_status → search_results → hotel_details → tour`.

Result: `BLOCKED_EXTERNAL` before the first HTTP request because the execution environment could not resolve DNS for `anytoour.ru` (`curl: (6) Could not resolve host`). This is not classified as a product defect and does not create a Developer task.

## Next transition
Keep the pilot in `qa` with an external verification blocker recorded. Continue independent safe work; rerun only this narrow live probe from an environment with working DNS rather than restoring the retired full user-journey matrix.
