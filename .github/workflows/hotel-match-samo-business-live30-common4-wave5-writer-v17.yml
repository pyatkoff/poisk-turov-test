name: MATCH SAMO business-live30 wave5 writer v17 serial
on:
  pull_request:
    types: [opened, synchronize]
    branches: [feature/anex-search-adapter-20260907]
    paths:
      - 'scripts/diagnostics/hotel_match_samo_business_live30_common4_wave5_writer_v17.php'
      - 'tests/hotel_match_samo_business_live30_common4_wave5_writer_v17_test.php'
      - '.github/workflows/hotel-match-samo-business-live30-common4-wave5-writer-v17.yml'
permissions: {contents: read, actions: read, issues: read}
concurrency: {group: hotel-match-samo-business-live30-common4-wave5-writer-v17, cancel-in-progress: false}
env:
  AUDIT_OP: hotel-match-samo-business-live30-common4-wave5-current-1971-20260925-v16c
  W1: hotel-match-samo-business-live30-common4-wave5-writer-1971-20260925-v17-w1
  W2: hotel-match-samo-business-live30-common4-wave5-writer-1971-20260925-v17-w2
  BASE: c3b28595541ecb85286227aa146d70329bf324f3
  CLAIM: '5829674112'
  AUDIT_ARTIFACT: '10854607010'
  AUDIT_ARCHIVE_SHA: 1d4a1f66015990e09945a414c70beb2ae619d26c63663bf43ee8f478e1f8122e
  AUDIT_RESULT_SHA: 1791594e468c29a433c2a3263bf29ade2d34dbb3311e060c2c0071b28b5cf8f8
jobs:
  focused:
    if: github.repository == 'pyatkoff/poisk-turov-test' && github.actor == 'pyatkoff' && github.event.pull_request.head.ref == 'match/samo-business-live30-wave5-writer-v17-20260925'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with: {ref: '${{ github.event.pull_request.head.sha }}', fetch-depth: 0, persist-credentials: false}
      - run: php -l scripts/diagnostics/hotel_match_samo_business_live30_common4_wave5_writer_v17.php
      - run: php scripts/diagnostics/hotel_match_samo_business_live30_common4_wave5_writer_v17.php --self-test
      - run: php tests/hotel_match_samo_business_live30_common4_wave5_writer_v17_test.php
      - run: |
          ! grep -E 'curl_|https?://|Tourvisor|SearchTour|PRICES\?|package/getFlights' scripts/diagnostics/hotel_match_samo_business_live30_common4_wave5_writer_v17.php
  live:
    needs: focused
    if: github.repository == 'pyatkoff/poisk-turov-test' && github.actor == 'pyatkoff' && github.event.pull_request.head.ref == 'match/samo-business-live30-wave5-writer-v17-20260925'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with: {ref: '${{ github.event.pull_request.head.sha }}', fetch-depth: 0, persist-credentials: false}
      - name: Validate claim Security and exact CURRENT audit
        env: {GH_TOKEN: '${{ github.token }}', HEAD: '${{ github.event.pull_request.head.sha }}'}
        run: |
          set -euo pipefail;umask 077;mkdir -p input/audit terminal/w1 terminal/w2
          test "$(git merge-base HEAD "$BASE")" = "$BASE"
          actual="$(git diff --name-only "$BASE..HEAD" | sort | tr '\n' ' ')"
          test "$actual" = ".github/workflows/hotel-match-samo-business-live30-common4-wave5-writer-v17.yml scripts/diagnostics/hotel_match_samo_business_live30_common4_wave5_writer_v17.php tests/hotel_match_samo_business_live30_common4_wave5_writer_v17_test.php "
          gh api "repos/$GITHUB_REPOSITORY/issues/comments/$CLAIM" >input/claim.json
          python3 - <<'PY'
          import json,os,time,urllib.request
          c=json.load(open('input/claim.json'));b=c['body']
          assert c['user']['id']==226193297 and c['author_association']=='OWNER' and c['issue_url'].endswith('/issues/3419')
          for x in [os.environ['W1'],os.environ['W2'],'expected writes=300','offset0','expected writes=199','offset300','Max300']: assert x in b,x
          u='https://api.github.com/repos/'+os.environ['GITHUB_REPOSITORY']+'/actions/runs?head_sha='+os.environ['HEAD']+'&per_page=100'
          for _ in range(120):
              q=urllib.request.Request(u,headers={'Authorization':'Bearer '+os.environ['GH_TOKEN'],'Accept':'application/vnd.github+json'})
              with urllib.request.urlopen(q,timeout=30) as f:runs=json.load(f)['workflow_runs']
              xs=[x for x in runs if x['name']=='Security guard' and x['head_sha']==os.environ['HEAD']]
              if xs:
                  x=max(xs,key=lambda y:y['id'])
                  if x['status']=='completed':
                      assert x['conclusion']=='success',x['conclusion'];break
              time.sleep(3)
          else: raise SystemExit('security_not_green')
          PY
          curl -fsSL -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/artifacts/$AUDIT_ARTIFACT/zip" -o input/audit.zip
          test "$(sha256sum input/audit.zip|awk '{print $1}')" = "$AUDIT_ARCHIVE_SHA"
          unzip -q input/audit.zip -d input/audit
          result="$(find input/audit -type f -name result.json -print -quit)";receipt="$(find input/audit -type f -name receipt.json -print -quit)"
          test -n "$result";test -n "$receipt";cp "$result" input/audit-result.json;cp "$receipt" input/audit-receipt.json
          python3 - <<'PY'
          import json,hashlib,pathlib,os
          r=json.load(open('input/audit-result.json'));q=json.load(open('input/audit-receipt.json'))
          h=hashlib.sha256(pathlib.Path('input/audit-result.json').read_bytes()).hexdigest()
          assert q['result_sha256']==h==os.environ['AUDIT_RESULT_SHA']
          assert r['operation']==os.environ['AUDIT_OP'] and r['state']=='completed_read_only_samo_business_common4_current'
          assert r['captured_single_native_edge_count']==508 and r['writer_ready_total']==499
          assert r['writer_ready_counts']=={'operator_342':499}
          assert r['status_counts']=={'hold_manual_target':6,'hold_source_occupied':3,'writer_ready':499}
          assert r['provider_http_calls']==r['database_writes']==r['mapping_writes']==0
          pathlib.Path('input/audit.sha').write_text(h)
          PY
      - name: Commit first 300 Intourist identities
        env: {KEY: '${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}', HOST: '${{ secrets.ANYTOOUR_DEPLOY_HOST }}', USER: '${{ secrets.ANYTOOUR_DEPLOY_USER }}', HEAD: '${{ github.event.pull_request.head.sha }}', OP: '${{ env.W1 }}'}
        run: |
          set -euo pipefail;umask 077
          key="$RUNNER_TEMP/key";known="$RUNNER_TEMP/known";trap 'rm -f "$key" "$known"' EXIT
          if [[ "$KEY" == *"PRIVATE KEY"* ]];then printf '%s\n' "$KEY"|tr -d '\r'>"$key";else printf '%s' "$KEY"|base64 --decode>"$key";fi
          chmod 600 "$key";ssh-keygen -y -f "$key">/dev/null;ssh-keyscan -T15 -t ed25519 "$HOST">"$known" 2>/dev/null
          remote="$USER@$HOST";opts=(-i "$key" -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=yes -o "UserKnownHostsFile=$known" -o GlobalKnownHostsFile=/dev/null -o ConnectTimeout=15)
          op=".anytoour-match/operations/$OP";asha="$(cat input/audit.sha)"
          python3 - <<'PY'
          import json,os,pathlib
          pathlib.Path('input/reservation-w1.json').write_text(json.dumps({'operation':os.environ['OP'],'state':'reserved_before_write','expected_writes':300,'write_offset':0,'audit_operation':os.environ['AUDIT_OP'],'provider_http_calls':0},sort_keys=True)+'\n')
          PY
          ssh "${opts[@]}" "$remote" "set -eu;umask 077;test ! -e \"\$HOME/$op\";mkdir -p \"\$HOME/$op/payload/scripts/diagnostics\""
          scp "${opts[@]}" input/reservation-w1.json "$remote:$op/reservation.json"
          scp "${opts[@]}" input/audit-result.json "$remote:$op/payload/audit-result.json"
          scp "${opts[@]}" scripts/diagnostics/hotel_match_samo_business_live30_common4_wave5_writer_v17.php "$remote:$op/payload/scripts/diagnostics/"
          set +e
          ssh "${opts[@]}" "$remote" "set -eu;cd \"\$HOME/www/anytoour.ru\";ANYTOUR_ROOT=\"\$HOME/www/anytoour.ru\" MATCH_OPERATION_DIR=\"\$HOME/$op\" MATCH_AUDIT_RESULT=\"\$HOME/$op/payload/audit-result.json\" MATCH_AUDIT_SHA='$asha' MATCH_SOURCE_SHA='$HEAD' MATCH_WRITER_OPERATION='$OP' MATCH_EXPECTED_WRITES=300 MATCH_WRITE_OFFSET=0 php \"\$HOME/$op/payload/$(basename scripts/diagnostics/hotel_match_samo_business_live30_common4_wave5_writer_v17.php)\" --execute" >terminal/w1/remote.stdout 2>terminal/w1/remote.stderr
          status=$?
          ssh "${opts[@]}" "$remote" "cd \"\$HOME/$op\";tar -czf - reservation.json pre-commit.json commit-attempt.json result.json receipt.json" >terminal/w1/server.tgz
          set -e
          python3 - <<'PY'
          import tarfile,pathlib,json,hashlib
          with tarfile.open('terminal/w1/server.tgz','r:gz') as t:
              for m in t.getmembers():
                  if m.isfile():pathlib.Path('terminal/w1',m.name).write_bytes(t.extractfile(m).read())
          r=json.load(open('terminal/w1/result.json'));q=json.load(open('terminal/w1/receipt.json'))
          assert q['result_sha256']==hashlib.sha256(pathlib.Path('terminal/w1/result.json').read_bytes()).hexdigest()
          assert r['state']=='committed_verified' and r['inserted']==300 and r['database_writes']==300 and r['mapping_writes']==300
          assert r['inserted_by_namespace']=={'operator_342':300} and r['readback_verified'] is True
          print(json.dumps({k:r[k] for k in ['state','inserted','inserted_by_namespace','unique_targets','readback_verified']},sort_keys=True))
          PY
          test "$status" -eq 0
      - name: Commit remaining 199 Intourist identities
        env: {KEY: '${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}', HOST: '${{ secrets.ANYTOOUR_DEPLOY_HOST }}', USER: '${{ secrets.ANYTOOUR_DEPLOY_USER }}', HEAD: '${{ github.event.pull_request.head.sha }}', OP: '${{ env.W2 }}'}
        run: |
          set -euo pipefail;umask 077
          test "$(python3 -c "import json;print(json.load(open('terminal/w1/result.json'))['state'])")" = committed_verified
          key="$RUNNER_TEMP/key";known="$RUNNER_TEMP/known";trap 'rm -f "$key" "$known"' EXIT
          if [[ "$KEY" == *"PRIVATE KEY"* ]];then printf '%s\n' "$KEY"|tr -d '\r'>"$key";else printf '%s' "$KEY"|base64 --decode>"$key";fi
          chmod 600 "$key";ssh-keygen -y -f "$key">/dev/null;ssh-keyscan -T15 -t ed25519 "$HOST">"$known" 2>/dev/null
          remote="$USER@$HOST";opts=(-i "$key" -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=yes -o "UserKnownHostsFile=$known" -o GlobalKnownHostsFile=/dev/null -o ConnectTimeout=15)
          op=".anytoour-match/operations/$OP";asha="$(cat input/audit.sha)"
          python3 - <<'PY'
          import json,os,pathlib
          pathlib.Path('input/reservation-w2.json').write_text(json.dumps({'operation':os.environ['OP'],'state':'reserved_before_write','expected_writes':199,'write_offset':300,'audit_operation':os.environ['AUDIT_OP'],'provider_http_calls':0},sort_keys=True)+'\n')
          PY
          ssh "${opts[@]}" "$remote" "set -eu;umask 077;test ! -e \"\$HOME/$op\";mkdir -p \"\$HOME/$op/payload/scripts/diagnostics\""
          scp "${opts[@]}" input/reservation-w2.json "$remote:$op/reservation.json"
          scp "${opts[@]}" input/audit-result.json "$remote:$op/payload/audit-result.json"
          scp "${opts[@]}" scripts/diagnostics/hotel_match_samo_business_live30_common4_wave5_writer_v17.php "$remote:$op/payload/scripts/diagnostics/"
          set +e
          ssh "${opts[@]}" "$remote" "set -eu;cd \"\$HOME/www/anytoour.ru\";ANYTOUR_ROOT=\"\$HOME/www/anytoour.ru\" MATCH_OPERATION_DIR=\"\$HOME/$op\" MATCH_AUDIT_RESULT=\"\$HOME/$op/payload/audit-result.json\" MATCH_AUDIT_SHA='$asha' MATCH_SOURCE_SHA='$HEAD' MATCH_WRITER_OPERATION='$OP' MATCH_EXPECTED_WRITES=199 MATCH_WRITE_OFFSET=300 php \"\$HOME/$op/payload/$(basename scripts/diagnostics/hotel_match_samo_business_live30_common4_wave5_writer_v17.php)\" --execute" >terminal/w2/remote.stdout 2>terminal/w2/remote.stderr
          status=$?
          ssh "${opts[@]}" "$remote" "cd \"\$HOME/$op\";tar -czf - reservation.json pre-commit.json commit-attempt.json result.json receipt.json" >terminal/w2/server.tgz
          set -e
          python3 - <<'PY'
          import tarfile,pathlib,json,hashlib
          with tarfile.open('terminal/w2/server.tgz','r:gz') as t:
              for m in t.getmembers():
                  if m.isfile():pathlib.Path('terminal/w2',m.name).write_bytes(t.extractfile(m).read())
          r=json.load(open('terminal/w2/result.json'));q=json.load(open('terminal/w2/receipt.json'))
          assert q['result_sha256']==hashlib.sha256(pathlib.Path('terminal/w2/result.json').read_bytes()).hexdigest()
          assert r['state']=='committed_verified' and r['inserted']==199 and r['database_writes']==199 and r['mapping_writes']==199
          assert r['inserted_by_namespace']=={'operator_342':199} and r['readback_verified'] is True
          print(json.dumps({k:r[k] for k in ['state','inserted','inserted_by_namespace','unique_targets','readback_verified']},sort_keys=True))
          PY
          test "$status" -eq 0
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with: {name: 'match-samo-business-live30-wave5-writer-v17-${{ github.run_id }}', path: terminal/, retention-days: 90, if-no-files-found: error}
