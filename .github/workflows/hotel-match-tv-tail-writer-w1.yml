name: MATCH TV tail writer W1
on:
  pull_request:
    types: [opened, synchronize]
    branches: [feature/anex-search-adapter-20260907]
    paths: ['.github/workflows/hotel-match-tv-tail-writer-w1.yml']
permissions: {contents: read, actions: read, issues: read}
concurrency: {group: hotel-match-common4-tail-writer-w1-20260924, cancel-in-progress: false}
env:
  OPDIR: hotel-match-common4-tail-writer-1971-20260924-v15-w1
  BASE: d8d08043cd6f7c14e9d6de7bae9955102bf548b6
  CLAIM: '5804468591'
  AUDIT_ARTIFACT: '10780823859'
  AUDIT_ARCHIVE_SHA: b3a0d90bfedc809bb5f517bf05a01542204d2271cc56766d66023dd3e9499d2c
  EXPECTED_WRITES: '300'
  WRITE_OFFSET: '0'
jobs:
  live:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with: {ref: '${{ github.event.pull_request.head.sha }}', fetch-depth: 0, persist-credentials: false}
      - name: Validate authority and audit
        env: {GH_TOKEN: '${{ github.token }}', HEAD: '${{ github.event.pull_request.head.sha }}'}
        run: |
          set -euo pipefail;umask 077;mkdir -p input/audit terminal
          test "$(git merge-base HEAD "$BASE")" = "$BASE"
          test "$(git diff --name-only "$BASE..HEAD" | sort | tr '\n' ' ')" = ".github/workflows/hotel-match-tv-tail-writer-w1.yml "
          gh api "repos/$GITHUB_REPOSITORY/issues/comments/$CLAIM" >input/claim.json
          python3 - <<'PY'
          import json,os,time,urllib.request
          c=json.load(open('input/claim.json'));b=c['body']
          assert c['user']['id']==226193297 and c['author_association']=='OWNER'
          for x in [os.environ['OPDIR'],'offset0 exact expected writes=300',os.environ['AUDIT_ARCHIVE_SHA'],'SERIALIZABLE','post-COMMIT readback','HTTP0']:
              assert x in b,x
          u='https://api.github.com/repos/'+os.environ['GITHUB_REPOSITORY']+'/actions/runs?head_sha='+os.environ['HEAD']+'&per_page=100'
          for _ in range(100):
              q=urllib.request.Request(u,headers={'Authorization':'Bearer '+os.environ['GH_TOKEN'],'Accept':'application/vnd.github+json'})
              with urllib.request.urlopen(q,timeout=30) as f:runs=json.load(f)['workflow_runs']
              xs=[x for x in runs if x['name']=='Security guard' and x['head_sha']==os.environ['HEAD']]
              if xs:
                  x=max(xs,key=lambda y:y['id'])
                  if x['status']=='completed':
                      assert x['conclusion']=='success';break
              time.sleep(3)
          else: raise SystemExit('security_not_green')
          PY
          curl -fsSL -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/artifacts/$AUDIT_ARTIFACT/zip" -o input/audit.zip
          test "$(sha256sum input/audit.zip|awk '{print $1}')" = "$AUDIT_ARCHIVE_SHA"
          unzip -q input/audit.zip -d input/audit
          result="$(find input/audit -type f -name result.json -print -quit)"; receipt="$(find input/audit -type f -name receipt.json -print -quit)"
          cp "$result" input/audit-result.json;cp "$receipt" input/audit-receipt.json
          python3 - <<'PY'
          import json,hashlib,pathlib
          r=json.load(open('input/audit-result.json'));q=json.load(open('input/audit-receipt.json'))
          assert q['result_sha256']==hashlib.sha256(pathlib.Path('input/audit-result.json').read_bytes()).hexdigest()
          assert r['operation']=='hotel-match-common4-tail-current-1971-20260924-v15'
          assert r['state']=='completed_read_only_mass_current'
          assert r['writer_ready_counts']=={'anex':41,'identity':367}
          assert r['supplier_calls']==0 and r['database_writes']==0 and r['mapping_writes']==0
          PY
      - name: Execute guarded W1
        env:
          KEY: '${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}'
          HOST: '${{ secrets.ANYTOOUR_DEPLOY_HOST }}'
          USER: '${{ secrets.ANYTOOUR_DEPLOY_USER }}'
          HEAD: '${{ github.event.pull_request.head.sha }}'
        run: |
          set -euo pipefail;umask 077
          key="$RUNNER_TEMP/key";known="$RUNNER_TEMP/known";trap 'rm -f "$key" "$known"' EXIT
          if [[ "$KEY" == *"PRIVATE KEY"* ]];then printf '%s\n' "$KEY"|tr -d '\r'>"$key";else printf '%s' "$KEY"|base64 --decode>"$key";fi
          chmod 600 "$key";ssh-keygen -y -f "$key">/dev/null;ssh-keyscan -T15 -t ed25519 "$HOST">"$known" 2>/dev/null
          remote="$USER@$HOST";opts=(-i "$key" -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=yes -o "UserKnownHostsFile=$known" -o GlobalKnownHostsFile=/dev/null -o ConnectTimeout=15)
          op=".anytoour-match/operations/$OPDIR"
          auditsha="$(sha256sum input/audit-result.json|awk '{print $1}')"
          python3 - <<'PY'
          import json,os,pathlib
          pathlib.Path('input/reservation.json').write_text(json.dumps({'operation':os.environ['OPDIR'],'state':'reserved_before_write','expected_writes':300,'write_offset':0},sort_keys=True)+'\n')
          PY
          ssh "${opts[@]}" "$remote" "set -eu;umask 077;test ! -e "\$HOME/$op";mkdir -p "\$HOME/$op/payload""
          scp "${opts[@]}" input/reservation.json "$remote:$op/reservation.json"
          scp "${opts[@]}" input/audit-result.json scripts/diagnostics/hotel_match_common4_tail_writer_v15.php "$remote:$op/payload/"
          set +e
          ssh "${opts[@]}" "$remote" "set -eu;cd "\$HOME/www/anytoour.ru";ANYTOUR_ROOT="\$HOME/www/anytoour.ru" MATCH_OPERATION_DIR="\$HOME/$op" MATCH_AUDIT_RESULT="\$HOME/$op/payload/audit-result.json" MATCH_AUDIT_SHA='$auditsha' MATCH_SOURCE_SHA='$HEAD' MATCH_EXPECTED_WRITES='300' MATCH_WRITE_OFFSET='0' php "\$HOME/$op/payload/hotel_match_common4_tail_writer_v15.php" --execute" >terminal/remote.stdout 2>terminal/remote.stderr
          status=$?
          ssh "${opts[@]}" "$remote" "cd "\$HOME/$op";mkdir -p export;for f in reservation.json pre-commit.json commit-attempt.json result.json receipt.json;do test -f "\$f"&&cp "\$f" export/;done;tar -czf - -C export ." >terminal/server.tgz
          set -e
          python3 - <<'PY'
          import tarfile,pathlib,json,hashlib
          with tarfile.open('terminal/server.tgz','r:gz') as t:
              for m in t.getmembers():
                  n=m.name.lstrip('./')
                  if m.isfile():pathlib.Path('terminal',n).write_bytes(t.extractfile(m).read())
          r=json.load(open('terminal/result.json'));q=json.load(open('terminal/receipt.json'))
          assert q['result_sha256']==hashlib.sha256(pathlib.Path('terminal/result.json').read_bytes()).hexdigest()
          assert r['state']=='committed_verified' and r['inserted']==300 and r['readback_verified'] is True
          print(json.dumps({'state':r['state'],'inserted':r['inserted'],'inserted_by_namespace':r['inserted_by_namespace'],'unique_targets':r['unique_targets']},sort_keys=True))
          PY
          test "$status" -eq 0
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with: {name: 'match-tv-tail-writer-w1-${{ github.run_id }}', path: terminal/, retention-days: 90, if-no-files-found: error}
