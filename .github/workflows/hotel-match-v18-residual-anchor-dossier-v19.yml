name: MATCH V18 residual anchor dossier V19
'on':
  pull_request:
    types: [opened]
    branches: [feature/anex-search-adapter-20260907]
    paths:
      - scripts/diagnostics/hotel_match_v18_residual_anchor_dossier_v19.php
      - tests/hotel_match_v18_residual_anchor_dossier_v19_test.php
      - .github/workflows/hotel-match-v18-residual-anchor-dossier-v19.yml
permissions: {contents: read, actions: read, issues: read}
concurrency: {group: hotel-match-v18-residual-anchor-dossier-1971-20260922-v19, cancel-in-progress: false}
env:
  OP: hotel-match-v18-residual-anchor-dossier-1971-20260922-v19
  BASE: fcb8c5b05e452e3c7d5df7fbe96be6017bc170f9
  CLAIM: '5783914636'
  INPUT_ARTIFACT: '10717889803'
jobs:
  dossier:
    if: github.repository == 'pyatkoff/poisk-turov-test' && github.actor == 'pyatkoff' && github.event.pull_request.head.ref == 'feat/match-v18-residual-anchor-dossier-1971-20260922-v19' && github.run_attempt == 1
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with: {ref: '${{ github.event.pull_request.head.sha }}', fetch-depth: 0, persist-credentials: false}
      - name: Validate source, input and reserve
        env: {GH_TOKEN: '${{ github.token }}', HEAD: '${{ github.event.pull_request.head.sha }}'}
        run: |
          set -euo pipefail;umask 077;mkdir -p input reservation terminal
          test "$(git rev-parse HEAD)" = "$HEAD"
          test "$(git merge-base HEAD "$BASE")" = "$BASE"
          expected=".github/workflows/hotel-match-v18-residual-anchor-dossier-v19.yml scripts/diagnostics/hotel_match_v18_residual_anchor_dossier_v19.php tests/hotel_match_v18_residual_anchor_dossier_v19_test.php "
          actual="$(git diff --name-only "$BASE..HEAD" | sort | tr '\n' ' ')"
          test "$actual" = "$expected"
          php -l scripts/diagnostics/hotel_match_v18_residual_anchor_dossier_v19.php
          php scripts/diagnostics/hotel_match_v18_residual_anchor_dossier_v19.php --self-test
          php tests/hotel_match_v18_residual_anchor_dossier_v19_test.php
          gh api "repos/$GITHUB_REPOSITORY/issues/comments/$CLAIM" >input/claim.json
          python3 - <<'PY'
          import json,os,pathlib,datetime
          c=json.load(open('input/claim.json'));b=c['body']
          assert c['user']['id']==226193297 and c['author_association']=='OWNER' and c['issue_url'].endswith('/issues/3419')
          for t in [os.environ['OP'],'77','64','consistent_multi_anchor','Provider/Tourvisor/SAMO/ANEX/Andromeda HTTP0','DB READ ONLY']:
              assert t in b,t
          r={'operation':os.environ['OP'],'state':'reserved_before_db_read','source_sha':os.environ['HEAD'],'provider_http_calls':0,'database_writes':0,'mapping_writes':0,'reserved_at_utc':datetime.datetime.now(datetime.timezone.utc).isoformat()}
          pathlib.Path('reservation/reservation.json').write_text(json.dumps(r,sort_keys=True)+'\n')
          PY
          gh api "repos/$GITHUB_REPOSITORY/actions/artifacts/$INPUT_ARTIFACT/zip" >input/v18.zip
          echo "cb2ac32967b623d53a0bd976c4eb1a03a236a302ab64d156e9e37d1d096f1406  input/v18.zip" | sha256sum -c -
          python3 - <<'PY'
          import zipfile,pathlib,hashlib
          with zipfile.ZipFile('input/v18.zip') as z:b=z.read('result.json')
          assert hashlib.sha256(b).hexdigest()=='d02f4e1e0664e0bc759681c70049957bf83e86e020a462e4c3fb25f7676ca475'
          pathlib.Path('input/v18-result.json').write_bytes(b)
          PY
          rm input/v18.zip input/claim.json
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with: {name: 'match-v19-reservation-${{ github.run_id }}', path: reservation/, retention-days: 90}
      - name: Require exact-head Security
        env: {GH_TOKEN: '${{ github.token }}', HEAD: '${{ github.event.pull_request.head.sha }}'}
        run: |
          python3 - <<'PY'
          import json,os,time,urllib.request
          u='https://api.github.com/repos/'+os.environ['GITHUB_REPOSITORY']+'/actions/runs?head_sha='+os.environ['HEAD']+'&per_page=100'
          for _ in range(80):
              req=urllib.request.Request(u,headers={'Authorization':'Bearer '+os.environ['GH_TOKEN'],'Accept':'application/vnd.github+json'})
              with urllib.request.urlopen(req,timeout=30) as f:runs=json.load(f)['workflow_runs']
              xs=[x for x in runs if x['name']=='Security guard' and x['head_sha']==os.environ['HEAD']]
              if xs:
                  x=max(xs,key=lambda y:y['id'])
                  if x['status']=='completed':
                      assert x['conclusion']=='success',x['conclusion'];break
              time.sleep(5)
          else:raise SystemExit('security_not_green')
          PY
      - name: Execute CURRENT anchor dossier
        env:
          KEY: '${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}'
          HOST: '${{ secrets.ANYTOOUR_DEPLOY_HOST }}'
          USER: '${{ secrets.ANYTOOUR_DEPLOY_USER }}'
          HEAD: '${{ github.event.pull_request.head.sha }}'
        run: |
          set -euo pipefail;umask 077;test -n "$KEY";test -n "$HOST";test -n "$USER"
          key="$RUNNER_TEMP/key";known="$RUNNER_TEMP/known";trap 'rm -f "$key" "$known"' EXIT
          if [[ "$KEY" == *"PRIVATE KEY"* ]];then printf '%s\n' "$KEY"|tr -d '\r'>"$key";else printf '%s' "$KEY"|base64 --decode>"$key";fi
          chmod 600 "$key";ssh-keygen -y -f "$key">/dev/null;ssh-keyscan -T15 -t ed25519 "$HOST">"$known" 2>/dev/null
          remote="$USER@$HOST";op=".anytoour-match/operations/$OP";opts=(-i "$key" -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=yes -o "UserKnownHostsFile=$known" -o GlobalKnownHostsFile=/dev/null -o ConnectTimeout=15)
          ssh "${opts[@]}" "$remote" "set -eu;umask 077;test ! -e \"\$HOME/$op\";mkdir -p \"\$HOME/$op/payload\""
          scp "${opts[@]}" reservation/reservation.json input/v18-result.json scripts/diagnostics/hotel_match_v18_residual_anchor_dossier_v19.php "$remote:$op/payload/"
          ssh "${opts[@]}" "$remote" "set -eu;mv \"\$HOME/$op/payload/reservation.json\" \"\$HOME/$op/reservation.json\";cd \"\$HOME/www/anytoour.ru\";ANYTOUR_ROOT=\"\$HOME/www/anytoour.ru\" MATCH_OPERATION_DIR=\"\$HOME/$op\" MATCH_V18_RESULT=\"\$HOME/$op/payload/v18-result.json\" MATCH_SOURCE_SHA='$HEAD' php \"\$HOME/$op/payload/hotel_match_v18_residual_anchor_dossier_v19.php\" --execute" >terminal/remote.stdout 2>terminal/remote.stderr
          ssh "${opts[@]}" "$remote" "cd \"\$HOME/$op\";mkdir export;for f in reservation.json result.json receipt.json;do cp \"\$f\" export/;done;tar -czf - -C export ." >terminal/server.tgz
          python3 - <<'PY'
          import tarfile,pathlib,json,hashlib
          with tarfile.open('terminal/server.tgz','r:gz') as t:
              for m in t.getmembers():
                  n=m.name.lstrip('./')
                  if m.isfile():pathlib.Path('terminal',n).write_bytes(t.extractfile(m).read())
          r=json.load(open('terminal/result.json'));q=json.load(open('terminal/receipt.json'))
          assert r['state']==q['state']=='completed_read_only_anchor_dossier'
          assert q['result_sha256']==hashlib.sha256(pathlib.Path('terminal/result.json').read_bytes()).hexdigest()
          assert r['input_rows']==77 and r['unique_targets']==64
          assert r['provider_http_calls']==r['database_writes']==r['mapping_writes']==0
          print(json.dumps({'state':r['state'],'verdict_counts':r['verdict_counts'],'unique_targets_by_verdict':r['unique_targets_by_verdict'],'namespace_verdict_counts':r['namespace_verdict_counts'],'anchor_count_verdict_counts':r['anchor_count_verdict_counts']},ensure_ascii=False,sort_keys=True))
          PY
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with: {name: 'match-v19-terminal-${{ github.run_id }}', path: terminal/, retention-days: 90, if-no-files-found: error}
