name: Search3 Andromeda selected quote
on:
  pull_request:
    paths:
      - 'v2/andromeda-provider-v1.js'
      - 'tests/search3-andromeda-selected-quote.cjs'
      - '.github/workflows/search3-andromeda-selected-quote-check.yml'
permissions:
  contents: read
jobs:
  quote-ui:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - name: Syntax and selected quote flow
        run: |
          node --check v2/andromeda-provider-v1.js
          node tests/search3-andromeda-selected-quote.cjs
      - name: Enforce no booking or lead handoff
        run: |
          ! grep -RE "data-direct-tour|leadApi|action[[:space:]]*:[[:space:]]*['\"]bron['\"]|booking_enabled[[:space:]]*[:=][[:space:]]*true" v2/andromeda-provider-v1.js
