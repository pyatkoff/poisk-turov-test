name: MATCH current ANEX detail fuzzy review v17
run-name: MATCH current ANEX detail fuzzy review #1971 v17
on:
  push:
    branches: ['ops/match-anex-current-detail-fuzzy-1971-20260913-v17']
    paths:
      - '.github/match-anex-current-detail-fuzzy-1971-v17.arm'
permissions:
  contents: read
  actions: read
concurrency:
  group: match-anex-current-detail-fuzzy-1971-v17
  cancel-in-progress: false
env:
  OPERATION_ID: hotel-match-anex-current-detail-fuzzy-1971-20260913-v17
  PINNED_MATCH_SOURCE: c5d798cecdf2eeab96a3c83f16163873b378d5a8
  EXPECTED_MANIFEST_SHA256: 5f10e02fa811c2ba7a4a9b4bf251f9e6f0d5a62f0d05ca006e89bc7f7f29caa4
  SOURCE_ARTIFACT_ID: '10311246616'
  SOURCE_ARTIFACT_SHA256: d29728b346ed17d38a1c8d29930beae151204f7480711a32e3f80bec04b9e802
  SOURCE_RESULT_SHA256: 52818f55e75a599802d058408c9e43267d1c1928eaa1b04a36f398dcf9f8c45d
  SOURCE_PREFLIGHT_SHA256: f2686a8099e216c2dad2d8b7cd19e77ffce605b132fe3dc759e85a1a9c163d08
  SOURCE_TARGET_SHA256: 9253d53504bfd8ed931b82016ae7c00f51c78d1abe38b00e2e54769065e9ca03
jobs:
  review:
    if: github.actor == 'pyatkoff' && github.run_attempt == 1
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Validate manifest, restore sealed v16 evidence and pin review sources
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(cat .github/match-anex-current-detail-fuzzy-1971-v17.arm)" = "$OPERATION_ID"
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test "$(sha256sum .github/match-anex-current-detail-fuzzy-1971-v17.manifest.json | awk '{print $1}')" = "$EXPECTED_MANIFEST_SHA256"
          mkdir -m 700 "$RUNNER_TEMP/v17-input"
          curl --fail --silent --show-error --location \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/artifacts/$SOURCE_ARTIFACT_ID/zip" \
            -o "$RUNNER_TEMP/v16.zip"
          test "$(sha256sum "$RUNNER_TEMP/v16.zip" | awk '{print $1}')" = "$SOURCE_ARTIFACT_SHA256"
          unzip -q "$RUNNER_TEMP/v16.zip" -d "$RUNNER_TEMP/v17-input"
          test "$(sha256sum "$RUNNER_TEMP/v17-input/result.json" | awk '{print $1}')" = "$SOURCE_RESULT_SHA256"
          test "$(sha256sum "$RUNNER_TEMP/v17-input/preflight.json" | awk '{print $1}')" = "$SOURCE_PREFLIGHT_SHA256"
          python3 - <<'PY'
          import json,os
          from pathlib import Path
          p=Path(os.environ['RUNNER_TEMP'])/'v17-input'
          pre=json.loads((p/'preflight.json').read_text())
          res=json.loads((p/'result.json').read_text())
          assert pre['operation_id']=='hotel-match-anex-current-details-1971-20260913-v16'
          assert pre['target_sha256']==os.environ['SOURCE_TARGET_SHA256'] and len(pre['target_ids'])==67 and len(pre['target_context'])==67
          assert res['status']=='completed' and res['operation_id']=='hotel-match-anex-current-details-1971-20260913-v16'
          assert res['database_writes']==res['mapping_writes']==res['bookings']==0 and res['historical_operations_replayed'] is False
          d=res['details']; assert d['target_sha256']==os.environ['SOURCE_TARGET_SHA256'] and d['requested']==67 and d['ok']==56 and d['empty']==11 and d['errors']==0 and len(d['rows'])==67
          assert sum(1 for x in d['rows'] if x['status']=='ok')==56
          PY
          git fetch --no-tags --depth=1 "https://github.com/$GITHUB_REPOSITORY.git" "$PINNED_MATCH_SOURCE"
          git show "$PINNED_MATCH_SOURCE:scripts/diagnostics/hotel_match_anex_sold_details_strong_review.php" > "$RUNNER_TEMP/strong.php"
          git show "$PINNED_MATCH_SOURCE:scripts/diagnostics/hotel_match_anex_direct_details_coordinate_review.php" > "$RUNNER_TEMP/coord.php"
          python3 - <<'PY'
          import os
          from pathlib import Path
          root=Path(os.environ['RUNNER_TEMP'])
          strong=root/'strong.php'; s=strong.read_text(encoding='utf-8')
          patches={
            "const HMASR_OPERATION = 'hotel-match-anex-sold-details-strong-review-1971-20260911-v1';": "const HMASR_OPERATION = 'hotel-match-anex-current-detail-fuzzy-1971-20260913-v17';",
            "const HMASR_SOURCE_OPERATION = 'hotel-match-anex-sold-details-review-1971-20260911-v2';": "const HMASR_SOURCE_OPERATION = 'hotel-match-anex-current-details-1971-20260913-v16';",
            "const HMASR_SOURCE_RESULT_SHA256 = '86f3cbe232a262fa6c6bcd5d87a56af8167ce52f1dc7b589c7d4605fd57cc7fa';": "const HMASR_SOURCE_RESULT_SHA256 = '52818f55e75a599802d058408c9e43267d1c1928eaa1b04a36f398dcf9f8c45d';",
          }
          for old,new in patches.items():
              if s.count(old)!=1: raise SystemExit('strong source drift')
              s=s.replace(old,new,1)
          strong.write_text(s,encoding='utf-8')
          coord=root/'coord.php'; c=coord.read_text(encoding='utf-8')
          replacements={
            "const HMADDCR_OPERATION = 'hotel-match-anex-direct-details-coordinate-review-1971-20260911-v1';": "const HMADDCR_OPERATION = 'hotel-match-anex-current-detail-fuzzy-1971-20260913-v17';",
            "const HMADDCR_DIRECT_RESULT_SHA256 = '86f3cbe232a262fa6c6bcd5d87a56af8167ce52f1dc7b589c7d4605fd57cc7fa';": "const HMADDCR_DIRECT_RESULT_SHA256 = '52818f55e75a599802d058408c9e43267d1c1928eaa1b04a36f398dcf9f8c45d';",
            "const HMADDCR_DIRECT_PLAN_SHA256 = '18ebd5b63c9a5043a9fd573390a0b06331cd099f1956e3d9eea7eb1c2bd9bad0';": "const HMADDCR_DIRECT_PLAN_SHA256 = 'f2686a8099e216c2dad2d8b7cd19e77ffce605b132fe3dc759e85a1a9c163d08';",
            "if(count($sources)!==208)": "if(count($sources)!==56)",
          }
          for old,new in replacements.items():
              if c.count(old)!=1: raise SystemExit('coordinate source drift: '+old)
              c=c.replace(old,new,1)
          coord.write_text(c,encoding='utf-8')
          PY
          php -l "$RUNNER_TEMP/strong.php"
          php -l "$RUNNER_TEMP/coord.php"
          php -l scripts/diagnostics/hotel_full_catalog_reconcile.php
      - name: Reserve immutable read-only operation before CURRENT DB access
        shell: bash
        run: |
          set -euo pipefail
          STATE="$RUNNER_TEMP/$OPERATION_ID"; mkdir -m 700 "$STATE"; export STATE
          python3 - <<'PY'
          import hashlib,json,os
          from pathlib import Path
          state=Path(os.environ['STATE'])
          x={'operation_id':os.environ['OPERATION_ID'],'state':'reserved_before_current_db_read','run_id':int(os.environ['GITHUB_RUN_ID']),'run_attempt':1,'source_sha':os.environ['GITHUB_SHA'],'source_artifact_id':int(os.environ['SOURCE_ARTIFACT_ID']),'source_artifact_sha256':os.environ['SOURCE_ARTIFACT_SHA256'],'source_result_sha256':os.environ['SOURCE_RESULT_SHA256'],'source_preflight_sha256':os.environ['SOURCE_PREFLIGHT_SHA256'],'source_target_sha256':os.environ['SOURCE_TARGET_SHA256'],'source_detail_ok':56,'database_writes':0,'mapping_writes':0,'supplier_calls':0,'tourvisor_calls':0,'bookings':0,'lead_calls':0,'no_replay':True}
          raw=(json.dumps(x,sort_keys=True)+'\n').encode(); (state/'reservation.json').write_bytes(raw)
          if (state/'reservation.json').read_bytes()!=raw: raise SystemExit('reservation readback failed')
          PY
      - name: Preserve reservation before CURRENT DB read
        uses: actions/upload-artifact@v4
        with:
          name: match-anex-current-detail-fuzzy-1971-20260913-v17-reservation
          path: ${{ runner.temp }}/hotel-match-anex-current-detail-fuzzy-1971-20260913-v17/reservation.json
          if-no-files-found: error
          retention-days: 90
      - name: Run both conservative CURRENT review lanes and intersect safety guards
        env:
          ANYTOOUR_DEPLOY_SSH_KEY: ${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}
          ANYTOOUR_DEPLOY_HOST: ${{ secrets.ANYTOOUR_DEPLOY_HOST }}
          ANYTOOUR_DEPLOY_USER: ${{ secrets.ANYTOOUR_DEPLOY_USER }}
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json,os,shlex,subprocess,tempfile,hashlib
          from pathlib import Path
          root=Path(os.environ['RUNNER_TEMP']); state=root/os.environ['OPERATION_ID']
          pre=json.loads((root/'v17-input/preflight.json').read_text()); source=json.loads((root/'v17-input/result.json').read_text())
          source['target_sha256']=source['details']['target_sha256']
          def body(path):
              s=Path(path).read_text(encoding='utf-8')
              if s.startswith('<?php'): s=s[5:]
              return s.replace('declare(strict_types=1);','',1)
          helper=body('scripts/diagnostics/hotel_full_catalog_reconcile.php')
          strong=body(root/'strong.php').replace("if (!defined('FC_LIBRARY_ONLY')) define('FC_LIBRARY_ONLY', true);\nrequire_once __DIR__ . '/hotel_full_catalog_reconcile.php';\n",'',1)
          coord=body(root/'coord.php').replace("if (!defined('FC_LIBRARY_ONLY')) define('FC_LIBRARY_ONLY', true);\nrequire_once __DIR__ . '/hotel_match_anex_sold_details_strong_review.php';\n",'',1)
          source_raw=json.dumps(source,ensure_ascii=False,separators=(',',':')); pre_raw=json.dumps(pre,ensure_ascii=False,separators=(',',':'))
          wrapper=r'''
          try{
            $root=realpath(getcwd());if(!$root||basename($root)!=='anytoour.ru')throw new RuntimeException('V17_ROOT');$dbHelper=realpath($root.(is_file($root.'/data/db-v1.php')?'/data/db-v1.php':'/v2/data/db-v1.php'));if(!$dbHelper||strpos($dbHelper,$root.DIRECTORY_SEPARATOR)!==0)throw new RuntimeException('V17_DB');require_once $dbHelper;$db=v2_data_db();
            $source=json_decode('__SOURCE__',true,64,JSON_THROW_ON_ERROR);$plan=json_decode('__PLAN__',true,64,JSON_THROW_ON_ERROR);
            $strong=hmasr_review($db,$source,$plan,HMASR_OPERATION);$coord=hmaddcr_review($db,$source,$plan,HMADDCR_OPERATION);
            $sp=[];foreach($strong['prepared']??[] as $r)$sp[(int)$r['anex_hotel_id']]=$r;$cp=[];foreach($coord['prepared']??[] as $r)$cp[(int)$r['anex_hotel_id']]=$r;
            $db->setAttribute(PDO::ATTR_ERRMODE,PDO::ERRMODE_EXCEPTION);$db->exec('SET TRANSACTION ISOLATION LEVEL REPEATABLE READ');$db->exec('START TRANSACTION READ ONLY');
            try{$protected=[];foreach($db->query('SELECT anex_hotel_id FROM anex_hotel_search_mappings WHERE enabled=1 UNION SELECT anex_hotel_id FROM anex_hotel_decisions')->fetchAll(PDO::FETCH_COLUMN) as $id)$protected[(int)$id]=true;$occupied=[];foreach($db->query("SELECT anex_hotel_id,catalog_hotel_id FROM anex_hotel_search_mappings WHERE enabled=1 UNION ALL SELECT anex_hotel_id,catalog_hotel_id FROM anex_hotel_decisions WHERE decision_status='accepted' AND catalog_hotel_id IS NOT NULL")->fetchAll(PDO::FETCH_ASSOC) as $r)$occupied[(int)$r['catalog_hotel_id']][(int)$r['anex_hotel_id']]=true;$excluded=[];foreach($db->query('SELECT anex_hotel_id,catalog_hotel_id FROM anex_review_pair_exclusions')->fetchAll(PDO::FETCH_ASSOC) as $r)$excluded[(int)$r['anex_hotel_id']][(int)$r['catalog_hotel_id']]=true;$db->commit();}catch(Throwable $e){if($db->inTransaction())$db->rollBack();throw $e;}
            $ids=array_unique(array_merge(array_keys($sp),array_keys($cp)));sort($ids,SORT_NUMERIC);$provisional=[];$laneConflicts=[];$withheld=[];
            foreach($ids as $aid){$s=$sp[$aid]??null;$c=$cp[$aid]??null;if($s&&$c&&(int)$s['target_local_hotel_id']!==(int)$c['target_local_hotel_id']){$laneConflicts[]=['anex_hotel_id'=>$aid,'strong_target'=>(int)$s['target_local_hotel_id'],'coordinate_target'=>(int)$c['target_local_hotel_id']];continue;}$row=$s??$c;$target=(int)$row['target_local_hotel_id'];if(isset($protected[$aid])){$withheld[]=['anex_hotel_id'=>$aid,'target_local_hotel_id'=>$target,'reason'=>'became_protected'];continue;}if(isset($excluded[$aid][$target])){$withheld[]=['anex_hotel_id'=>$aid,'target_local_hotel_id'=>$target,'reason'=>'pair_excluded'];continue;}$other=array_values(array_filter(array_keys($occupied[$target]??[]),static fn($x)=>(int)$x!==$aid));if($other){$withheld[]=['anex_hotel_id'=>$aid,'target_local_hotel_id'=>$target,'reason'=>'target_occupied','other_anex_ids'=>$other];continue;}$provisional[$aid]=['anex_hotel_id'=>$aid,'target_local_hotel_id'=>$target,'live'=>(bool)($row['live']??false),'search_count'=>(int)($row['search_count']??0),'source_name'=>$row['source_name']??null,'target_name'=>$row['target_name']??null,'distance_m'=>$row['distance_m']??null,'place_match'=>(bool)($row['place_match']??false),'andromeda_bridge'=>(bool)($row['andromeda_bridge']??false),'strong_rule'=>$s['rule']??null,'coordinate_rule'=>$c['rule']??null,'strong_score'=>$s['score']??null,'strong_margin'=>$s['margin']??null,'coordinate_score'=>$c['score']??null,'lanes'=>array_values(array_filter([$s?'strong_name_margin':null,$c?'coordinate_cluster':null]))];}
            $byTarget=[];foreach($provisional as $aid=>$r)$byTarget[(int)$r['target_local_hotel_id']][]=(int)$aid;$safe=[];foreach($provisional as $aid=>$r){$claims=$byTarget[(int)$r['target_local_hotel_id']]??[];if(count($claims)>1){$withheld[]=['anex_hotel_id'=>(int)$aid,'target_local_hotel_id'=>(int)$r['target_local_hotel_id'],'reason'=>'batch_target_collision','claiming_anex_ids'=>$claims];continue;}$safe[]=$r;}
            usort($safe,static fn($a,$b)=>(int)$b['live']<=>(int)$a['live'] ?: $b['search_count']<=>$a['search_count'] ?: $a['anex_hotel_id']<=>$b['anex_hotel_id']);$coverage=fc_coverage($db);
            $out=['status'=>'completed','operation_id'=>HMASR_OPERATION,'database_writes'=>0,'mapping_writes'=>0,'supplier_calls'=>0,'tourvisor_calls'=>0,'historical_operations_replayed'=>false,'source_operation'=>'hotel-match-anex-current-details-1971-20260913-v16','source_result_sha256'=>'52818f55e75a599802d058408c9e43267d1c1928eaa1b04a36f398dcf9f8c45d','source_detail_ok'=>56,'strong_stats'=>$strong['stats']??[],'coordinate_stats'=>$coord['stats']??[],'strong_prepared'=>count($sp),'coordinate_prepared'=>count($cp),'lane_conflicts'=>$laneConflicts,'withheld'=>$withheld,'safe_prepared'=>$safe,'safe_count'=>count($safe),'coverage'=>$coverage];echo json_encode($out,JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES),"\n";
          }catch(Throwable $e){$m=$e->getMessage();echo json_encode(['status'=>'failed','operation_id'=>HMASR_OPERATION,'reason'=>(is_string($m)&&preg_match('/\A[A-Z0-9_]{1,100}\z/D',$m)?$m:'V17_REMOTE_ERROR'),'database_writes'=>0,'mapping_writes'=>0,'supplier_calls'=>0,'tourvisor_calls'=>0],JSON_UNESCAPED_SLASHES),"\n";exit(2);}
          '''.replace('__SOURCE__',source_raw.replace('\\','\\\\').replace("'","\\'")).replace('__PLAN__',pre_raw.replace('\\','\\\\').replace("'","\\'"))
          code="define('FC_LIBRARY_ONLY',true);\n"+helper+'\n'+strong+'\n'+coord+'\n'+wrapper
          req=('ANYTOOUR_DEPLOY_SSH_KEY','ANYTOOUR_DEPLOY_HOST','ANYTOOUR_DEPLOY_USER');host=os.environ[req[1]].strip();user=os.environ[req[2]].strip();keytext=os.environ[req[0]].strip()
          with tempfile.TemporaryDirectory(prefix='match-anex-v17-',dir=os.environ.get('RUNNER_TEMP')) as tmp:
              key=Path(tmp)/'key';key.write_text(keytext.rstrip()+'\n');key.chmod(0o600);known=Path(tmp)/'known_hosts';cmd=['ssh','-vv','-T','-i',str(key),'-o','IdentitiesOnly=yes','-o','BatchMode=yes','-o','StrictHostKeyChecking=accept-new','-o','UserKnownHostsFile='+str(known),'-o','ConnectTimeout=15','-o','ServerAliveInterval=15','-o','ServerAliveCountMax=3','-l',user,host,'cd "$HOME/www/anytoour.ru" && php -d display_errors=0 -d log_errors=0 -r '+shlex.quote(code)];clean={k:v for k,v in os.environ.items() if k not in req and not k.startswith('ANEX_') and not k.startswith('ANDROMEDA_')};p=subprocess.run(cmd,text=True,capture_output=True,timeout=480,env=clean)
          stderr=str(p.stderr);prog={'authenticated':'Authenticated to ' in stderr or 'Authentication succeeded' in stderr,'command_sent':'Sending command:' in stderr,'remote_exit_seen':'Exit status ' in stderr}
          try:r=json.loads(p.stdout)
          except Exception:r={'status':'unknown','operation_id':os.environ['OPERATION_ID'],'reason':'V17_OUTPUT','database_writes':0,'mapping_writes':0,'supplier_calls':0,'tourvisor_calls':0}
          result=(json.dumps(r,ensure_ascii=False,sort_keys=True,separators=(',',':'))+'\n').encode();(state/'result.json').write_bytes(result);reservation=(state/'reservation.json').read_bytes();receipt={'operation_id':os.environ['OPERATION_ID'],'state':'completed' if p.returncode==0 and r.get('status')=='completed' else ('unknown_no_replay' if prog['command_sent'] else 'failed_precommand'),'run_id':int(os.environ['GITHUB_RUN_ID']),'source_sha':os.environ['GITHUB_SHA'],'result_sha256':hashlib.sha256(result).hexdigest(),'reservation_sha256':hashlib.sha256(reservation).hexdigest(),'exit_code':p.returncode,'ssh':prog,'database_writes':r.get('database_writes',0),'mapping_writes':r.get('mapping_writes',0),'supplier_calls':r.get('supplier_calls',0),'tourvisor_calls':r.get('tourvisor_calls',0),'no_replay':True};(state/'receipt.json').write_text(json.dumps(receipt,sort_keys=True)+'\n')
          print(json.dumps({'state':receipt['state'],'strong_prepared':r.get('strong_prepared'),'coordinate_prepared':r.get('coordinate_prepared'),'safe_count':r.get('safe_count'),'lane_conflicts':len(r.get('lane_conflicts') or []),'withheld':len(r.get('withheld') or []),'coverage':r.get('coverage')},sort_keys=True))
          if receipt['state']!='completed' or r.get('operation_id')!=os.environ['OPERATION_ID'] or r.get('database_writes')!=0 or r.get('mapping_writes')!=0 or r.get('supplier_calls')!=0 or r.get('tourvisor_calls')!=0 or r.get('historical_operations_replayed') is not False: raise SystemExit(2)
          PY
      - name: Preserve sealed mass review and receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: match-anex-current-detail-fuzzy-1971-20260913-v17-result
          path: |
            ${{ runner.temp }}/hotel-match-anex-current-detail-fuzzy-1971-20260913-v17/reservation.json
            ${{ runner.temp }}/hotel-match-anex-current-detail-fuzzy-1971-20260913-v17/result.json
            ${{ runner.temp }}/hotel-match-anex-current-detail-fuzzy-1971-20260913-v17/receipt.json
          if-no-files-found: warn
          retention-days: 90
