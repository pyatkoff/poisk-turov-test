name: MATCH current ANEX direct details v16
run-name: MATCH current ANEX direct details #1971 v16
on:
  push:
    branches: ['ops/match-anex-current-details-1971-20260913-v16']
    paths:
      - '.github/match-anex-current-details-1971-v16.arm'
permissions:
  contents: read
  actions: read
concurrency:
  group: match-anex-current-details-1971-v16
  cancel-in-progress: false
env:
  OPERATION_ID: hotel-match-anex-current-details-1971-20260913-v16
  PINNED_MATCH_SOURCE: c5d798cecdf2eeab96a3c83f16163873b378d5a8
  OLD_DETAILS_OPERATION: hotel-match-anex-sold-details-review-1971-20260911-v1
  EXPECTED_MANIFEST_SHA256: 9aee226b562df16d163ab184d1d4e1cb448cb56caaf38a6311598b6a044f2ee4
jobs:
  evidence:
    if: github.actor == 'pyatkoff' && github.run_attempt == 1
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Validate immutable input and pinned read-only libraries
        shell: bash
        run: |
          set -euo pipefail
          test "$(cat .github/match-anex-current-details-1971-v16.arm)" = "$OPERATION_ID"
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test "$(sha256sum .github/match-anex-current-details-1971-v16.manifest.json | awk '{print $1}')" = "$EXPECTED_MANIFEST_SHA256"
          php -l app/integrations/anex-client.php
          php -l scripts/diagnostics/hotel_full_catalog_reconcile.php
          git fetch --no-tags --depth=1 "https://github.com/$GITHUB_REPOSITORY.git" "$PINNED_MATCH_SOURCE"
          git show "$PINNED_MATCH_SOURCE:scripts/diagnostics/hotel_match_anex_sold_details_review.php" > "$RUNNER_TEMP/hmadr.php"
          python3 - <<'PY'
          import json,os
          from pathlib import Path
          m=json.loads(Path('.github/match-anex-current-details-1971-v16.manifest.json').read_text())
          assert m['schema']=='hotel-match-anex-current-sellability-manifest/1'
          assert m['source_operation_id']=='hotel-match-anex-current-sellability-1971-20260913-v15'
          assert m['source_run_id']==34736543890 and m['source_artifact_id']==10311192781
          assert m['source_result_sha256']=='12668b5efb717cef96edabca21f3e5f6a0285ae1959bd89732aaabea073bcf80'
          assert m['sold_on_probe']==67 and len(m['sold'])==67 and m['database_writes']==m['mapping_writes']==m['bookings']==0
          ids=[int(x['anex_hotel_id']) for x in m['sold']]
          assert len(ids)==len(set(ids))==67 and ids==sorted(ids)
          p=Path(os.environ['RUNNER_TEMP'])/'hmadr.php'; s=p.read_text(encoding='utf-8')
          old=os.environ['OLD_DETAILS_OPERATION']; new=os.environ['OPERATION_ID']
          if s.count(old)!=1: raise SystemExit('details operation drift')
          p.write_text(s.replace(old,new,1),encoding='utf-8')
          PY
          php -l "$RUNNER_TEMP/hmadr.php"
      - name: Reserve immutable operation before DB or supplier access
        shell: bash
        run: |
          set -euo pipefail
          STATE="$RUNNER_TEMP/$OPERATION_ID"; mkdir -m 700 "$STATE"; export STATE
          python3 - <<'PY'
          import hashlib,json,os
          from pathlib import Path
          state=Path(os.environ['STATE']); manifest=Path('.github/match-anex-current-details-1971-v16.manifest.json').read_bytes()
          x={'operation_id':os.environ['OPERATION_ID'],'state':'reserved_before_db_or_supplier_access','run_id':int(os.environ['GITHUB_RUN_ID']),'run_attempt':1,'source_sha':os.environ['GITHUB_SHA'],'input_manifest_sha256':hashlib.sha256(manifest).hexdigest(),'source_operation':'hotel-match-anex-current-sellability-1971-20260913-v15','source_result_sha256':'12668b5efb717cef96edabca21f3e5f6a0285ae1959bd89732aaabea073bcf80','source_sold':67,'database_writes':0,'mapping_writes':0,'bookings':0,'lead_calls':0,'historical_operations_replayed':False,'no_replay':True}
          raw=(json.dumps(x,sort_keys=True)+'\n').encode();(state/'reservation.json').write_bytes(raw)
          if (state/'reservation.json').read_bytes()!=raw: raise SystemExit('reservation readback failed')
          PY
      - name: Preserve reservation before access
        uses: actions/upload-artifact@v4
        with:
          name: match-anex-current-details-1971-20260913-v16-reservation
          path: ${{ runner.temp }}/hotel-match-anex-current-details-1971-20260913-v16/reservation.json
          if-no-files-found: error
          retention-days: 90
      - name: CURRENT DB preflight and target freeze
        env:
          ANYTOOUR_DEPLOY_SSH_KEY: ${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}
          ANYTOOUR_DEPLOY_HOST: ${{ secrets.ANYTOOUR_DEPLOY_HOST }}
          ANYTOOUR_DEPLOY_USER: ${{ secrets.ANYTOOUR_DEPLOY_USER }}
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json,os,shlex,subprocess,tempfile,hashlib
          from pathlib import Path
          state=Path(os.environ['RUNNER_TEMP'])/os.environ['OPERATION_ID']; manifest=json.loads(Path('.github/match-anex-current-details-1971-v16.manifest.json').read_text())
          def body(path):
              s=Path(path).read_text(encoding='utf-8')
              if s.startswith('<?php'): s=s[5:]
              return s.replace('declare(strict_types=1);','',1)
          helper=body('scripts/diagnostics/hotel_full_catalog_reconcile.php')
          details=body(Path(os.environ['RUNNER_TEMP'])/'hmadr.php').replace("if (!defined('FC_LIBRARY_ONLY')) define('FC_LIBRARY_ONLY', true);\nrequire_once __DIR__ . '/hotel_full_catalog_reconcile.php';\n",'',1)
          manifest_raw=json.dumps(manifest,ensure_ascii=False,separators=(',',':'))
          wrapper=r'''
          function v16_plan(PDO $db,array $m): array {
            if(($m['schema']??'')!=='hotel-match-anex-current-sellability-manifest/1'||($m['source_operation_id']??'')!=='hotel-match-anex-current-sellability-1971-20260913-v15'||($m['source_result_sha256']??'')!=='12668b5efb717cef96edabca21f3e5f6a0285ae1959bd89732aaabea073bcf80'||!is_array($m['sold']??null)||count($m['sold'])!==67)throw new RuntimeException('V16_MANIFEST');
            $db->setAttribute(PDO::ATTR_ERRMODE,PDO::ERRMODE_EXCEPTION);$db->exec('SET TRANSACTION ISOLATION LEVEL REPEATABLE READ');$db->exec('START TRANSACTION READ ONLY');
            try{$manual=array_fill_keys(array_map('intval',$db->query('SELECT anex_hotel_id FROM anex_hotel_decisions')->fetchAll(PDO::FETCH_COLUMN)),true);$mapped=array_fill_keys(array_map('intval',$db->query('SELECT anex_hotel_id FROM anex_hotel_search_mappings WHERE enabled=1')->fetchAll(PDO::FETCH_COLUMN)),true);$staging=[];foreach($db->query('SELECT anex_hotel_id,api_country FROM anex_hotels')->fetchAll(PDO::FETCH_ASSOC) as $r)$staging[(int)$r['anex_hotel_id']]=$r;$observed=[];foreach($db->query('SELECT anex_hotel_id,country_id,search_count,last_seen_utc FROM anex_search_hotel_observations ORDER BY search_count DESC,last_seen_utc DESC,anex_hotel_id')->fetchAll(PDO::FETCH_ASSOC) as $r){$id=(int)$r['anex_hotel_id'];if(!isset($observed[$id]))$observed[$id]=$r;}$ids=[];$ctx=[];$stats=['source_sold'=>67,'already_mapped'=>0,'manual'=>0,'country_other'=>0,'current_unresolved_sold'=>0,'live_unresolved_sold'=>0];$seen=[];
              foreach($m['sold'] as $sold){$id=(int)($sold['anex_hotel_id']??0);if($id<=0||isset($seen[$id]))throw new RuntimeException('V16_MANIFEST_ID');$seen[$id]=true;if(isset($manual[$id])){$stats['manual']++;continue;}if(isset($mapped[$id])){$stats['already_mapped']++;continue;}$o=$observed[$id]??null;$s=$staging[$id]??null;$country=(int)($o['country_id']??0);if(!isset(HMADR_CORE8[$country]))$country=(int)(fc_country($s['api_country']??'')??0);if(!isset(HMADR_CORE8[$country])){$stats['country_other']++;continue;}$live=(int)($o['search_count']??0)>0;$stats['current_unresolved_sold']++;if($live)$stats['live_unresolved_sold']++;$ids[]=$id;$ctx[]=['anex_hotel_id'=>$id,'country_id'=>$country,'live'=>$live,'search_count'=>(int)($o['search_count']??0),'last_seen_utc'=>$o['last_seen_utc']??null,'probe_dates'=>$sold['probe_dates']??[]];}
              sort($ids,SORT_NUMERIC);$hash=hash('sha256',json_encode($ids,JSON_UNESCAPED_SLASHES));$db->commit();return ['status'=>'ready','operation_id'=>HMADR_OPERATION,'database_writes'=>0,'mapping_writes'=>0,'supplier_calls'=>0,'stats'=>$stats,'target_ids'=>$ids,'target_sha256'=>$hash,'target_context'=>$ctx];
            }catch(Throwable $e){if($db->inTransaction())$db->rollBack();throw $e;}
          }
          try{$root=realpath(getcwd());if(!$root||basename($root)!=='anytoour.ru')throw new RuntimeException('V16_ROOT');$dbHelper=realpath($root.(is_file($root.'/data/db-v1.php')?'/data/db-v1.php':'/v2/data/db-v1.php'));if(!$dbHelper||strpos($dbHelper,$root.DIRECTORY_SEPARATOR)!==0)throw new RuntimeException('V16_DB');require_once $dbHelper;$m=json_decode('__MANIFEST__',true,64,JSON_THROW_ON_ERROR);$r=v16_plan(v2_data_db(),$m);echo json_encode($r,JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES),"\n";}catch(Throwable $e){echo json_encode(['status'=>'failed','operation_id'=>HMADR_OPERATION,'reason'=>'V16_PREFLIGHT','database_writes'=>0,'mapping_writes'=>0,'supplier_calls'=>0]),"\n";exit(2);}
          '''.replace('__MANIFEST__',manifest_raw.replace('\\','\\\\').replace("'","\\'"))
          code="define('FC_LIBRARY_ONLY',true);\n"+helper+'\n'+details+'\n'+wrapper
          req=('ANYTOOUR_DEPLOY_SSH_KEY','ANYTOOUR_DEPLOY_HOST','ANYTOOUR_DEPLOY_USER');host=os.environ[req[1]].strip();user=os.environ[req[2]].strip();keytext=os.environ[req[0]].strip()
          with tempfile.TemporaryDirectory(prefix='match-anex-v16-pre-',dir=os.environ.get('RUNNER_TEMP')) as tmp:
              key=Path(tmp)/'key';key.write_text(keytext.rstrip()+'\n');key.chmod(0o600);known=Path(tmp)/'known_hosts';cmd=['ssh','-T','-i',str(key),'-o','IdentitiesOnly=yes','-o','BatchMode=yes','-o','StrictHostKeyChecking=accept-new','-o','UserKnownHostsFile='+str(known),'-o','ConnectTimeout=15','-l',user,host,'cd "$HOME/www/anytoour.ru" && php -d display_errors=0 -d log_errors=0 -r '+shlex.quote(code)];clean={k:v for k,v in os.environ.items() if k not in req and not k.startswith('ANEX_') and not k.startswith('ANDROMEDA_')};p=subprocess.run(cmd,text=True,capture_output=True,timeout=120,env=clean)
          try:r=json.loads(p.stdout)
          except Exception:r={'status':'failed','operation_id':os.environ['OPERATION_ID'],'reason':'V16_PREFLIGHT_OUTPUT','database_writes':0,'mapping_writes':0,'supplier_calls':0}
          raw=(json.dumps(r,sort_keys=True)+'\n').encode();(state/'preflight.json').write_bytes(raw)
          if p.returncode!=0 or r.get('status')!='ready' or r.get('operation_id')!=os.environ['OPERATION_ID'] or r.get('database_writes')!=0 or r.get('mapping_writes')!=0 or r.get('supplier_calls')!=0 or not isinstance(r.get('target_ids'),list) or len(r['target_ids'])>67: raise SystemExit(2)
          print(json.dumps({'targets':len(r['target_ids']),'stats':r['stats'],'target_sha256':r['target_sha256']},sort_keys=True))
          PY
      - name: Run direct details once and reconcile CURRENT evidence
        env:
          ANYTOOUR_DEPLOY_SSH_KEY: ${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}
          ANYTOOUR_DEPLOY_HOST: ${{ secrets.ANYTOOUR_DEPLOY_HOST }}
          ANYTOOUR_DEPLOY_USER: ${{ secrets.ANYTOOUR_DEPLOY_USER }}
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json,os,shlex,subprocess,tempfile,hashlib
          from pathlib import Path
          state=Path(os.environ['RUNNER_TEMP'])/os.environ['OPERATION_ID'];plan=json.loads((state/'preflight.json').read_text())
          def body(path):
              s=Path(path).read_text(encoding='utf-8')
              if s.startswith('<?php'): s=s[5:]
              return s.replace('declare(strict_types=1);','',1)
          helper=body('scripts/diagnostics/hotel_full_catalog_reconcile.php');client=body('app/integrations/anex-client.php');details=body(Path(os.environ['RUNNER_TEMP'])/'hmadr.php').replace("if (!defined('FC_LIBRARY_ONLY')) define('FC_LIBRARY_ONLY', true);\nrequire_once __DIR__ . '/hotel_full_catalog_reconcile.php';\n",'',1)
          plan_raw=json.dumps(plan,ensure_ascii=False,separators=(',',':'))
          wrapper=r'''
          try{$root=realpath(getcwd());$home=(string)getenv('HOME');if(!$root||basename($root)!=='anytoour.ru'||$home==='')throw new RuntimeException('V16_ROOT');$private=realpath($home.'/.anytoour-anex/search3-preview.php');if(!$private||!is_file($private))throw new RuntimeException('V16_PRIVATE');require $private;if(!defined('ANEX_API_TOKEN')||!is_string(ANEX_API_TOKEN)||trim(ANEX_API_TOKEN)==='')throw new RuntimeException('V16_TOKEN');$dbHelper=realpath($root.(is_file($root.'/data/db-v1.php')?'/data/db-v1.php':'/v2/data/db-v1.php'));if(!$dbHelper||strpos($dbHelper,$root.DIRECTORY_SEPARATOR)!==0)throw new RuntimeException('V16_DB');require_once $dbHelper;$plan=json_decode('__PLAN__',true,64,JSON_THROW_ON_ERROR);$details=hmadr_collect_details($plan['target_ids'],ANEX_API_TOKEN,$plan['target_sha256']);$review=hmadr_candidate_review(v2_data_db(),$plan,$details,HMADR_OPERATION);$out=['status'=>'completed','operation_id'=>HMADR_OPERATION,'database_writes'=>0,'mapping_writes'=>0,'bookings'=>0,'historical_operations_replayed'=>false,'plan_stats'=>$plan['stats'],'details'=>$details,'review'=>$review];echo json_encode($out,JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES),"\n";}catch(Throwable $e){$m=$e->getMessage();echo json_encode(['status'=>'failed','operation_id'=>HMADR_OPERATION,'reason'=>(is_string($m)&&preg_match('/\A[A-Z0-9_]{1,80}\z/D',$m)?$m:'V16_REMOTE_ERROR'),'database_writes'=>0,'mapping_writes'=>0,'bookings'=>0]),"\n";exit(2);}
          '''.replace('__PLAN__',plan_raw.replace('\\','\\\\').replace("'","\\'"))
          code="define('FC_LIBRARY_ONLY',true);\n"+helper+'\n'+client+'\n'+details+'\n'+wrapper
          req=('ANYTOOUR_DEPLOY_SSH_KEY','ANYTOOUR_DEPLOY_HOST','ANYTOOUR_DEPLOY_USER');host=os.environ[req[1]].strip();user=os.environ[req[2]].strip();keytext=os.environ[req[0]].strip()
          with tempfile.TemporaryDirectory(prefix='match-anex-v16-live-',dir=os.environ.get('RUNNER_TEMP')) as tmp:
              key=Path(tmp)/'key';key.write_text(keytext.rstrip()+'\n');key.chmod(0o600);known=Path(tmp)/'known_hosts';cmd=['ssh','-vv','-T','-i',str(key),'-o','IdentitiesOnly=yes','-o','BatchMode=yes','-o','StrictHostKeyChecking=accept-new','-o','UserKnownHostsFile='+str(known),'-o','ConnectTimeout=15','-o','ServerAliveInterval=15','-o','ServerAliveCountMax=3','-l',user,host,'cd "$HOME/www/anytoour.ru" && php -d display_errors=0 -d log_errors=0 -r '+shlex.quote(code)];clean={k:v for k,v in os.environ.items() if k not in req and not k.startswith('ANEX_') and not k.startswith('ANDROMEDA_')};p=subprocess.run(cmd,text=True,capture_output=True,timeout=900,env=clean)
          stderr=str(p.stderr);prog={'authenticated':'Authenticated to ' in stderr or 'Authentication succeeded' in stderr,'command_sent':'Sending command:' in stderr,'remote_exit_seen':'Exit status ' in stderr}
          try:r=json.loads(p.stdout)
          except Exception:r={'status':'unknown','operation_id':os.environ['OPERATION_ID'],'reason':'V16_OUTPUT','database_writes':0,'mapping_writes':0,'bookings':0}
          result=(json.dumps(r,ensure_ascii=False,sort_keys=True,separators=(',',':'))+'\n').encode();(state/'result.json').write_bytes(result);reservation=(state/'reservation.json').read_bytes();receipt={'operation_id':os.environ['OPERATION_ID'],'state':'completed' if p.returncode==0 and r.get('status')=='completed' else ('unknown_no_replay' if prog['command_sent'] else 'failed_precommand'),'run_id':int(os.environ['GITHUB_RUN_ID']),'source_sha':os.environ['GITHUB_SHA'],'result_sha256':hashlib.sha256(result).hexdigest(),'reservation_sha256':hashlib.sha256(reservation).hexdigest(),'exit_code':p.returncode,'ssh':prog,'database_writes':r.get('database_writes',0),'mapping_writes':r.get('mapping_writes',0),'bookings':r.get('bookings',0),'supplier_calls':((r.get('details') or {}).get('supplier_calls')),'no_replay':True};(state/'receipt.json').write_text(json.dumps(receipt,sort_keys=True)+'\n')
          d=r.get('details') or {};v=r.get('review') or {};print(json.dumps({'state':receipt['state'],'requested':d.get('requested'),'detail_ok':d.get('ok'),'detail_empty':d.get('empty'),'detail_errors':d.get('errors'),'supplier_calls':d.get('supplier_calls'),'prepared':(v.get('reasons') or {}).get('prepared'),'review_stats':v.get('reasons')},sort_keys=True))
          if receipt['state']!='completed' or r.get('operation_id')!=os.environ['OPERATION_ID'] or r.get('database_writes')!=0 or r.get('mapping_writes')!=0 or r.get('bookings')!=0 or r.get('historical_operations_replayed') is not False: raise SystemExit(2)
          PY
      - name: Preserve sealed evidence and receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: match-anex-current-details-1971-20260913-v16-result
          path: |
            ${{ runner.temp }}/hotel-match-anex-current-details-1971-20260913-v16/reservation.json
            ${{ runner.temp }}/hotel-match-anex-current-details-1971-20260913-v16/preflight.json
            ${{ runner.temp }}/hotel-match-anex-current-details-1971-20260913-v16/result.json
            ${{ runner.temp }}/hotel-match-anex-current-details-1971-20260913-v16/receipt.json
          if-no-files-found: warn
          retention-days: 90
