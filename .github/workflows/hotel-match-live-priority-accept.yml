name: MATCH live-priority bulk accept (disarmed)
run-name: MATCH live-priority bulk accept #1971 (NO-REPLAY)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  no_replay:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "NO-REPLAY hotel-match-live-priority-accept-1971-20260911-v1"
