name: Tour search test maintenance
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic server changes are disabled. Work is limited to poisk-turov-test."
