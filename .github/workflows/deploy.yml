name: Tour search maintenance

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: No automatic deployment
        run: echo "Automatic server changes are disabled."
