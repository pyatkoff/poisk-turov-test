name: Search3 AnyTour offer store

on:
  pull_request:
    branches: [release/search3-production-ready-v1]
    paths:
      - 'v2/data/anytour-offer-store-v1.php'
      - 'v2/data/migrations/20260916-anytour-canonical-catalog.sql'
      - 'v2/data/migrations/20260916-anytour-offer-store.sql'
      - 'tests/anytour-offer-store-v1-test.php'
      - 'docs/project/search3-anytour-offer-store-contract.md'
      - '.github/workflows/search3-anytour-offer-store.yml'

permissions:
  contents: read

jobs:
  offer-store:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: anytour_offer_store_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -proot"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          persist-credentials: false
      - uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240
        with:
          php-version: '8.3'
          extensions: mbstring, pdo_mysql
      - name: Lint isolated source
        shell: bash
        run: |
          set -euo pipefail
          php -l v2/data/anytour-offer-store-v1.php
          php -l tests/anytour-offer-store-v1-test.php
      - name: Exercise three-provider store against disposable MySQL
        env:
          ANYTOUR_OFFER_TEST_DSN: mysql:host=127.0.0.1;port=3306;dbname=anytour_offer_store_test;charset=utf8mb4
          ANYTOUR_OFFER_TEST_USER: root
          ANYTOUR_OFFER_TEST_PASSWORD: root
        shell: bash
        run: |
          set -euo pipefail
          php tests/anytour-offer-store-v1-test.php --require-sql
