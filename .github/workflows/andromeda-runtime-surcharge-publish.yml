name: Publish checked Andromeda surcharge runtime
on:
  pull_request:
    paths:
      - '.github/workflows/andromeda-runtime-surcharge-publish.yml'
  workflow_dispatch:
    inputs:
      confirmation:
        description: "publish-andromeda-surcharge-runtime-1717-v4 OR run-andromeda-surcharge-e2e-1717-v3"
        required: true
        type: string
  issue_comment:
    types: [created]
permissions:
  contents: read
  actions: read
concurrency:
  group: anytoour-andromeda-detail-publication
  cancel-in-progress: false
jobs:
  # v3 installation is completed: run34894766843/receipt10367874902. Never replay it.
  # v4 publishes the checked #2480 receipt fix; it does not run supplier E2E.
  publish:
    if: >-
      github.repository == 'pyatkoff/poisk-turov-test' && github.ref == 'refs/heads/main' &&
      github.actor == 'pyatkoff' && github.event.sender.id == 226193297 &&
      ((github.event_name == 'workflow_dispatch' && github.event.inputs.confirmation == 'publish-andromeda-surcharge-runtime-1717-v4') ||
       (github.event_name == 'issue_comment' && github.event.action == 'created' &&
        github.event.issue.number == 996 && !github.event.issue.pull_request &&
        github.event.comment.user.id == 226193297 &&
        github.event.comment.body == '/publish-andromeda-surcharge-runtime-1717-v4'))
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Require exact owner command and checked source lineage
        uses: actions/github-script@v7
        with:
          script: |
            const owner = 226193297;
            const command = '/publish-andromeda-surcharge-runtime-1717-v4';
            const runtimeHead = '6c174898ebf4876ae024f7173d53e1f6dc4837f7';
            const runtimeMerge = 'd2c1b777f2e600ebbca0ec8002ad75ff7de4870a';
            const publisherHead = '64706822fc54f4d4423ea6bbd0ad966144151387';
            const publisherMerge = '23dd08a985eb58ed93652d97b60dac6c4145020c';
            if (process.env.GITHUB_RUN_ATTEMPT !== '1') throw new Error('Do not replay an unknown publication; inspect its receipt.');
            if (context.payload.sender?.id !== owner || context.actor !== 'pyatkoff' || process.env.GITHUB_TRIGGERING_ACTOR !== 'pyatkoff') throw new Error('Owner invocation required.');
            if (context.repo.owner !== 'pyatkoff' || context.repo.repo !== 'poisk-turov-test' || context.ref !== 'refs/heads/main') throw new Error('Repository/ref mismatch.');
            if (context.eventName === 'workflow_dispatch') {
              if (context.payload.inputs?.confirmation !== 'publish-andromeda-surcharge-runtime-1717-v4') throw new Error('Confirmation mismatch.');
            } else if (context.eventName !== 'issue_comment' || context.payload.action !== 'created' ||
                       context.payload.issue?.number !== 996 || context.payload.issue?.pull_request ||
                       context.payload.comment?.user?.id !== owner || context.payload.comment?.body !== command) {
              throw new Error('Exact created owner command in issue996 required.');
            }
            const {data: main} = await github.rest.git.getRef({...context.repo, ref: 'heads/main'});
            if (main.object.sha !== context.sha) throw new Error('Main advanced; review rather than replay.');
            const {data: runtimePr} = await github.rest.pulls.get({...context.repo, pull_number: 2480});
            if (!runtimePr.merged || runtimePr.head.sha !== runtimeHead || runtimePr.merge_commit_sha !== runtimeMerge || runtimePr.base.ref !== 'feature/anex-search-adapter-20260907') throw new Error('Runtime PR provenance mismatch.');
            const {data: publisherPr} = await github.rest.pulls.get({...context.repo, pull_number: 2461});
            if (!publisherPr.merged || publisherPr.head.sha !== publisherHead || publisherPr.merge_commit_sha !== publisherMerge || publisherPr.base.ref !== 'feature/anex-search-adapter-20260907') throw new Error('Publisher PR provenance mismatch.');
            const gates = [
              [34906332728, runtimeHead],
              [34906332715, runtimeHead],
              [34906332739, runtimeHead],
              [34892746421, publisherHead]
            ];
            for (const [run_id, sha] of gates) {
              const {data: run} = await github.rest.actions.getWorkflowRun({...context.repo, run_id});
              if (run.head_sha !== sha || run.status !== 'completed' || run.conclusion !== 'success') throw new Error('Pinned CI gate failed: ' + run_id);
            }
            const {data: artifact} = await github.rest.actions.getArtifact({...context.repo, artifact_id: 10372349715});
            if (artifact.id !== 10372349715 || artifact.expired !== false || artifact.workflow_run?.id !== 34906332715 ||
                artifact.workflow_run?.head_sha !== runtimeHead ||
                artifact.digest !== 'sha256:db005447d29f2894e0cd93ee1e781fd2572f367753dedb853c8359cc959036e2') throw new Error('Artifact identity/digest mismatch.');
      - uses: actions/checkout@v4
        with:
          ref: 23dd08a985eb58ed93652d97b60dac6c4145020c
          path: publisher
          persist-credentials: false
      - uses: actions/checkout@v4
        with:
          ref: 95fa11f5a1cb7e18c905c9760a1eae7f2c0ee7aa
          path: helper
          persist-credentials: false
      - uses: actions/download-artifact@v4
        with:
          artifact-ids: '10372349715'
          run-id: '34906332715'
          github-token: ${{ github.token }}
          path: candidate
          merge-multiple: true
      - name: Publish exact checked runtime to isolated Andromeda preview
        env:
          RUNTIME_SOURCE_SHA: 6c174898ebf4876ae024f7173d53e1f6dc4837f7
          ANYTOOUR_DEPLOY_SSH_KEY: ${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}
          ANYTOOUR_DEPLOY_HOST: ${{ secrets.ANYTOOUR_DEPLOY_HOST }}
          ANYTOOUR_DEPLOY_USER: ${{ secrets.ANYTOOUR_DEPLOY_USER }}
        run: python3 -B publisher/scripts/diagnostics/andromeda_runtime_source_publish.py candidate helper
      - name: Verify both HTTP entrypoints without search or quote operations
        run: |
          python3 - <<'PYHTTP'
          import hashlib, http.client, json, os
          from pathlib import Path
          out = Path(os.environ['RUNNER_TEMP']) / 'andromeda-runtime-source-publication'
          published = json.loads((out / 'result.json').read_text())
          if published.get('status') not in ('published','already_published'):
              raise SystemExit('Publication is unconfirmed; do not replay')
          report = {'status':'incomplete','source':published['source'],'checks':[],
                    'validation_only':True,'supplier_calls':0,'database_reads':0,'booking_calls':0}
          cases = [('GET',{},405,'method_not_allowed'),
                   ('POST',{},403,'forbidden'),
                   ('POST',{'X-Requested-With':'AnyTourSearch3','Content-Type':'text/plain'},400,'invalid_request')]
          try:
              for endpoint in ('api-andromeda-search3-preview.php','api-andromeda-quote-preview.php'):
                  for method, headers, status, error in cases:
                      connection = http.client.HTTPSConnection('anytoour.ru', timeout=20)
                      try:
                          connection.request(method, '/_preview/search3-anex-candidate/' + endpoint,
                                             body=None if method == 'GET' else b'', headers=headers)
                          response = connection.getresponse()
                          body = response.read(4097)
                          row = {'endpoint':endpoint,'method':method,'expected_status':status,
                                 'status':response.status,'body_sha256':hashlib.sha256(body).hexdigest()}
                          report['checks'].append(row)
                          if (response.status != status or len(body) > 4096
                                  or json.loads(body) != {'ok':False,'error':error}):
                              raise SystemExit('HTTP rejection readback failed; inspect receipt, do not republish')
                          row['verified'] = True
                      finally:
                          connection.close()
              report['status'] = 'verified'
          finally:
              (out / 'http-readback.json').write_text(json.dumps(report, sort_keys=True, indent=2) + '\n')
          print(json.dumps(report, sort_keys=True))
          PYHTTP
      - name: Preserve publication receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: andromeda-runtime-surcharge-publication-${{ github.run_id }}
          path: ${{ runner.temp }}/andromeda-runtime-source-publication/
          if-no-files-found: error
          retention-days: 14

  surcharge-e2e:
    if: >-
      github.repository == 'pyatkoff/poisk-turov-test' && github.ref == 'refs/heads/main' &&
      github.actor == 'pyatkoff' && github.event.sender.id == 226193297 &&
      ((github.event_name == 'workflow_dispatch' && github.event.inputs.confirmation == 'run-andromeda-surcharge-e2e-1717-v3') ||
       (github.event_name == 'issue_comment' && github.event.action == 'created' &&
        github.event.issue.number == 996 && !github.event.issue.pull_request &&
        github.event.comment.user.id == 226193297 &&
        github.event.comment.body == '/run-andromeda-surcharge-e2e-1717-v3'))
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Require exact owner command, checked E2E source and published runtime
        uses: actions/github-script@v7
        with:
          script: |
            const owner = 226193297;
            const command = '/run-andromeda-surcharge-e2e-1717-v3';
            const e2eHead = '7d09c33f4acf5b7d9b13db35f54666d96333beb6';
            const e2eMerge = 'a9296c59935de6ba4af2b614f5c81afe3c5e0abf';
            const publicationRun = 34911727400;
            const accessRun = 34897622645;
            if (process.env.GITHUB_RUN_ATTEMPT !== '1') throw new Error('No replay; inspect the E2E checkpoint.');
            if (context.payload.sender?.id !== owner || context.actor !== 'pyatkoff' || process.env.GITHUB_TRIGGERING_ACTOR !== 'pyatkoff') throw new Error('Owner invocation required.');
            if (context.repo.owner !== 'pyatkoff' || context.repo.repo !== 'poisk-turov-test' || context.ref !== 'refs/heads/main') throw new Error('Repository/ref mismatch.');
            if (context.eventName === 'workflow_dispatch') {
              if (context.payload.inputs?.confirmation !== 'run-andromeda-surcharge-e2e-1717-v3') throw new Error('Confirmation mismatch.');
            } else if (context.eventName !== 'issue_comment' || context.payload.action !== 'created' ||
                       context.payload.issue?.number !== 996 || context.payload.issue?.pull_request ||
                       context.payload.comment?.user?.id !== owner || context.payload.comment?.body !== command) {
              throw new Error('Exact created owner command in issue996 required.');
            }
            const {data: main} = await github.rest.git.getRef({...context.repo, ref: 'heads/main'});
            if (main.object.sha !== context.sha) throw new Error('Main advanced; review rather than replay.');
            const {data: e2ePr} = await github.rest.pulls.get({...context.repo, pull_number: 2484});
            if (!e2ePr.merged || e2ePr.head.sha !== e2eHead || e2ePr.merge_commit_sha !== e2eMerge || e2ePr.base.ref !== 'feature/anex-search-adapter-20260907') throw new Error('E2E PR provenance mismatch.');
            for (const run_id of [34912403063, 34912403079]) {
              const {data: run} = await github.rest.actions.getWorkflowRun({...context.repo, run_id});
              if (run.head_sha !== e2eHead || run.status !== 'completed' || run.conclusion !== 'success') throw new Error('Pinned E2E CI failed: ' + run_id);
            }
            const {data: published} = await github.rest.actions.getWorkflowRun({...context.repo, run_id: publicationRun});
            if (published.head_sha !== 'cab540a705a28272d4791a57eab17bd55f905550' || published.status !== 'completed' || published.conclusion !== 'success') throw new Error('Terminal runtime publication mismatch.');
            const {data: publicationReceipt} = await github.rest.actions.getArtifact({...context.repo, artifact_id: 10374721928});
            if (publicationReceipt.expired !== false || publicationReceipt.workflow_run?.id !== publicationRun ||
                publicationReceipt.digest !== 'sha256:bac38b289a3e1b3953d5692b2ca7d486f6ee1eb951936ac51b4764b4fe7ba9e0') throw new Error('Runtime publication receipt mismatch.');
            const {data: access} = await github.rest.actions.getWorkflowRun({...context.repo, run_id: accessRun});
            if (access.head_sha !== '4081970c5d2b713cc77b7ed564ab010135499156' || access.status !== 'completed' || access.conclusion !== 'success') throw new Error('Quote HTTP access gate failed.');
            const {data: accessReceipt} = await github.rest.actions.getArtifact({...context.repo, artifact_id: 10369517015});
            if (accessReceipt.expired !== false || accessReceipt.workflow_run?.id !== accessRun ||
                accessReceipt.digest !== 'sha256:0b80a49a788bd2db791a10762ab1a2045caa0ba14d776e0fc651aec6739796c2') throw new Error('Quote HTTP access receipt mismatch.');
      - uses: actions/checkout@v4
        with:
          ref: a9296c59935de6ba4af2b614f5c81afe3c5e0abf
          path: e2e
          persist-credentials: false
      - uses: actions/checkout@v4
        with:
          ref: 95fa11f5a1cb7e18c905c9760a1eae7f2c0ee7aa
          path: helper
          persist-credentials: false
      - name: Execute one immutable served-price E2E operation
        env:
          ANYTOOUR_DEPLOY_SSH_KEY: ${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}
          ANYTOOUR_DEPLOY_HOST: ${{ secrets.ANYTOOUR_DEPLOY_HOST }}
          ANYTOOUR_DEPLOY_USER: ${{ secrets.ANYTOOUR_DEPLOY_USER }}
          PYTHONPATH: helper/scripts/diagnostics
        run: |
          python3 - <<'PY'
          import json, os, pathlib
          from anex_search3_owner_decisions import ssh_php
          source = pathlib.Path('e2e/scripts/diagnostics/andromeda_search_surcharge_e2e.php').read_text()
          if not source.startswith('<?php\n') or len(source.encode()) > 65536:
              raise SystemExit('checked E2E source invalid')
          out = pathlib.Path(os.environ['RUNNER_TEMP']) / 'andromeda-surcharge-e2e'
          out.mkdir(mode=0o700)
          try:
              result = ssh_php(source.removeprefix('<?php\n'), {}, maximum_bytes=65536)
          except Exception:
              (out / 'result.json').write_text(json.dumps({'status':'unconfirmed','operation':'andromeda-search-surcharge-e2e-1717-v3-egypt-2026-12-20-3a-10n'}))
              raise
          (out / 'result.json').write_text(json.dumps(result, sort_keys=True, indent=2) + '\n')
          if not isinstance(result, dict) or result.get('status') != 'complete' or result.get('supplier_scenario') != 1:
              raise SystemExit('E2E operation not complete; do not replay')
          if result.get('mapping_writes') != 0 or result.get('booking_calls') != 0 or result.get('calc_calls') != 1:
              raise SystemExit('E2E guard mismatch')
          print(json.dumps(result, sort_keys=True))
          PY
      - name: Preserve sanitized E2E receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: andromeda-surcharge-e2e-${{ github.run_id }}
          path: ${{ runner.temp }}/andromeda-surcharge-e2e/
          if-no-files-found: error
          retention-days: 14

  readback-offline:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - name: Exercise exact fixed-checkpoint reader without SSH or supplier access
        run: |
          python3 - <<'PY'
          import hashlib, json, pathlib, subprocess, tempfile, textwrap
          text = pathlib.Path('.github/workflows/andromeda-runtime-surcharge-publish.yml').read_text()
          source = textwrap.dedent(text.rsplit("source = r'''", 1)[1].split("'''", 1)[0]).strip()
          operation = 'andromeda-search-surcharge-e2e-1717-v1-egypt-2026-12-10-2a'
          with tempfile.TemporaryDirectory() as tmp:
              account = pathlib.Path(tmp) / 'account'
              root = account / 'www' / 'anytoour.ru'; root.mkdir(parents=True)
              private = account / '.anytoour-andromeda'; private.mkdir()
              directory = private / operation
              def run():
                  process = subprocess.run(['php', '-d', 'allow_url_fopen=0', '-r', source], cwd=root, text=True, capture_output=True, check=True)
                  assert process.stderr == '', process.stderr
                  result = json.loads(process.stdout)
                  assert result['supplier_calls'] == result['database_reads'] == result['database_writes'] == result['booking_calls'] == 0
                  assert result['operation_replayed'] is False
                  assert 'PRIVATE_SENTINEL' not in process.stdout
                  return result
              result = run(); assert result['status'] == 'inspected' and result['operation_exists'] is False
              directory.mkdir()
              reservation = directory / 'reservation.json'; outcome = directory / 'result.json'
              reservation.write_text(json.dumps({'operation':operation,'runtime_source':'b32993261eb8657004e7d4fd146b65a95264d4bc','scenario_sha256':'a'*64,'private':'PRIVATE_SENTINEL'}))
              outcome.write_text(json.dumps({'operation':operation,'status':'unknown','reason':'ANDROMEDA_PACKAGE_OUTCOME_UNKNOWN','sid':'PRIVATE_SENTINEL'}))
              before = [p.read_bytes() for p in [reservation,outcome]]
              result = run(); assert result['operation_status'] == 'unknown' and result['reason'] == 'ANDROMEDA_PACKAGE_OUTCOME_UNKNOWN'
              assert result['files']['result']['sha256'] == hashlib.sha256(before[1]).hexdigest()
              assert before == [p.read_bytes() for p in [reservation,outcome]]
              outcome.write_text(json.dumps({'operation':operation,'status':'unknown','reason':'PRIVATE_SENTINEL'}))
              assert run()['reason'] == 'unclassified_operation_failure'
              outcome.unlink(); outcome.symlink_to(reservation)
              assert run()['inspection_error'] == 'checkpoint_path_invalid'
              outcome.unlink(); outcome.write_text('{')
              assert run()['inspection_error'] == 'checkpoint_invalid'
              outcome.write_text(json.dumps({'operation':'other','status':'complete'}))
              assert run()['inspection_error'] == 'checkpoint_operation_mismatch'
              outcome.write_text('x'*65537)
              assert run()['inspection_error'] == 'checkpoint_size_or_link'
          print('Fixed readback: missing, retained unknown, digest, privacy, symlink, malformed, foreign and bounded-size cases passed; supplier/SSH=0')
          PY

  surcharge-e2e-readback:
    if: >-
      github.repository == 'pyatkoff/poisk-turov-test' && github.ref == 'refs/heads/main' &&
      github.actor == 'pyatkoff' && github.event.sender.id == 226193297 &&
      github.event_name == 'issue_comment' && github.event.action == 'created' &&
      github.event.issue.number == 996 && !github.event.issue.pull_request &&
      github.event.comment.user.id == 226193297 &&
      github.event.comment.body == '/inspect-andromeda-surcharge-e2e-1717-v1'
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Bind owner-only inspection to the terminal E2E run
        uses: actions/github-script@v7
        with:
          script: |
            if (process.env.GITHUB_RUN_ATTEMPT !== '1' || process.env.GITHUB_TRIGGERING_ACTOR !== 'pyatkoff') throw new Error('No automatic rerun.');
            if (context.ref !== 'refs/heads/main' || context.actor !== 'pyatkoff' || context.payload.sender?.id !== 226193297 ||
                context.eventName !== 'issue_comment' || context.payload.action !== 'created' || context.payload.issue?.number !== 996 ||
                context.payload.issue?.pull_request || context.payload.comment?.user?.id !== 226193297 ||
                context.payload.comment?.body !== '/inspect-andromeda-surcharge-e2e-1717-v1') throw new Error('Exact owner command required.');
            const {data: main} = await github.rest.git.getRef({...context.repo, ref:'heads/main'});
            if (main.object.sha !== context.sha) throw new Error('Main advanced; stop.');
            const {data: run} = await github.rest.actions.getWorkflowRun({...context.repo, run_id:34874678175});
            if (run.head_sha !== '275a416ba3ad25d9b04523470b4eab50334808b9' || run.status !== 'completed' || run.conclusion !== 'failure') throw new Error('Terminal source run mismatch.');
      - uses: actions/checkout@v4
        with:
          ref: 95fa11f5a1cb7e18c905c9760a1eae7f2c0ee7aa
          path: helper
          persist-credentials: false
      - name: Read only fixed reservation and result; never invoke the E2E operation
        env:
          ANYTOOUR_DEPLOY_SSH_KEY: ${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}
          ANYTOOUR_DEPLOY_HOST: ${{ secrets.ANYTOOUR_DEPLOY_HOST }}
          ANYTOOUR_DEPLOY_USER: ${{ secrets.ANYTOOUR_DEPLOY_USER }}
          PYTHONPATH: helper/scripts/diagnostics
        run: |
          python3 - <<'PY'
          import json, os, pathlib
          from anex_search3_owner_decisions import ssh_php
          source = r'''
          error_reporting(0); ini_set('display_errors','0'); ini_set('log_errors','0');
          $operation = 'andromeda-search-surcharge-e2e-1717-v1-egypt-2026-12-10-2a';
          $out = ['status'=>'blocked','operation'=>$operation,'supplier_calls'=>0,'database_reads'=>0,'database_writes'=>0,'booking_calls'=>0,'operation_replayed'=>false];
          try {
              $root = realpath(getcwd());
              if (PHP_SAPI !== 'cli' || !$root || basename($root) !== 'anytoour.ru') throw new RuntimeException('project_invalid');
              $private = dirname($root, 2) . '/.anytoour-andromeda';
              if (realpath($private) !== $private || !is_dir($private) || !is_readable($private)) throw new RuntimeException('private_directory_unavailable');
              $directory = $private . '/' . $operation;
              $out['operation_exists'] = file_exists($directory);
              if (is_link($directory) || ($out['operation_exists'] && (realpath($directory) !== $directory || !is_dir($directory) || !is_readable($directory)))) throw new RuntimeException('checkpoint_path_invalid');
              $out['files'] = [];
              foreach (['reservation','result'] as $name) {
                  $path = $directory . '/' . $name . '.json';
                  $out['files'][$name] = ['present'=>false];
                  if (is_link($path)) throw new RuntimeException('checkpoint_path_invalid');
                  if (!file_exists($path)) continue;
                  if (realpath($path) !== $path || !is_file($path)) throw new RuntimeException('checkpoint_path_invalid');
                  $file = fopen($path, 'rb');
                  if (!$file) throw new RuntimeException('checkpoint_unreadable');
                  try {
                      $stat = fstat($file);
                      if ($stat['nlink'] !== 1 || $stat['size'] > 65536) throw new RuntimeException('checkpoint_size_or_link');
                      $bytes = stream_get_contents($file, 65537);
                      if (!is_string($bytes) || strlen($bytes) !== $stat['size']) throw new RuntimeException('checkpoint_read_failed');
                  } finally { fclose($file); }
                  $value = json_decode($bytes, true, 16, JSON_THROW_ON_ERROR);
                  if (!is_array($value) || ($value['operation'] ?? null) !== $operation) throw new RuntimeException('checkpoint_operation_mismatch');
                  $out['files'][$name] = ['present'=>true,'sha256'=>hash('sha256',$bytes)];
                  if ($name === 'reservation') {
                      foreach (['runtime_source'=>40,'scenario_sha256'=>64] as $key=>$length) {
                          if (!is_string($value[$key] ?? null) || !preg_match('/^[a-f0-9]{'.$length.'}$/D',$value[$key])) throw new RuntimeException('reservation_invalid');
                          $out[$key] = $value[$key];
                      }
                  } else {
                      $status = $value['status'] ?? null;
                      if (!in_array($status,['blocked','unknown','complete'],true)) throw new RuntimeException('result_status_invalid');
                      $out['operation_status'] = $status;
                      $reason = $value['reason'] ?? null;
                      $known = ['project_invalid','runtime_missing','publication_lock_missing','publication_busy','runtime_not_installed','private_runtime_missing','operation_exists_no_replay','reservation_failed','database_missing','no_mapped_offer','selected_offer_invalid','surcharge_capture_invalid','retained_offer_missing','listing_surcharge_not_applied','listing_money_invalid','checkpoint_exists','checkpoint_write','checkpoint_sync','checkpoint_readback','checkpoint_too_large','operation_unconfirmed','meal_not_supported','departure_not_supported','country_not_supported'];
                      $out['reason'] = $reason === null ? null : (is_string($reason) && (in_array($reason,$known,true) || preg_match('/^ANDROMEDA_[A-Z_]{2,80}$/D',$reason)) ? $reason : 'unclassified_operation_failure');
                      $out['reason_sha256'] = is_string($reason) ? hash('sha256',$reason) : null;
                      $out['outcome'] = in_array($value['outcome'] ?? null,['surcharge_unavailable','listing_estimate_applied'],true) ? $value['outcome'] : null;
                  }
              }
              $out['status'] = 'inspected';
          } catch (Throwable $error) {
              $allowed = ['project_invalid','private_directory_unavailable','checkpoint_path_invalid','checkpoint_unreadable','checkpoint_size_or_link','checkpoint_read_failed','checkpoint_operation_mismatch','reservation_invalid','result_status_invalid'];
              $out['inspection_error'] = in_array($error->getMessage(),$allowed,true) ? $error->getMessage() : 'checkpoint_invalid';
          }
          echo json_encode($out, JSON_THROW_ON_ERROR);
          '''
          out = pathlib.Path(os.environ['RUNNER_TEMP']) / 'andromeda-surcharge-e2e-readback'
          out.mkdir(mode=0o700)
          try:
              result = ssh_php(source, {}, maximum_bytes=65536)
          except Exception:
              (out / 'result.json').write_text(json.dumps({'status':'inspection_unconfirmed','operation_replayed':False}))
              raise
          result['control_source'] = os.environ['GITHUB_SHA']
          (out / 'result.json').write_text(json.dumps(result, sort_keys=True, indent=2) + '\n')
          if result.get('status') != 'inspected' or result.get('operation_replayed') is not False:
              raise SystemExit('Checkpoint inspection incomplete; operation was not replayed')
          print(json.dumps(result, sort_keys=True))
          PY
      - name: Preserve sanitized checkpoint inspection
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: andromeda-surcharge-e2e-readback-${{ github.run_id }}
          path: ${{ runner.temp }}/andromeda-surcharge-e2e-readback/
          if-no-files-found: error
          retention-days: 14

  quote-access-inspection:
    if: >-
      github.event_name == 'pull_request' ||
      (github.repository == 'pyatkoff/poisk-turov-test' && github.ref == 'refs/heads/main' &&
       github.actor == 'pyatkoff' && github.event.sender.id == 226193297 &&
       github.event_name == 'issue_comment' && github.event.action == 'created' &&
       github.event.issue.number == 996 && !github.event.issue.pull_request &&
       github.event.comment.user.id == 226193297 &&
       github.event.comment.body == '/inspect-andromeda-quote-http-2464')
    runs-on: ubuntu-latest
    timeout-minutes: 3
    env:
      QUOTE_INSPECTION_PHP: |
        error_reporting(0);ini_set('display_errors','0');ini_set('log_errors','0');
        ini_set('pcre.backtrack_limit','10000');ini_set('pcre.recursion_limit','1000');
        $out=['status'=>'blocked','source'=>'64706822fc54f4d4423ea6bbd0ad966144151387',
          'supplier_calls'=>0,'database_reads'=>0,'database_writes'=>0,'booking_calls'=>0,'filesystem_writes'=>0];
        try{
          $root=realpath(getcwd());if(PHP_SAPI!=='cli'||!$root||basename($root)!=='anytoour.ru')throw new RuntimeException('project_invalid');
          $prefix='_preview/search3-anex-candidate/';$out['files']=[];$out['directories']=[];
          foreach(['','_preview','_preview/search3-anex-candidate'] as $rel){
            $path=$root.($rel===''?'':'/'.$rel);
            if(!is_dir($path)||realpath($path)!==$path)throw new RuntimeException('directory_invalid');
            $out['directories'][$rel===''?'project':$rel]=['mode'=>sprintf('%04o',fileperms($path)&0777)];
          }
          $apis=['api-andromeda-search3-preview.php'=>'fc338f37496290871793c6b1fe0f5614b4aea4db2a2f0503277117a676a912e4',
            'api-andromeda-quote-preview.php'=>'12c64956f6b98a06a282c5965c1001cb6658f902412ddf07a09ece7c8a676c48'];
          $paths=['.htaccess','_preview/.htaccess',$prefix.'.htaccess'];
          foreach(array_keys($apis) as $api)$paths[]=$prefix.$api;
          foreach($paths as $rel){
            $path=$root.'/'.$rel;if(is_link($path))throw new RuntimeException('file_path_invalid');
            if(!file_exists($path)){$out['files'][$rel]=['present'=>false];continue;}
            if(!is_file($path)||realpath($path)!==$path)throw new RuntimeException('file_path_invalid');
            $stat=stat($path);$limit=str_ends_with($rel,'.htaccess')?65536:524288;
            if($stat['nlink']!==1||$stat['size']>$limit)throw new RuntimeException('file_size_or_link');
            $bytes=file_get_contents($path,false,null,0,$limit+1);
            if(!is_string($bytes)||strlen($bytes)!==$stat['size'])throw new RuntimeException('file_read_failed');
            $item=['present'=>true,'mode'=>sprintf('%04o',$stat['mode']&0777),'sha256'=>hash('sha256',$bytes)];
            if(isset($apis[basename($rel)])){$item['expected_hash']=$item['sha256']===$apis[basename($rel)];}
            else{
              $item['file_blocks']=[];
              preg_match_all('~<(FilesMatch|Files)\s+([^>\r\n]{1,800})>(.*?)</\1>~is',$bytes,$blocks,PREG_SET_ORDER);
              foreach(array_slice($blocks,0,64) as $block){
                $selector=trim(trim($block[2]),"\"'");
                $rule=['kind'=>strtolower($block[1]),'selector_sha256'=>hash('sha256',$selector),
                  'require_all_denied'=>(bool)preg_match('/^\s*Require\s+all\s+denied\s*$/mi',$block[3]),
                  'require_all_granted'=>(bool)preg_match('/^\s*Require\s+all\s+granted\s*$/mi',$block[3]),
                  'deny_from_all'=>(bool)preg_match('/^\s*Deny\s+from\s+all\s*$/mi',$block[3])];
                foreach(array_keys($apis) as $api){
                  $match=strtolower($block[1])==='filesmatch'?preg_match('~'.str_replace('~','\\~',$selector).'~',$api):(int)fnmatch($selector,$api);
                  $rule['matches'][$api]=$match===false?null:(bool)$match;
                }
                $item['file_blocks'][]=$rule;
              }
              $item['quote_name_mentioned']=str_contains($bytes,'api-andromeda-quote-preview');
              $item['rewrite_forbidden_present']=(bool)preg_match('/^\s*RewriteRule\b[^\r\n]*\[(?:[^\]\r\n]*,)?F(?:,|\])/mi',$bytes);
            }
            $out['files'][$rel]=$item;
          }
          $out['status']='inspected';
        }catch(Throwable $e){
          $allowed=['project_invalid','directory_invalid','file_path_invalid','file_size_or_link','file_read_failed'];
          $out['reason']=in_array($e->getMessage(),$allowed,true)?$e->getMessage():'inspection_failed';
        }
        echo json_encode($out,JSON_THROW_ON_ERROR);
    steps:
      - name: Exercise the exact read-only inspection on local fixtures
        if: github.event_name == 'pull_request'
        run: |
          python3 - <<'PY'
          import hashlib,json,os,pathlib,subprocess,tempfile
          with tempfile.TemporaryDirectory() as tmp:
              root=pathlib.Path(tmp)/'anytoour.ru'; target=root/'_preview/search3-anex-candidate';target.mkdir(parents=True)
              quote=target/'api-andromeda-quote-preview.php';quote.write_text('PRIVATE_SENTINEL');quote.chmod(0o600)
              access=target/'.htaccess';access.write_text('<FilesMatch "^api-andromeda-quote-preview[.]php$">\nRequire all denied\n</FilesMatch>\nAuthUserFile /PRIVATE_SENTINEL\n')
              def run():
                  p=subprocess.run(['php','-d','allow_url_fopen=0','-r',os.environ['QUOTE_INSPECTION_PHP']],cwd=root,text=True,capture_output=True,check=True,timeout=5)
                  assert p.stderr=='' and 'PRIVATE_SENTINEL' not in p.stdout
                  result=json.loads(p.stdout)
                  assert all(result[k]==0 for k in ['supplier_calls','database_reads','database_writes','booking_calls','filesystem_writes'])
                  return result
              before={p:p.read_bytes() for p in [quote,access]}
              result=run();assert result['status']=='inspected'
              q=result['files']['_preview/search3-anex-candidate/api-andromeda-quote-preview.php'];assert q['mode']=='0600' and q['expected_hash'] is False
              rule=result['files']['_preview/search3-anex-candidate/.htaccess']['file_blocks'][0]
              assert rule['require_all_denied'] and rule['matches']=={'api-andromeda-quote-preview.php':True,'api-andromeda-search3-preview.php':False}
              assert before=={p:p.read_bytes() for p in before}
              access.write_text('<FilesMatch "^(?!api-andromeda-search3-preview[.]php$).*\\.php$">\nRequire all denied\n</FilesMatch>')
              rule=run()['files']['_preview/search3-anex-candidate/.htaccess']['file_blocks'][0]
              assert rule['matches']=={'api-andromeda-quote-preview.php':True,'api-andromeda-search3-preview.php':False}
              access.write_text('<Files "api-andromeda-quote-preview.php">\nRequire all granted\n</Files>')
              assert run()['files']['_preview/search3-anex-candidate/.htaccess']['file_blocks'][0]['require_all_granted']
              access.write_text('x'*65537);assert run()['reason']=='file_size_or_link'
              access.unlink();access.symlink_to(quote);assert run()['reason']=='file_path_invalid'
              access.unlink();assert run()['files']['_preview/search3-anex-candidate/.htaccess']['present'] is False
              quote.unlink();assert run()['files']['_preview/search3-anex-candidate/api-andromeda-quote-preview.php']['present'] is False
          print('Read-only quote access inspection: modes, hash, explicit/negative file rules, privacy, size, links and missing cases pass; network0')
          PY
      - name: Bind inspection to the completed publication and exact owner command
        if: github.event_name != 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            if (process.env.GITHUB_RUN_ATTEMPT !== '1' || process.env.GITHUB_TRIGGERING_ACTOR !== 'pyatkoff' ||
                context.actor !== 'pyatkoff' || context.payload.sender?.id !== 226193297 || context.ref !== 'refs/heads/main' ||
                context.eventName !== 'issue_comment' || context.payload.action !== 'created' || context.payload.issue?.number !== 996 ||
                context.payload.issue?.pull_request || context.payload.comment?.user?.id !== 226193297 ||
                context.payload.comment?.body !== '/inspect-andromeda-quote-http-2464') throw new Error('Exact owner inspection required.');
            const {data: main}=await github.rest.git.getRef({...context.repo,ref:'heads/main'});
            if(main.object.sha!==context.sha) throw new Error('Main advanced; stop.');
            const {data: run}=await github.rest.actions.getWorkflowRun({...context.repo,run_id:34894766843});
            if(run.head_sha!=='8412314bcf19fd1b658895c49726499730ec8b85'||run.status!=='completed'||run.conclusion!=='failure') throw new Error('Terminal publication mismatch.');
            const {data: receipt}=await github.rest.actions.getArtifact({...context.repo,artifact_id:10367874902});
            if(receipt.expired!==false||receipt.workflow_run?.id!==34894766843||receipt.digest!=='sha256:92817ff32131aa980beb7c5936d0d7d441d7c74fbdc09d7f4c844cb5b58f0132') throw new Error('Publication receipt mismatch.');
      - uses: actions/checkout@v4
        if: github.event_name != 'pull_request'
        with:
          ref: 95fa11f5a1cb7e18c905c9760a1eae7f2c0ee7aa
          path: helper
          persist-credentials: false
      - name: Read fixed preview file metadata and non-secret rule matches only
        if: github.event_name != 'pull_request'
        env:
          ANYTOOUR_DEPLOY_SSH_KEY: ${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}
          ANYTOOUR_DEPLOY_HOST: ${{ secrets.ANYTOOUR_DEPLOY_HOST }}
          ANYTOOUR_DEPLOY_USER: ${{ secrets.ANYTOOUR_DEPLOY_USER }}
          PYTHONPATH: helper/scripts/diagnostics
        run: |
          python3 - <<'PY'
          import json,os,pathlib
          from anex_search3_owner_decisions import ssh_php
          out=pathlib.Path(os.environ['RUNNER_TEMP'])/'andromeda-quote-access-inspection';out.mkdir(mode=0o700)
          try:
              result=ssh_php(os.environ['QUOTE_INSPECTION_PHP'],{},maximum_bytes=65536)
          except Exception:
              (out/'result.json').write_text(json.dumps({'status':'inspection_unconfirmed','publication_replayed':False}))
              raise
          result['control_source']=os.environ['GITHUB_SHA'];result['publication_replayed']=False
          (out/'result.json').write_text(json.dumps(result,sort_keys=True,indent=2)+'\n')
          if result.get('status')!='inspected' or any(result.get(k)!=0 for k in ['supplier_calls','database_reads','database_writes','booking_calls','filesystem_writes']):
              raise SystemExit('Read-only inspection incomplete; publication not replayed')
          print(json.dumps(result,sort_keys=True))
          PY
      - name: Preserve sanitized access inspection
        if: always() && github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: andromeda-quote-access-inspection-${{ github.run_id }}
          path: ${{ runner.temp }}/andromeda-quote-access-inspection/
          if-no-files-found: error
          retention-days: 14

  surcharge-e2e-v2-readback:
    if: >-
      github.event_name == 'pull_request' ||
      (github.repository == 'pyatkoff/poisk-turov-test' && github.ref == 'refs/heads/main' &&
       github.actor == 'pyatkoff' && github.event.sender.id == 226193297 &&
       github.event_name == 'issue_comment' && github.event.action == 'created' &&
       github.event.issue.number == 996 && !github.event.issue.pull_request &&
       github.event.comment.user.id == 226193297 &&
       github.event.comment.body == '/inspect-andromeda-surcharge-e2e-1717-v2-money')
    runs-on: ubuntu-latest
    timeout-minutes: 3
    env:
      ARCHIVAL_MONEY_PHP: |
        function v2_archive_json(string $path, int $limit): array {
            if (is_link($path) || realpath($path) !== $path || !is_file($path)) throw new RuntimeException('archive_path_invalid');
            $file = fopen($path, 'rb');
            if (!$file) throw new RuntimeException('archive_unreadable');
            try {
                $stat = fstat($file);
                if ($stat['nlink'] !== 1 || $stat['size'] > $limit) throw new RuntimeException('archive_size_or_link');
                $bytes = stream_get_contents($file, $limit + 1);
                if (!is_string($bytes) || strlen($bytes) !== $stat['size']) throw new RuntimeException('archive_read_failed');
            } finally { fclose($file); }
            $value = json_decode($bytes, true, 48, JSON_THROW_ON_ERROR);
            if (!is_array($value)) throw new RuntimeException('archive_json_invalid');
            return [$value, hash('sha256', $bytes), $stat['mtime']];
        }
        function v2_archive_money($value): array {
            if (!is_array($value) || !is_string($value['amount'] ?? null) || !preg_match('/^(?:0|[1-9][0-9]{0,11})(?:\.[0-9]{1,2})?$/D', $value['amount'])
                || !is_string($value['currency'] ?? null) || !preg_match('/^[A-Z]{3}$/D', $value['currency'])) throw new RuntimeException('archive_money_invalid');
            return ['amount'=>$value['amount'], 'currency'=>$value['currency']];
        }
        function v2_archive_units(array $value): int {
            $parts = explode('.', $value['amount']);
            return (int)$parts[0] * 100 + (int)str_pad($parts[1] ?? '', 2, '0');
        }
        function v2_archive_money_read(string $private, array $reservation, int $finished): array {
            $directory = $private . '/searches';
            if (realpath($directory) !== $directory || !is_dir($directory)) throw new RuntimeException('archive_store_missing');
            $source = '64706822fc54f4d4423ea6bbd0ad966144151387';
            $start = $reservation['created_at'] ?? null;
            if (!is_int($start) || $start < 1 || $finished < $start) throw new RuntimeException('archive_time_invalid');
            $matches = []; $entries = 0; $sidecars = 0;
            foreach (new DirectoryIterator($directory) as $entry) {
                if ($entry->isDot()) continue;
                if (++$entries > 10000) throw new RuntimeException('archive_scan_limit');
                $name = $entry->getFilename();
                if (!preg_match('/^([a-f0-9]{64})-([1-9][0-9]{0,11})-1-(offer_[a-f0-9]{64})-surcharge-v1\.json$/D', $name, $parts)) continue;
                if (++$sidecars > 128) throw new RuntimeException('archive_scan_limit');
                [$record, $digest] = v2_archive_json($directory . '/' . $name, 16384);
                $ctx = $record['context'] ?? [];
                if (($ctx['generation'] ?? null) !== 17171215) continue;
                if (($record['source'] ?? null) !== $source || ($record['status'] ?? null) !== 'complete' || ($record['version'] ?? null) !== 1
                    || ($record['implementation_sha256'] ?? null) !== '28995a73f46f3e1a9d953c034cd5bf514d53c6c4d46d71d5ba593b7d255a8112'
                    || ($ctx['provider'] ?? null) !== 'andromeda' || ($ctx['search_ref'] ?? null) !== $parts[1]
                    || ($ctx['page'] ?? null) !== 1 || ($ctx['offer_ref'] ?? null) !== $parts[3] || ($ctx['operator_ref'] ?? null) !== '5'
                    || !array_key_exists('hotel_scope', $ctx) || $ctx['hotel_scope'] !== null || !is_int($ctx['local_id'] ?? null) || $ctx['local_id'] < 1
                    || ($record['snapshot_created_at'] ?? null) !== (int)$parts[2]
                    || !is_int($record['observed_at'] ?? null) || $record['observed_at'] < $start || $record['observed_at'] > $finished) throw new RuntimeException('archive_context_mismatch');
                $matches[] = [$record, $digest, $parts];
            }
            if (count($matches) !== 1) throw new RuntimeException(count($matches) ? 'archive_ambiguous' : 'archive_not_retained');
            [$record, $surchargeHash, $parts] = $matches[0]; $ctx = $record['context'];
            [$first, $searchHash] = v2_archive_json($directory . '/' . $parts[1] . '-1.json', 3000000);
            $store = $first['store'] ?? []; $criteria = $store['criteria'] ?? [];
            if (!in_array($first['status'] ?? null, ['complete','partial'], true) || ($first['generation'] ?? null) !== 17171215
                || ($store['generation'] ?? null) !== 17171215 || ($store['search_ref'] ?? null) !== $parts[1]
                || ($store['created_at'] ?? null) !== (int)$parts[2] || ($store['snapshot']['page'] ?? null) !== 1
                || ($first['criteria'] ?? null) !== $criteria || hash('sha256', json_encode($criteria, JSON_THROW_ON_ERROR)) !== ($record['criteria_sha256'] ?? null)) throw new RuntimeException('archive_search_mismatch');
            foreach (['CHECKIN_BEG'=>'20261215','CHECKIN_END'=>'20261215','ADULT'=>'2','CHILD'=>'1','AGES'=>'8','NIGHTS_FROM'=>'7','NIGHTS_TILL'=>'7','OPERATORS'=>'5','PAGE'=>'1'] as $key=>$wanted) {
                if ((!is_int($criteria[$key] ?? null) && !is_string($criteria[$key] ?? null)) || (string)$criteria[$key] !== $wanted) throw new RuntimeException('archive_search_mismatch');
            }
            if (isset($criteria['HOTELS']) || $store['created_at'] < $start || $store['created_at'] > $finished) throw new RuntimeException('archive_search_mismatch');
            $found = [];
            foreach ($store['snapshot']['offers'] ?? [] as $offer) if (($offer['offer_ref'] ?? null) === $ctx['offer_ref']) $found[] = $offer;
            if (count($found) !== 1) throw new RuntimeException('archive_offer_mismatch');
            $offer = $found[0];
            if (($offer['local_hotel_id'] ?? null) !== $ctx['local_id'] || ($offer['operator_ref'] ?? null) !== '5'
                || ($offer['check_in'] ?? null) !== '2026-12-15' || ($offer['nights'] ?? null) !== 7 || ($offer['adults'] ?? null) !== 2 || ($offer['children'] ?? null) !== 1
                || !is_string($store['raw_ids'][$ctx['offer_ref']] ?? null)
                || hash('sha256', $store['raw_ids'][$ctx['offer_ref']]) !== ($record['supplier_offer_sha256'] ?? null)) throw new RuntimeException('archive_offer_mismatch');
            $stem = $directory . '/' . $parts[1] . '-' . $parts[2] . '-1-' . $parts[3];
            [$package, $packageHash] = v2_archive_json($stem . '-package.json', 3000000);
            $savedPackage = $package['record'] ?? [];
            if (($package['source'] ?? null) !== $source || ($savedPackage['status'] ?? null) !== 'captured' || ($savedPackage['context'] ?? null) !== $ctx
                || !is_array($savedPackage['private_package'] ?? null) || ($savedPackage['package_sha256'] ?? null) !== ($record['package_sha256'] ?? null)
                || hash('sha256', json_encode($savedPackage['private_package'], JSON_THROW_ON_ERROR)) !== $record['package_sha256']) throw new RuntimeException('archive_package_mismatch');
            $fact = $record['fact'] ?? [];
            if (($fact['provider'] ?? null) !== 'andromeda' || ($fact['state'] ?? null) !== 'estimated' || ($fact['arithmetic_applied'] ?? null) !== true
                || ($fact['final_price_verified'] ?? null) !== false || ($fact['surcharge_scope'] ?? null) !== 'party') throw new RuntimeException('archive_fact_invalid');
            $base = v2_archive_money($offer['price'] ?? null); $factBase = v2_archive_money($fact['search_price'] ?? null);
            $charge = v2_archive_money($fact['party_surcharge'] ?? null); $total = v2_archive_money($fact['search_price_with_surcharge'] ?? null);
            if ($base['currency'] !== $factBase['currency'] || $base['currency'] !== $charge['currency'] || $base['currency'] !== $total['currency']
                || v2_archive_units($base) !== v2_archive_units($factBase) || v2_archive_units($base) <= 0
                || v2_archive_units($base) + v2_archive_units($charge) !== v2_archive_units($total)
                || ($fact['search_price_with_surcharge']['source'] ?? null) !== 'derived_search_estimate') throw new RuntimeException('archive_money_mismatch');
            $reported = v2_archive_money($fact['transport_markup_reported'] ?? null);
            $result = ['archive_only'=>true,'operation_replayed'=>false,'runtime_source'=>$source,
                'generation'=>17171215,'local_hotel_id'=>$ctx['local_id'],'date'=>'2026-12-15','nights'=>7,'adults'=>2,'child_ages'=>[8],
                'operator'=>'ANEX','base_search_price'=>$base,'transport_markup_reported'=>$reported,'party_surcharge'=>$charge,'recorded_estimate'=>$total,
                'observed_at'=>$record['observed_at'],'expires_at'=>$record['expires_at'] ?? null,'final_price_verified'=>false,
                'historical_api_response_recorded'=>false,'browser_display_verified'=>false,'calc_called'=>false,
                'search_sha256'=>$searchHash,'surcharge_sha256'=>$surchargeHash,'package_file_sha256'=>$packageHash];
            foreach (['hotel','room','placement'] as $key) {
                $text=$offer[$key] ?? null;
                if (is_string($text) && strlen($text)<=300 && preg_match('//u',$text) && !preg_match('/[\x00-\x1f\x7f]/',$text)) $result[$key]=$text;
            }
            $result['currency_rates_reported']=[];
            foreach ($fact['operator_currency_rates_reported'] ?? [] as $rate) {
                if (!is_array($rate) || !is_string($rate['currency'] ?? null) || !preg_match('/^[A-Z]{3}$/D',$rate['currency'])
                    || !is_string($rate['rate'] ?? null) || !preg_match('/^(?:0|[1-9][0-9]{0,11})(?:\.[0-9]{1,6})?$/D',$rate['rate'])) throw new RuntimeException('archive_rate_invalid');
                $result['currency_rates_reported'][]=['currency'=>$rate['currency'],'rate'=>$rate['rate']];
                if (count($result['currency_rates_reported'])>16) throw new RuntimeException('archive_rate_invalid');
            }
            return $result;
        }
      SURCHARGE_V2_READBACK_PHP: |
        error_reporting(0);ini_set('display_errors','0');ini_set('log_errors','0');
        $operation='andromeda-search-surcharge-e2e-1717-v2-egypt-2026-12-15-2a1c8';
        $out=['status'=>'blocked','operation'=>$operation,'supplier_calls'=>0,'database_reads'=>0,'database_writes'=>0,'booking_calls'=>0,'filesystem_writes'=>0,'operation_replayed'=>false];
        function anytour_safe_money_readback($value){
          if(!is_array($value)||!is_string($value['amount']??null)||!preg_match('/^(?:0|[1-9][0-9]{0,11})(?:\.[0-9]{1,4})?$/D',$value['amount'])||!is_string($value['currency']??null)||!preg_match('/^[A-Z]{3}$/D',$value['currency']))return null;
          return ['amount'=>$value['amount'],'currency'=>$value['currency']];
        }
        try{
          $root=realpath(getcwd());if(PHP_SAPI!=='cli'||!$root||basename($root)!=='anytoour.ru')throw new RuntimeException('project_invalid');
          $private=dirname($root,2).'/.anytoour-andromeda';
          if(realpath($private)!==$private||!is_dir($private)||!is_readable($private))throw new RuntimeException('private_directory_unavailable');
          $directory=$private.'/'.$operation;$exists=file_exists($directory);$out['operation_exists']=$exists;
          if(is_link($directory)||($exists&&(realpath($directory)!==$directory||!is_dir($directory)||!is_readable($directory))))throw new RuntimeException('checkpoint_path_invalid');
          if(!$exists){$out['checkpoint_state']='absent';$out['status']='inspected';echo json_encode($out,JSON_THROW_ON_ERROR);return;}
          $out['files']=[];$reservationPresent=false;$resultPresent=false;$reservation=[];$resultModified=0;
          foreach(['reservation','result'] as $name){
            $path=$directory.'/'.$name.'.json';$out['files'][$name]=['present'=>false];
            if(is_link($path))throw new RuntimeException('checkpoint_path_invalid');if(!file_exists($path))continue;
            if(realpath($path)!==$path||!is_file($path))throw new RuntimeException('checkpoint_path_invalid');
            $file=fopen($path,'rb');if(!$file)throw new RuntimeException('checkpoint_unreadable');
            try{$stat=fstat($file);if($stat['nlink']!==1||$stat['size']>65536)throw new RuntimeException('checkpoint_size_or_link');$bytes=stream_get_contents($file,65537);if(!is_string($bytes)||strlen($bytes)!==$stat['size'])throw new RuntimeException('checkpoint_read_failed');}finally{fclose($file);}
            $value=json_decode($bytes,true,24,JSON_THROW_ON_ERROR);if(!is_array($value)||($value['operation']??null)!==$operation)throw new RuntimeException('checkpoint_operation_mismatch');
            $out['files'][$name]=['present'=>true,'sha256'=>hash('sha256',$bytes)];
            if($name==='reservation'){
              $reservationPresent=true;$reservation=$value;
              foreach(['runtime_source'=>40,'scenario_sha256'=>64] as $key=>$length){if(!is_string($value[$key]??null)||!preg_match('/^[a-f0-9]{'.$length.'}$/D',$value[$key]))throw new RuntimeException('reservation_invalid');$out[$key]=$value[$key];}
            }else{
              $resultPresent=true;$resultModified=$stat['mtime'];$status=$value['status']??null;if(!in_array($status,['blocked','unknown','complete'],true))throw new RuntimeException('result_status_invalid');
              $out['operation_status']=$status;$out['checkpoint_state']=$status;
              $phase=$value['phase']??null;$phases=['preflight','reserved','search','surcharge_capture','retained_listing','listing_recorded','quote','complete'];$out['phase']=is_string($phase)&&in_array($phase,$phases,true)?$phase:null;
              $reason=$value['reason']??null;$out['reason']=is_string($reason)&&preg_match('/^[A-Za-z0-9_.:-]{1,96}$/D',$reason)?$reason:($reason===null?null:'unclassified_operation_failure');$out['reason_sha256']=is_string($reason)?hash('sha256',$reason):null;
              $out['outcome']=in_array($value['outcome']??null,['served_quote_compared','surcharge_unavailable','listing_estimate_applied'],true)?$value['outcome']:null;
              foreach(['supplier_scenario','mapping_writes','booking_calls'] as $key){$out[$key]=is_int($value[$key]??null)?$value[$key]:null;}
              $out['calc_calls']=(is_int($value['calc_calls']??null)||($value['calc_calls']??'x')===null)?($value['calc_calls']??null):null;
              foreach(['received_offers','mapped_offers','local_hotel_id','supplier_counter_delta','relative_delta_bps'] as $key){if(is_int($value[$key]??null))$out[$key]=$value[$key];}
              if(is_array($value['listing_money']??null)){
                $listing=$value['listing_money'];$out['listing_money']=['base'=>anytour_safe_money_readback($listing['base']??null),'surcharge'=>anytour_safe_money_readback($listing['surcharge']??null),'served_price'=>anytour_safe_money_readback($listing['served_price']??null),'price_basis'=>in_array($listing['price_basis']??null,['search_base','transport_surcharge_estimate'],true)?$listing['price_basis']:null,'arithmetic_applied'=>is_bool($listing['arithmetic_applied']??null)?$listing['arithmetic_applied']:null,'final_price_verified'=>is_bool($listing['final_price_verified']??null)?$listing['final_price_verified']:null];
              }
              if(is_array($value['quote_observation']??null)){
                $quote=$value['quote_observation'];$out['quote_observation']=['served_price'=>anytour_safe_money_readback($quote['served_price']??null),'final_price'=>anytour_safe_money_readback($quote['final_price']??null),'signed_delta_amount'=>is_string($quote['signed_delta_amount']??null)&&preg_match('/^-?(?:0|[1-9][0-9]{0,11})(?:\.[0-9]{1,4})?$/D',$quote['signed_delta_amount'])?$quote['signed_delta_amount']:null,'absolute_delta_amount'=>is_string($quote['absolute_delta_amount']??null)&&preg_match('/^(?:0|[1-9][0-9]{0,11})(?:\.[0-9]{1,4})?$/D',$quote['absolute_delta_amount'])?$quote['absolute_delta_amount']:null,'relative_delta_bps'=>is_int($quote['relative_delta_bps']??null)?$quote['relative_delta_bps']:null,'price_basis'=>in_array($quote['price_basis']??null,['search_base','transport_surcharge_estimate'],true)?$quote['price_basis']:null,'final_price_verified'=>($quote['final_price_verified']??null)===true];
              }
            }
          }
          if(!$resultPresent)$out['checkpoint_state']=$reservationPresent?'reserved_no_result':'directory_without_checkpoint';
          if($resultPresent && $reservationPresent){
            if($out['files']['reservation']['sha256']!=='d08344fd8b04ac3e783386bfe99cddb94e14479e97eb1ae5cab85f2607dd5178'
              ||$out['files']['result']['sha256']!=='2f2cbaa68f013bc1f502fb6dadc8ae22e29cb521feffed40f72bdfc4d4a8efcf'
              ||$out['scenario_sha256']!=='5f2331f77dc8cf00ef912176e454354852fd12665ddc384d0d36bc7dd455d1fa'){
              $out['archive_error']='operation_pin_mismatch';
            }else{$out['archival_money']=v2_archive_money_read($private,$reservation,$resultModified);}
          }
          $out['status']='inspected';
        }catch(Throwable $e){$allowed=['project_invalid','private_directory_unavailable','checkpoint_path_invalid','checkpoint_unreadable','checkpoint_size_or_link','checkpoint_read_failed','checkpoint_operation_mismatch','reservation_invalid','result_status_invalid','archive_path_invalid','archive_unreadable','archive_size_or_link','archive_read_failed','archive_json_invalid','archive_store_missing','archive_time_invalid','archive_scan_limit','archive_context_mismatch','archive_ambiguous','archive_not_retained','archive_search_mismatch','archive_offer_mismatch','archive_package_mismatch','archive_fact_invalid','archive_money_invalid','archive_money_mismatch','archive_rate_invalid'];$out['inspection_error']=in_array($e->getMessage(),$allowed,true)?$e->getMessage():'checkpoint_invalid';}
        echo json_encode($out,JSON_THROW_ON_ERROR);
    steps:
      - name: Exercise fixed v2 checkpoint classification without SSH
        if: github.event_name == 'pull_request'
        run: |
          python3 - <<'PY'
          import json,os,pathlib,subprocess,tempfile
          operation='andromeda-search-surcharge-e2e-1717-v2-egypt-2026-12-15-2a1c8'
          with tempfile.TemporaryDirectory() as tmp:
              account=pathlib.Path(tmp)/'account';root=account/'www/anytoour.ru';root.mkdir(parents=True);private=account/'.anytoour-andromeda';private.mkdir();directory=private/operation
              def run():
                  p=subprocess.run(['php','-d','allow_url_fopen=0','-r',os.environ['ARCHIVAL_MONEY_PHP']+os.environ['SURCHARGE_V2_READBACK_PHP']],cwd=root,text=True,capture_output=True,check=True,timeout=5);assert p.stderr=='' and 'PRIVATE_SENTINEL' not in p.stdout;result=json.loads(p.stdout);assert all(result[k]==0 for k in ['supplier_calls','database_reads','database_writes','booking_calls','filesystem_writes']);assert result['operation_replayed'] is False;return result
              assert run()['checkpoint_state']=='absent';directory.mkdir();(directory/'reservation.json').write_text(json.dumps({'operation':operation,'runtime_source':'64706822fc54f4d4423ea6bbd0ad966144151387','scenario_sha256':'a'*64,'private':'PRIVATE_SENTINEL'}));assert run()['checkpoint_state']=='reserved_no_result'
              (directory/'result.json').write_text(json.dumps({'operation':operation,'status':'unknown','phase':'quote','reason':'served_quote_observation_invalid','supplier_scenario':1,'mapping_writes':0,'booking_calls':0,'calc_calls':None,'private':'PRIVATE_SENTINEL'}));result=run();assert result['checkpoint_state']=='unknown' and result['phase']=='quote' and result['reason']=='served_quote_observation_invalid' and result['calc_calls'] is None
              assert result['archive_error']=='operation_pin_mismatch' and 'archival_money' not in result
          print('Fixed v2 checkpoint readback: absent/reserved/unknown classification and privacy pass; supplier/network0')
          PY
      - name: Verify archive binding and recorded money without network
        if: github.event_name == 'pull_request'
        run: |
          python3 - <<'PY'
          import os,subprocess
          fixture=r'''
          $root=sys_get_temp_dir().'/andromeda-archive-'.bin2hex(random_bytes(8));mkdir($root,0700);mkdir($root.'/searches',0700);
          $ref=str_repeat('a',64);$offerRef='offer_'.str_repeat('b',64);$stem=$root.'/searches/'.$ref.'-100-1-'.$offerRef;
          $criteria=['CHECKIN_BEG'=>'20261215','CHECKIN_END'=>'20261215','ADULT'=>2,'CHILD'=>1,'AGES'=>'8','NIGHTS_FROM'=>7,'NIGHTS_TILL'=>7,'OPERATORS'=>'5','PAGE'=>1];
          $ctx=['provider'=>'andromeda','search_ref'=>$ref,'generation'=>17171215,'page'=>1,'offer_ref'=>$offerRef,'operator_ref'=>'5','hotel_scope'=>null,'local_id'=>42];
          $source='64706822fc54f4d4423ea6bbd0ad966144151387';$claim=['private'=>'PRIVATE_SENTINEL'];
          $offer=['offer_ref'=>$offerRef,'local_hotel_id'=>42,'operator_ref'=>'5','check_in'=>'2026-12-15','nights'=>7,'adults'=>2,'children'=>1,'hotel'=>'Test hotel','price'=>['amount'=>'100000','currency'=>'RUB','fees'=>'unknown']];
          $record=['version'=>1,'status'=>'complete','source'=>$source,'implementation_sha256'=>'28995a73f46f3e1a9d953c034cd5bf514d53c6c4d46d71d5ba593b7d255a8112','context'=>$ctx,'snapshot_created_at'=>100,'observed_at'=>101,'expires_at'=>401,'criteria_sha256'=>hash('sha256',json_encode($criteria)),'supplier_offer_sha256'=>hash('sha256','PRIVATE_SENTINEL'),'package_sha256'=>hash('sha256',json_encode($claim)),'fact'=>['provider'=>'andromeda','state'=>'estimated','arithmetic_applied'=>true,'final_price_verified'=>false,'surcharge_scope'=>'party','search_price'=>['amount'=>'100000.00','currency'=>'RUB'],'party_surcharge'=>['amount'=>'18000.00','currency'=>'RUB'],'search_price_with_surcharge'=>['amount'=>'118000.00','currency'=>'RUB','source'=>'derived_search_estimate'],'transport_markup_reported'=>['amount'=>'200','currency'=>'USD'],'operator_currency_rates_reported'=>[['currency'=>'USD','rate'=>'90'],['currency'=>'RUB','rate'=>'1']]]];
          $search=['status'=>'complete','generation'=>17171215,'criteria'=>$criteria,'store'=>['generation'=>17171215,'search_ref'=>$ref,'created_at'=>100,'expires_at'=>1000,'criteria'=>$criteria,'raw_ids'=>[$offerRef=>'PRIVATE_SENTINEL'],'snapshot'=>['page'=>1,'offers'=>[$offer]]]];
          $package=['source'=>$source,'record'=>['status'=>'captured','context'=>$ctx,'private_package'=>$claim,'package_sha256'=>$record['package_sha256']]];
          $write=fn($p,$v)=>file_put_contents($p,json_encode($v));
          try{
            $write($stem.'-surcharge-v1.json',$record);$write($stem.'-package.json',$package);$write($root.'/searches/'.$ref.'-1.json',$search);
            $before=[];foreach(glob($root.'/searches/*') as $p)$before[$p]=hash_file('sha256',$p);
            $out=v2_archive_money_read($root,['created_at'=>99],103);
            if($out['recorded_estimate']['amount']!=='118000.00'||$out['party_surcharge']['amount']!=='18000.00'||$out['final_price_verified']!==false||$out['historical_api_response_recorded']!==false||str_contains(json_encode($out),'PRIVATE_SENTINEL'))throw new RuntimeException('positive/privacy failed');
            foreach($before as $p=>$h)if(hash_file('sha256',$p)!==$h)throw new RuntimeException('mutation');
            $bad=$record;$bad['source']=str_repeat('c',40);$write($stem.'-surcharge-v1.json',$bad);
            try{v2_archive_money_read($root,['created_at'=>99],103);throw new RuntimeException('bad accepted');}catch(RuntimeException $e){if($e->getMessage()!=='archive_context_mismatch')throw $e;}
            $write($stem.'-surcharge-v1.json',$record);$bad=$search;$bad['store']['raw_ids'][$offerRef]='FOREIGN';$write($root.'/searches/'.$ref.'-1.json',$bad);
            try{v2_archive_money_read($root,['created_at'=>99],103);throw new RuntimeException('bad accepted');}catch(RuntimeException $e){if($e->getMessage()!=='archive_offer_mismatch')throw $e;}
            $write($root.'/searches/'.$ref.'-1.json',$search);$bad=$record;$bad['fact']['party_surcharge']['amount']='18001';$write($stem.'-surcharge-v1.json',$bad);
            try{v2_archive_money_read($root,['created_at'=>99],103);throw new RuntimeException('bad accepted');}catch(RuntimeException $e){if($e->getMessage()!=='archive_money_mismatch')throw $e;}
            unlink($stem.'-surcharge-v1.json');symlink($stem.'-package.json',$stem.'-surcharge-v1.json');
            try{v2_archive_money_read($root,['created_at'=>99],103);throw new RuntimeException('bad accepted');}catch(RuntimeException $e){if($e->getMessage()!=='archive_path_invalid')throw $e;}
            unlink($stem.'-surcharge-v1.json');
            try{v2_archive_money_read($root,['created_at'=>99],103);throw new RuntimeException('bad accepted');}catch(RuntimeException $e){if($e->getMessage()!=='archive_not_retained')throw $e;}
            echo "Archive binding/money/privacy/missing cases pass; supplier0\n";
          }finally{foreach(glob($root.'/searches/*') as $p)unlink($p);rmdir($root.'/searches');rmdir($root);}
          '''
          subprocess.run(['php','-d','allow_url_fopen=0','-r',os.environ['ARCHIVAL_MONEY_PHP']+fixture],check=True,timeout=10)
          PY
      - name: Bind owner-only archival inspection to the terminal execution
        if: github.event_name != 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            if (process.env.GITHUB_RUN_ATTEMPT !== '1' || process.env.GITHUB_TRIGGERING_ACTOR !== 'pyatkoff' || context.actor !== 'pyatkoff' || context.payload.sender?.id !== 226193297 || context.ref !== 'refs/heads/main') throw new Error('Owner-only single inspection required.');
            if (context.eventName !== 'issue_comment' || context.payload.action !== 'created' || context.payload.issue?.number !== 996 || context.payload.issue?.pull_request || context.payload.comment?.user?.id !== 226193297 || context.payload.comment?.body !== '/inspect-andromeda-surcharge-e2e-1717-v2-money') throw new Error('Exact archival inspection command required.');
            const {data: main}=await github.rest.git.getRef({...context.repo,ref:'heads/main'});if(main.object.sha!==context.sha)throw new Error('Main advanced; stop.');
            const {data: sourceComment}=await github.rest.issues.getComment({...context.repo,comment_id:5671360276});
            if(sourceComment.user?.id!==226193297||sourceComment.created_at!=='2026-09-14T21:56:50Z'||sourceComment.updated_at!=='2026-09-14T21:56:50Z'||sourceComment.body!=='/run-andromeda-surcharge-e2e-1717-v2')throw new Error('Source command provenance mismatch.');
            const {data: run}=await github.rest.actions.getWorkflowRun({...context.repo,run_id:34901558904});
            if(run.head_sha!=='30106212b88616187e63fec625f91673576a0643'||run.event!=='issue_comment'||run.status!=='completed'||run.conclusion!=='failure'||run.run_attempt!==1)throw new Error('Terminal v2 run mismatch.');
            const {data: receipt}=await github.rest.actions.getArtifact({...context.repo,artifact_id:10370532737});
            if(receipt.expired!==false||receipt.workflow_run?.id!==34901558904||receipt.workflow_run?.head_sha!=='30106212b88616187e63fec625f91673576a0643'||receipt.digest!=='sha256:83b9f85a611cabff94fa7ae7535289101f80ed812d635d40f108ca4bc7959266')throw new Error('Terminal v2 fallback receipt mismatch.');
            const {data: readback}=await github.rest.actions.getArtifact({...context.repo,artifact_id:10370668015});
            if(readback.expired!==false||readback.workflow_run?.id!==34902201040||readback.digest!=='sha256:e9eb097cdf8760d3b915d671ba1efb0f9d3f1ea0ffa8df1c3fb3592af2594476')throw new Error('Fixed classification receipt mismatch.');
      - uses: actions/checkout@v4
        if: github.event_name != 'pull_request'
        with:
          ref: 95fa11f5a1cb7e18c905c9760a1eae7f2c0ee7aa
          path: helper
          persist-credentials: false
      - name: Read only retained search package and surcharge money
        if: github.event_name != 'pull_request'
        env:
          ANYTOOUR_DEPLOY_SSH_KEY: ${{ secrets.ANYTOOUR_DEPLOY_SSH_KEY }}
          ANYTOOUR_DEPLOY_HOST: ${{ secrets.ANYTOOUR_DEPLOY_HOST }}
          ANYTOOUR_DEPLOY_USER: ${{ secrets.ANYTOOUR_DEPLOY_USER }}
          PYTHONPATH: helper/scripts/diagnostics
        run: |
          python3 - <<'PY'
          import json,os,pathlib
          from anex_search3_owner_decisions import ssh_php
          out=pathlib.Path(os.environ['RUNNER_TEMP'])/'andromeda-surcharge-e2e-v2-readback';out.mkdir(mode=0o700)
          try: result=ssh_php(os.environ['ARCHIVAL_MONEY_PHP']+os.environ['SURCHARGE_V2_READBACK_PHP'],{},maximum_bytes=65536)
          except Exception:
              (out/'result.json').write_text(json.dumps({'status':'inspection_unconfirmed','operation_replayed':False}));raise
          result['control_source']=os.environ['GITHUB_SHA'];(out/'result.json').write_text(json.dumps(result,sort_keys=True,indent=2)+'\n')
          if result.get('status')!='inspected' or result.get('operation_replayed') is not False or any(result.get(k)!=0 for k in ['supplier_calls','database_reads','database_writes','booking_calls','filesystem_writes']):raise SystemExit('V2 archive inspection incomplete; operation not replayed')
          if result.get('archival_money',{}).get('archive_only') is not True:raise SystemExit('Retained money not recovered; inspect receipt without supplier replay')
          print(json.dumps(result,sort_keys=True))
          PY
      - name: Preserve sanitized v2 archival money
        if: always() && github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: andromeda-surcharge-e2e-v2-money-${{ github.run_id }}
          path: ${{ runner.temp }}/andromeda-surcharge-e2e-v2-readback/
          if-no-files-found: error
          retention-days: 14