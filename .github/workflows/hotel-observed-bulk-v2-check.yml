name: Hotel observed bulk stage2 check
on:
  pull_request:
    paths:
      - 'scripts/diagnostics/hotel_observed_bulk_reconcile_v2.php'
      - 'tests/hotel-observed-bulk-reconcile-v2-test.php'
      - 'tests/hotel_observed_bulk_reconcile_v2_source_test.py'
      - '.github/workflows/hotel-observed-bulk-v2-check.yml'
permissions: {contents: read}
jobs:
  bulk-v2:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
          MYSQL_DATABASE: anex_link_test
        ports: ['3306:3306']
        options: >-
          --health-cmd="mysqladmin ping --protocol=TCP --host=127.0.0.1 --port=3306 --silent"
          --health-interval=5s --health-timeout=5s --health-retries=20
    steps:
      - uses: actions/checkout@v4
        with: {persist-credentials: false}
      - run: |
          php -l scripts/diagnostics/hotel_observed_bulk_reconcile_v2.php
          python3 -B tests/hotel_observed_bulk_reconcile_v2_source_test.py
      - name: Reconcile canonical/geo observations on isolated MySQL
        env:
          ANEX_LINK_TEST_DSN: mysql:host=127.0.0.1;port=3306;dbname=anex_link_test;charset=utf8mb4
        run: php tests/hotel-observed-bulk-reconcile-v2-test.php
